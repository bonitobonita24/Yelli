# Project State — Yelli

# Auto-generated by Claude Code after every task. Never edit manually.

# Updated: 2026-05-14 by CLAUDE_CODE

PHASE: Phase 4 Part 5e complete
LAST_DONE: Part 5e — Admin pages + Super-admin (platform) pages + supporting tRPC routers. 16 files net (15 new + 1 audit.ts widened): packages/ui adds badge/alert/table primitives + Recharts CSS vars (--chart-1..5 light+dark); apps/web adds adminProcedure + superAdminProcedure to trpc.ts; departments router extends with create/update/delete/csvImport/regenerateDeviceToken; 3 new routers (admin = users+settings+reports+dashboard, billing = subscription+invoices+Xendit checkout 503-graceful, superadmin = orgs+platformSettings via platformPrisma); /admin/* (layout RSC role guard + AdminSidebar + dashboard with Recharts AreaChart + departments CRUD/CSV/device-token + users invite/role/deactivate + settings + billing + reports CSV download); /superadmin/* (layout isSuperAdmin RSC guard + organizations list/suspend/unsuspend + platform-settings singleton form). packages/db/src/audit.ts: widened writeAuditLog parameter from Prisma.TransactionClient to AuditLogWriter structural type — accepts both base + L6-extended client's $transaction callback (which diverge in TS due to exactOptionalPropertyTypes). 3 commits on scaffold/part-5e: Bundle A (d8761bb backend + UI primitives), Bundle B (d61d383 admin UI core), Bundle C (e649403 admin extras + superadmin). Direct Opus implementation per Step 2.5b — no Sonnet dispatch (5d retrospective: ≤4 files per Sonnet dispatch or direct Opus when platformPrisma + Recharts complexity present). All mutations wrapped in $transaction with L5 AuditLog writes; super-admin actions audit-prefixed PLATFORM:*; suspend bumps security_version on every active user (security.md AUTH DEFAULTS item 6 — session invalidation on tenant suspension); Xendit 503 fallback parallels LiveKit pattern via err.data.code === SERVICE_UNAVAILABLE check in client. pnpm -w typecheck + pnpm -w lint PASS (7 tasks each) at every bundle.
NEXT: Phase 4 Part 5f — In-call overlays (chat sidebar full-screen-on-mobile, file dropzone via Kibo UI, whiteboard canvas, recording indicator) + call history (/app/history) + recordings library (/app/recordings) + chat history (/app/chat/[id]) per execution-plan.md Part 5d row (note: original Part 5d was split into 5d=meetings and 5f=in-call-overlays — 5f is the remaining slice). Open NEW Claude Code session, say "Start Part 5f". Est. 11 files / ~75K — Tier 3, prefer ≤4 files per Sonnet dispatch OR direct Opus. Alternative: skip 5f for now and proceed to Part 7 (tools + deploy/compose + push.sh + Socket.IO custom server) which is critical-path for Phase 5 validation.
BLOCKERS: ⏳ CREDENTIALS.md placeholders must be filled before Phase 5 — GitHub PAT, Docker Hub token, SMTP, Komodo URL, Xendit keys, Turnstile prod keys, LiveKit keys. Phase 5e and beyond still proceed without them (Xendit checkout shows graceful 503 Alert when XENDIT_SECRET_KEY env unset, parallel to existing LiveKit pattern).
GIT_BRANCH: main (scaffold/part-5e squash-merged as a77a113 and deleted)
PORTS: APP=43512 DB=43502 CACHE=43504 LIVEKIT_SIGNAL=43532 LIVEKIT_TURN_UDP_START=43537 COTURN=43542 (dev — base 43502)
TOKEN_ESTIMATE: ~95K consumed this session (Opus orientation via ctx_batch_execute + ctx_search + 3 direct implementation bundles + governance writes — within 200K Opus 4.7 budget; tightly under prior 5d session's 110K because no Sonnet dispatch overhead)
CHECKPOINT_TYPE: full
FILES_TOUCHED_PART_5E: [apps/web/src/server/trpc/trpc.ts, apps/web/src/server/trpc/router.ts, apps/web/src/server/trpc/routers/departments.ts, apps/web/src/server/trpc/routers/admin.ts, apps/web/src/server/trpc/routers/billing.ts, apps/web/src/server/trpc/routers/superadmin.ts, apps/web/src/components/admin/admin-sidebar.tsx, apps/web/src/app/admin/layout.tsx, apps/web/src/app/admin/page.tsx, apps/web/src/app/admin/departments/page.tsx, apps/web/src/app/admin/users/page.tsx, apps/web/src/app/admin/settings/page.tsx, apps/web/src/app/admin/billing/page.tsx, apps/web/src/app/admin/reports/page.tsx, apps/web/src/app/superadmin/layout.tsx, apps/web/src/app/superadmin/page.tsx, apps/web/src/app/superadmin/platform-settings/page.tsx, apps/web/package.json, packages/db/src/audit.ts, packages/ui/package.json, packages/ui/src/index.ts, packages/ui/src/components/badge.tsx, packages/ui/src/components/alert.tsx, packages/ui/src/components/table.tsx, packages/ui/src/styles/globals.css, pnpm-lock.yaml, docs/CHANGELOG_AI.md, docs/IMPLEMENTATION_MAP.md, .cline/STATE.md, .cline/memory/agent-log.md, .cline/memory/lessons.md]
TIER_CLASSIFICATION: 3 — heavy (17 source files net + 4 governance + 3 config = 24 files net, 2 modules /admin + /superadmin, depth 3 — implemented directly by Opus 4.7 per Step 2.5b after 5d retrospective lesson that ≤4 files per Sonnet dispatch is the new floor; new 🟤 decisions logged: writeAuditLog signature widened to AuditLogWriter structural type, superAdminProcedure deliberately skips runWithTenantContext, Xendit 503 graceful degradation via err.data.code).
MODELS:
planning: claude-code (Opus 4.7 — V31 primary, Architect role)
execution: claude-opus-4-7 direct (no Sonnet dispatch — Step 2.5b escalation chosen up-front for Part 5e per 5d retrospective; platformPrisma + Recharts + Xendit integration complexity)
governance: gemini-2.5-flash-lite (non-critical doc writes — not invoked this session; Opus inline for all governance)
