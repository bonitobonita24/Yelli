# Project State — Yelli

# Auto-generated by Claude Code after every task. Never edit manually.

# Updated: 2026-05-14 by CLAUDE_CODE

PHASE: Phase 4 Part 7 complete
LAST_DONE: Part 7 — tools/ governance scripts + deploy/compose/ + Dockerfile + push.sh + COMMANDS.md + k8s-scaffold + .socraticodecontextartifacts.json. 32 files added: tools/ workspace (6 files — package.json + .eslintrc.cjs + 4 .mjs scripts using Ajv 2020-12 + js-yaml; DEV_ONLY_KEYS allowlist in check-env for LIVEKIT_TURN_UDP_START/COTURN_PORT/SMTP_UI_PORT; normalize() strips _-&/,()[].: in check-product-sync; hydration-lint excludes /src/server/ /src/lib/ /src/middleware. /src/env.); deploy/compose/{dev,stage,prod}/ × {db,cache,storage,media,pgadmin,app}.yml + pgadmin-servers.json (dev adds infra/MailHog only; stage/prod app+livekit use Traefik labels with APP_DOMAIN routing); deploy/compose/{start.sh,push.sh}; apps/web/{Dockerfile,.dockerignore} (multi-stage pnpm-workspace-aware: deps→builder→runner; non-root nodejs:1001; output: standalone); COMMANDS.md project-root command reference; deploy/k8s-scaffold/README.md (explicitly INACTIVE per Rule 6, locked off); .socraticodecontextartifacts.json (4 entries — gitignored). LiveKit dev uses --dev mode + single UDP 7882; stage/prod use --rtc-port-range-start/end=7882-7892 (11-port UDP map) + Traefik for WSS signaling at livekit-{staging,}.powerbyte.app. Coturn UDP relay 49160-49200. compose env_file = ../../../.env.<env> (3 levels up — templates.md said ../../ which is wrong for the deploy/compose/<env>/ depth). Verification: pnpm tools:validate-inputs ✓ ; pnpm tools:check-env ✗ EXPECTED (8 empty CREDENTIALS.md placeholders — matches Phase 5 pre-flight gate intent) + 4 informational warnings; pnpm tools:check-product-sync ✓ ; pnpm tools:hydration-lint ✓ (66 files, 0 false positives after path exclusion); pnpm -w typecheck ✓ 8/8 ; pnpm -w lint ✓ 8/8 ; docker compose config combined per env produces 11/10/10 valid services dev/stage/prod. Two-stage review PASS. Direct Opus implementation (Step 2.5b) — same approach as Part 5e; no Sonnet dispatch this session because Part 7 is ~32 files of cross-file-consistency YAML/scripts where Traefik label / container_name / volume name patterns must stay identical across envs. 5 new 🟤 decisions logged: 3-level env_file path; LiveKit dev vs stage UDP strategy; DEV_ONLY_KEYS allowlist; normalize() connector strip; Dockerfile workspace-aware multi-stage with pnpm filter ... transitive build.

NEXT: Phase 4 Part 5f OR Phase 4 Part 8. Part 7 unblocks Phase 5 validation (Phase 5 runs the 9 commands including the 4 new tools/ scripts). Two reasonable paths:
  (a) Phase 4 Part 5f — In-call overlays (chat sidebar full-screen-on-mobile, file dropzone via Kibo UI, whiteboard canvas, recording indicator) + call history (/app/history) + recordings library (/app/recordings) + chat history (/app/chat/[id]). Est. 11 files / ~75K — Tier 3, prefer ≤4 files per Sonnet dispatch OR direct Opus per 5d retrospective.
  (b) Phase 4 Part 8 — CI workflows (.github/workflows/ci.yml + docker-publish.yml), MANIFEST.txt, final IMPLEMENTATION_MAP rewrite, README.md generation, SocratiCode initial index trigger. Est. ~6 files / ~50K — SAFE.
Recommended order: 5f first (completes feature surface), then 8 (CI + final governance). If time-constrained, skip 5f and go to 8 — Phase 5 validation works without the in-call overlay UI (those features stub to "coming soon" pages or are referenced from existing /app/call/[id] room).

BLOCKERS: ⏳ CREDENTIALS.md placeholders must be filled before Phase 5 — GitHub PAT, Docker Hub token, SMTP, Komodo URL, Xendit keys, Turnstile prod keys, LiveKit API key/secret, Coturn static-auth-secret. pnpm tools:check-env currently reports 8 empty placeholders across .env.staging and .env.prod (LIVEKIT_API_KEY, LIVEKIT_API_SECRET, COTURN_STATIC_AUTH_SECRET, XENDIT_SECRET_KEY, XENDIT_PUBLIC_KEY, XENDIT_WEBHOOK_TOKEN, NEXT_PUBLIC_TURNSTILE_SITE_KEY, TURNSTILE_SECRET_KEY). This is the EXPECTED pre-Phase-5 state — fill from CREDENTIALS.md after Bootstrap human-fill phase, then re-run check-env to confirm clean. Phase 4 Parts 5f/8 still proceed without them.

GIT_BRANCH: main (scaffold/part-7 squash-merged as aa14b9f and deleted)
PORTS: APP=43512 DB=43502 CACHE=43504 LIVEKIT_SIGNAL=43532 LIVEKIT_TURN_UDP_START=43537 COTURN=43542 (dev — base 43502)
TOKEN_ESTIMATE: ~80K Opus 4.7 — within 200K budget. Lower than 5e's 95K because the work was infrastructure-mechanical (compose YAML templates), no complex runtime logic.
CHECKPOINT_TYPE: full
FILES_TOUCHED_PART_7: [tools/package.json, tools/.eslintrc.cjs, tools/validate-inputs.mjs, tools/check-env.mjs, tools/check-product-sync.mjs, tools/hydration-lint.mjs, deploy/compose/dev/docker-compose.db.yml, deploy/compose/dev/docker-compose.cache.yml, deploy/compose/dev/docker-compose.storage.yml, deploy/compose/dev/docker-compose.infra.yml, deploy/compose/dev/docker-compose.media.yml, deploy/compose/dev/docker-compose.pgadmin.yml, deploy/compose/dev/docker-compose.app.yml, deploy/compose/dev/pgadmin-servers.json, deploy/compose/stage/docker-compose.db.yml, deploy/compose/stage/docker-compose.cache.yml, deploy/compose/stage/docker-compose.storage.yml, deploy/compose/stage/docker-compose.media.yml, deploy/compose/stage/docker-compose.pgadmin.yml, deploy/compose/stage/docker-compose.app.yml, deploy/compose/stage/pgadmin-servers.json, deploy/compose/prod/docker-compose.db.yml, deploy/compose/prod/docker-compose.cache.yml, deploy/compose/prod/docker-compose.storage.yml, deploy/compose/prod/docker-compose.media.yml, deploy/compose/prod/docker-compose.pgadmin.yml, deploy/compose/prod/docker-compose.app.yml, deploy/compose/prod/pgadmin-servers.json, deploy/compose/start.sh, deploy/compose/push.sh, deploy/k8s-scaffold/README.md, apps/web/Dockerfile, apps/web/.dockerignore, COMMANDS.md, .socraticodecontextartifacts.json (gitignored — machine-local), pnpm-lock.yaml, docs/CHANGELOG_AI.md, docs/IMPLEMENTATION_MAP.md, .cline/STATE.md, .cline/memory/agent-log.md, .cline/memory/lessons.md]
TIER_CLASSIFICATION: 3 — heavy (~32 source files + 4 governance + 1 lockfile = 37 files, 2 modules tools+deploy with cross-cutting Docker concerns, depth 2 — implemented directly by Opus 4.7 per Step 2.5b after 5e retrospective lesson that ≤4 files per Sonnet dispatch and cross-env consistency requirements favor direct Opus over dispatched Sonnet).

MODELS:
planning: claude-code (Opus 4.7 — V31 primary, Architect role)
execution: claude-opus-4-7 direct (no Sonnet dispatch — Step 2.5b escalation chosen up-front for Part 7 per 5d+5e retrospective; cross-file consistency requirements across 17 compose files + Dockerfile + 4 tools)
governance: gemini-2.5-flash-lite (non-critical doc writes — not invoked this session; Opus inline for all governance)
