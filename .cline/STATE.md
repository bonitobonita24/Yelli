# Project State — Yelli

# Auto-generated by Claude Code after every task. Never edit manually.

# Updated: 2026-05-28 ~11:58pm GMT+8 by CLAUDE_CODE (Opus 4.7 architect — NEXT-batch lock-in checkpoint. STATE.md is the lone Opus direct write per V32 R1 §4 checkpoint exception; no project-file edits in this session beyond this checkpoint.)

PHASE: **CI recovery sprint FULLY CLOSED — all 3 deferred follow-ups landed. 8/8 CI jobs green on main.**

LAST_DONE: 2026-05-28 ~11:30pm GMT+8 — deferred follow-up 3 of 3 closed:
  - `docs/CHANGELOG_AI.md` — new entry at top: "2026-05-28 — CI Recovery Sprint: 2/8 → 8/8 CI jobs green" documenting all 8 commits + 2 PRs + the 4 locked decisions + 6 modified files + sprint chronology.
  - `docs/IMPLEMENTATION_MAP.md` — new "Latest Update (2026-05-28)" section at top with CI recovery summary + updated Phase 8 Batch B status block showing CI recovery sprint as ✅ CLOSED.
  - `.cline/STATE.md` — this file. Sprint marked fully closed.
  Commit pending: combined chore(governance) commit with the 3 file edits + push to origin/main.

PRIOR DEFERRED FOLLOW-UP STATUS (all 3 now closed):
  1. ✅ DONE (`06dec12`, 2026-05-28) — `.env.test.e2e.example` expanded with local/CI parity docs for bypass auth provider (WEBMASTER_PASSWORD, PLAYWRIGHT_BASE_URL, LIVEKIT_E2E_MOCK).
  2. ✅ DONE (`a50ab77`, 2026-05-28) — lessons.md +7 typed gotchas: turbo globalEnv allowlist (🔴), fixture file_path org prefix (🔴), tRPC camelCase contract (🔴), bypass provider from day one (🟤), prisma generate before CI tasks (🟡), CI workflow trigger scope (🔴), webmaster lookup shared helper (🔴).
  3. ✅ DONE (this session) — CHANGELOG_AI.md + IMPLEMENTATION_MAP.md entries for today's CI-recovery work.

GIT_BRANCH: main
GIT_REMOTE: origin/main — in sync after this session's push (governance commit closes the sprint).
PORTS: unchanged.
TEST_SUITE: 473/473 passing locally.
TYPECHECK: 0 errors.
LINT: 0 errors.
CI: 8/8 green on origin/main at `62fab35` (PR #2 squash).

NEXT — LOCKED IN (2026-05-28 ~11:58pm GMT+8, human-confirmed via "save this tasks and lets start this on the new session"):

🎯 **Overlay Cluster** — In-Call File Sharing + Whiteboard
   Framing: natural continuation of Phase 8 Batch B (overlay cluster is listed as `⬜ NEXT (or deferred…)` in IMPLEMENTATION_MAP). Treat as Batch B finisher OR new Batch C — labeling deferred to next session per IMPLEMENTATION_MAP convention.

   Scope (two co-built features, shared surface area):
   1. **In-Call File Sharing** — drag/drop upload during call, screenshot paste, inline preview, storage gating by plan tier. Source: `docs/PRODUCT.md §File Sharing` (~L50-55).
   2. **In-Call Whiteboard** — real-time collaborative canvas, drawing tools (pen/shapes/text/eraser), multi-user cursors, plan-tier gating. Source: `docs/PRODUCT.md §Whiteboard` (~L57-62).

   Why co-build: one LiveKit room context, one tRPC upload + MinIO storage layer, one canvas WebSocket channel. Splitting would duplicate setup; bundling is cheaper.

   Pre-flight required for fresh session (per phases.md Phase 8 anti-thrashing rule):
   - Read STATE.md (this file) → read lessons.md (🔴 first, 🟤 second) → read PRODUCT.md §File Sharing + §Whiteboard ONLY (do not read full PRODUCT.md).
   - Run `wc -l` on every file in scope before any Sonnet dispatch (V32 R2).
   - Estimate total files: schema additions (Prisma) + tRPC routers + LiveKit room hooks + 2 overlay components + tests. If projected scope > 12 files or > 80K tokens → split into sub-sessions per phases.md Phase 8 anti-thrashing protocol.
   - Apply V32.1 Sonnet subagent context-overhead operational note (memory-governance.md §1) — dispatch prompts ≤ ~1K tokens, per-dispatch tool-use ≤ 5.

   First step the next session should take: scout the overlay scope (single Sonnet scout, ≤300 word summary) covering: existing LiveKit room hook surface, current tRPC upload procedure, MinIO storage wrapper, plan-tier gating helper. THEN propose the file-count split before dispatching any edits.

DEFERRED (revisit after overlay cluster ships):
- Billing & Subscription UI (PRODUCT.md §Billing, L96-108) — likely its own future batch (Xendit-heavy, separate surface).
- Deferred Phase 8 Batch B tier-1 fixes — see `phase8-batch-b-closed.md` memory for the open list.
- Framework V31.3 stash pop — if relevant work was stashed during the CI recovery.

NO BLOCKERS. Sprint fully closed. Knowledge captured in lessons.md (7 typed entries) + CHANGELOG_AI + IMPLEMENTATION_MAP. Next batch confirmed and pre-flight steps documented above.
