# Project State — Yelli

# Auto-generated by Claude Code after every task. Never edit manually.

# Updated: 2026-05-24 12:30 GMT+8 by CLAUDE_CODE (Opus 4.7 architect)

PHASE: **Phase 8 Batch 1 Item 3 (Xendit payment + billing flow) STARTING.** Tier 3 confirmed (score 69.5; threshold 40). Decomposed into 5 atomic sub-sessions on shared feature branch `feat/phase8-item3-xendit-billing-flow` per user-confirmed branching strategy: 3a (Opus inline, ~24K) → 3b-i (Sonnet, ~15K) → 3b-ii (Sonnet, ~17K) → 3c-i (Opus inline, ~16K) → 3c-ii (Sonnet, ~14K) → governance + single squash-merge to main. Locked decisions for Item 3: PDF invoices via Xendit-hosted `pdf_url` (no self-generation); ProcessedWebhookEvent platform-global with `unique(event_id)` (no tenantId, justified by inherently cross-tenant webhook infrastructure semantics). All Item 3 sub-sessions tagged with model assignment per ~20K Sonnet effective budget observed in Item 2 (STATE.md L16 of previous revision).

LAST_DONE: 2026-05-24 12:08 GMT+8 — Phase 8 Batch 1 Item 2 (plan-tier enforcement engine) squash-merged to origin/main as commit `684bf5f`. 19 files / +1620 / -37 / 40 new test cases / typecheck 0 / lint 0 / audit exit 0 / test suite 35 files / 339 tests. Branch `feat/phase8-item2-plan-tier-enforcement` deleted. All four PRODUCT.md L97-104 caps relevant to Item 2 (users / admins / departments / autoAnswerStations) now gated server-side via `assertNumericPlanLimit` + surfaced via `<PlanLimitBanner>` on Speed Dial / Departments / Users pages. (Previous STATE.md revision said "awaiting squash-merge" — stale; reconciled this session.)

NEXT — execute sub-session 3a (Opus inline):
1. Read existing `apps/web/src/server/trpc/routers/billing.ts` (282 lines) for the checkout.createSession pattern to extend.
2. NEW `packages/shared/src/schemas/billing-checkout.ts` — Zod schemas for upgrade input (plan_tier ∈ {pro, enterprise}, billing_cycle ∈ {monthly, annual}, payment_methods array filtered to PRODUCT.md L105) + cancel input.
3. NEW `apps/web/src/lib/xendit/client.ts` — extract the inline Xendit fetch wrapper from billing.ts into a reusable helper (auth header, error mapping, types). Pure server-only.
4. MOD `apps/web/src/server/trpc/routers/billing.ts` — enrich checkout.createSession to accept billing_cycle + payment_methods + redirect URLs; add `subscription.upgrade` (creates Xendit Invoice with selected cycle, returns checkout URL — actual plan flip deferred to webhook) and `subscription.cancel` (sets cancel_at_period_end on Subscription, AuditLog).
5. NEW `apps/web/src/server/trpc/routers/billing-3a.test.ts` — ~10 cases: enriched createSession path, upgrade Pro monthly, upgrade Pro annual, upgrade Enterprise, cancel happy path, env-guard SERVICE_UNAVAILABLE, AuditLog emission, payment_methods filtering, non-admin denial, schema rejection of unknown plan_tier.
6. MOD `packages/shared/src/index.ts` — re-export billing-checkout schemas.
7. Validation per Rule 25 Stage 1+2: pnpm typecheck + pnpm test -- billing-3a.test.ts + pnpm lint.
8. Commit: `feat(billing): enrich Xendit checkout + subscription mutations — Phase 8 Batch 1 Item 3 sub-session 3a`. Single atomic commit on feature branch.

BLOCKERS: None for 3a. Note carried from Item 2 STATE: XENDIT_SECRET_KEY in .env.staging / .env.prod is empty by default (test keys only in .env.dev) — billing.checkout.createSession already handles SERVICE_UNAVAILABLE; same pattern flows to subscription.upgrade in 3a. Webhook path (3b) requires XENDIT_WEBHOOK_TOKEN to validate signatures — same env-empty guard pattern will apply.

GIT_BRANCH: feat/phase8-item3-xendit-billing-flow (0 commits ahead of main at start of 3a; target 5 commits before single squash-merge per user-confirmed Item 2 model).

PORTS: APP=43512 DB=43502 PGBOUNCER=43503 CACHE=43504 MINIO=43505 MINIO_CONSOLE=43506 SMTP=43507 SMTP_UI=43508 PGADMIN=43509 LIVEKIT_SIGNAL=43532 LIVEKIT_UDP=43537 COTURN=43542 SOCKET=43515 — dev stack not exercised this session (3a is code + tests; no DB migration until 3b-ii ProcessedWebhookEvent).

LESSONS_PENDING (carry from Item 2): if Sonnet ~20K effective budget pattern repeats in 3b-i / 3b-ii / 3c-ii dispatches, escalate to 🟤 decision in lessons.md + propose refinement to memory-governance.md §1 Step 2.5 (current docs say 30K Sonnet; observed 20K). Item 3 will be the second data point — three confirmed sub-budget overruns warrants the refinement.

TIER_CLASSIFICATION: Phase 8 Batch 1 Item 3 = Tier 3 (memory-governance.md §1 score = (13 files × 2.5) + (5 modules × 5) + (depth 4 × 3) = 69.5; threshold 40). 3 logical sub-sessions per IMPLEMENTATION_MAP L428-438; further decomposed to 5 atomic tasks per Sonnet 20K budget gate (3b and 3c each split 2-way; 3a kept atomic as Opus inline because mechanical extension of pre-loaded billing.ts pattern).

CHECKPOINT_TYPE: lightweight (this rewrite is pure governance reconciliation; no source files touched yet — full checkpoint will fire after 3a commit per memory-governance.md §2).

FILES_TOUCHED (this session, pre-3a setup):
- .cline/STATE.md (THIS REWRITE — Item 2 close-out + Item 3 startup reconciliation)

MODELS:
  planning:   claude-code (Phase 2 — V31 primary; not invoked this session)
  execution:  claude-opus-4-7 as Architect-controller; planned dispatches: claude-sonnet-4-6 × 3 (3b-i, 3b-ii, 3c-ii) + claude-opus-4-7 inline × 2 (3a, 3c-i) per memory-governance.md §2.5b (sub-session token estimates documented above)
  governance: claude-opus-4-7 inline (same Architect session — CHANGELOG_AI + agent-log + STATE writes after each sub-session)
