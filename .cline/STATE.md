# Project State — Yelli

# Auto-generated by Claude Code after every task. Never edit manually.

# Updated: 2026-05-15 15:30 GMT+8 by CLAUDE_CODE

PHASE: Phase 7 active — first Feature Update merged. Production build still passes. Partial Phase 5 (8 of 9) holds.

LAST_DONE: 2026-05-15 Phase 7 #1 (Opus 4.7 direct, ~55K context) — auth.register tRPC procedure + register page wire-up squash-merged to main as SHA `ce709ff`. 11 files changed, 162 insertions, 56 deletions. Sonnet dispatch attempted first → thrashed at 18 tool uses → escalated to Opus per memory-governance §2.5b (logged in lessons.md as 🟤 decision, first §2.5b escalation, within <20% allotment). Fixed Sonnet's 3 partial-output bugs (snake_case Prisma fields, unenforceable global email pre-check, missing createCallerFactory export). Build initially failed on `.js` extension leftovers in 4 packages/shared barrel files (latent since Part 2 — surfaced by first real consumer; logged as 🔴 gotcha). Tests DEFERRED — vitest not installed; logged as 🟤 decision with follow-up ticket.

NEXT: Continue Phase 7 polish. Two queued follow-ups documented in lessons.md:
  (a) Install vitest infrastructure (Phase 7 #2 prereq — unblocks Rule 25 TDD discipline for all subsequent tickets)
  (b) Wire forgot-password tRPC (Phase 7 punch-list item #2 — pairs naturally with #1)
Also still open: wire join/[token] (punch-list #3), build /app/meetings/new (#5), org-slug in JWT (#4), socket auth (#7), root landing page (#6). User picks order.

CURRENT_BUILD: 22 routes, /register now 5.86 kB (175 kB first-load). Middleware 99.1 kB. 43s.

BLOCKERS:
- CREDENTIALS.md still has 52 ⏳ placeholders across 8 REQUIRED sections — blocks full Phase 5 + Phase 6, does NOT block Phase 7 work on existing code
- vitest not installed → Rule 25 TDD ordering temporarily suspended. Next Phase 7 ticket should be vitest infra so subsequent tickets can resume strict TDD.
- Coturn container restarting (255) — needs COTURN_STATIC_AUTH_SECRET, but dev video calls work without it on localhost

GIT_BRANCH: main (clean working tree after squash-merge + governance docs commit pending below)

PORTS: APP=43512 (dev server on host) DB=43502 PGBOUNCER=43503 CACHE=43504 MINIO=43505 MINIO_CONSOLE=43506 MAILHOG_SMTP=43507 MAILHOG_UI=43508 PGADMIN=43509 LIVEKIT_SIGNAL=43532 LIVEKIT_TURN_UDP_START=43537 COTURN=43542

TOKEN_ESTIMATE: ~120K Opus 4.7 aggregate for the dev-bringup session — heavy due to multiple build retry cycles + multi-file diagnosis (jsdom hunt). Single session, Tier 3 in retrospect but pragmatically completed without sub-dispatch. Current session (Phase 7 prep) is lightweight.

CHECKPOINT_TYPE: full

FILES_TOUCHED_THIS_SESSION:
  modified:
    apps/web/next.config.ts                              (serverExternalPackages: jsdom + isomorphic-dompurify; transpilePackages: +@yelli/storage)
    apps/web/package.json                                (bcrypt→bcryptjs, +sync:livekit-styles script)
    apps/web/src/env.ts                                  (SKIP_ENV_VALIDATION + IS_SERVER guards)
    apps/web/src/middleware.ts                           (use authConfig from auth.config.ts)
    apps/web/src/server/auth.ts                          (extends authConfig, bcrypt→bcryptjs)
    apps/web/src/server/trpc/routers/admin.ts            (bcrypt→bcryptjs import)
    apps/web/src/styles/globals.css                      (LiveKit CSS rationale comment)
    apps/web/src/app/layout.tsx                          (<link rel=stylesheet> for livekit-styles.css)
    apps/web/src/app/(auth)/login/page.tsx               (Suspense wrapper around useSearchParams)
    apps/web/src/app/app/meeting/[id]/page.tsx           (import MeetingRoom from -loader)
    apps/web/src/app/app/call/[id]/page.tsx              (import IntercomCall from -loader)
    apps/web/src/components/meeting/meeting-room.tsx     (removed @livekit/components-styles side-effect import)
    apps/web/src/components/call/intercom-call.tsx       (removed @livekit/components-styles side-effect import)
    apps/web/src/components/speed-dial/speed-dial-grid.tsx (href /app/settings/departments/new → /admin/departments)
    packages/db/package.json                             (bcrypt→bcryptjs, db:seed --env-file-if-exists)
    packages/db/prisma/seed.ts                           (bcryptjs default import: hash → bcrypt.hash)
    packages/storage/src/index.ts                        (removed .js extensions from re-exports)
    pnpm-lock.yaml                                       (bcrypt→bcryptjs)
    .env.dev                                             (SMTP_FROM_NAME quoted; SMTP_FROM dev@yelli.test; PGADMIN_EMAIL dev-admin@yelli.com; +NEXT_PUBLIC_APP_URL)
    CREDENTIALS.md                                       (added Email row to First Admin Account section: webmaster@yelli.local)
  created:
    apps/web/src/server/auth.config.ts                   (edge-safe Auth.js shell)
    apps/web/src/components/meeting/meeting-room-loader.tsx  (dynamic ssr:false wrapper)
    apps/web/src/components/call/intercom-call-loader.tsx     (dynamic ssr:false wrapper)
    apps/web/public/livekit-styles.css                   (copy of @livekit/components-styles)
    scripts/seed-dev.sh                                  (idempotent seed runner — reads CREDENTIALS.md inside shell pipe, never echoes value)
  symlinks created:
    apps/web/.env.local → ../../.env.dev                 (so Next auto-loads dev env during pnpm dev/build)

TIER_CLASSIFICATION: 3 — heavy (20+ files touched across 6 modules, retrospective). Should have been sub-dispatched but completed in a single Opus session because each fix exposed the next and the cumulative graph wasn't visible up-front. Token budget held just under 120K.

PHASE_4_SCAFFOLD_BUGS_FIXED_THIS_SESSION:
  1. packages/storage used `./xxx.js` extensions in imports → broke Next webpack resolution. Fix: removed `.js` extensions to match db/jobs/shared monorepo style.
  2. apps/web/next.config.ts transpilePackages missing @yelli/storage. Fix: added.
  3. Middleware imported full @/server/auth → pulled bcrypt + Prisma into Edge bundle. Fix: split into auth.config.ts (edge-safe shell) + auth.ts (full server). Canonical Auth.js v5 pattern.
  4. bcrypt 5.1.1 had 6 HIGH CVEs (tar transitive via @mapbox/node-pre-gyp) + native-binding install fragility. Fix: drop-in swap to bcryptjs ^2.4.3 (pure JS, same API). Zero HIGH CVEs after.
  5. packages/db/prisma/seed.ts used named `import { hash } from 'bcrypt'` but bcryptjs has no named hash export. Fix: default import + bcrypt.hash(...). Was silent — seed failed at module-load with cryptic ESM error.
  6. apps/web/src/env.ts ran serverSchema.safeParse() at module-load even in browser → client components importing env (e.g. turnstile-widget) crashed. Fix: typeof window === "undefined" guard.
  7. .env.dev had SMTP_FROM_NAME=Yelli Dev (unquoted space → source fails) + SMTP_FROM=dev@localhost (Zod .email() rejects, no TLD) + PGADMIN_EMAIL=dev-admin@yelli.local (RFC 2606 reserved). Fix: quote the value, use dev@yelli.test + dev-admin@yelli.com.
  8. apps/web/src/env.ts schema required NEXT_PUBLIC_APP_URL but .env.dev didn't have it. Fix: appended to .env.dev.
  9. apps/web/src/app/(auth)/login/page.tsx used useSearchParams() without Suspense — Next 15 App Router static prerender requires Suspense boundary. Fix: extracted LoginForm, wrapped with Suspense.
 10. apps/web/src/components/speed-dial/speed-dial-grid.tsx empty-state link `/app/settings/departments/new` (route never built) → 404. Fix: redirected to `/admin/departments` where the actual UI lives.
 11. Build-time env loading: Prisma seed and Next build couldn't see .env.dev because Next only auto-loads `.env`/`.env.local`/`.env.development`. Fix: (a) `tsx --env-file-if-exists=../../.env.dev` for seed; (b) symlink `apps/web/.env.local` → `.env.dev` for Next.

ROOT_CAUSE_OF_BUILD_FAILURE_THAT_LOOKED_LIKE_LIVEKIT:
  The error path `.next/browser/default-stylesheet.css` framed the issue as a LiveKit CSS problem for ~90 minutes. After 7 dead-end CSS-loading approaches (transpilePackages variations, exports-map wildcards, globals.css @import, relative file path, static <link>), grep on the actual chunk revealed:
    p = e.readFileSync(f.resolve(__dirname, "../../browser/default-stylesheet.css"), ...)
  Source: jsdom (transitive via isomorphic-dompurify). jsdom does fs.readFileSync at module load. Webpack bundled jsdom, flattened directory structure → relative path broke during page-data collection. Fix: serverExternalPackages: ["isomorphic-dompurify", "jsdom"]. The LiveKit dynamic-loader pattern + static-CSS approach were preserved as defense-in-depth though they weren't the root cause.

MODELS:
  planning: claude-code (Opus 4.7 — V31 primary, single-session, no sub-dispatch)
  execution: claude-opus-4-7 direct
  governance: gemini-2.5-flash-lite (non-critical doc writes — not invoked this session; Opus inline for all governance)
