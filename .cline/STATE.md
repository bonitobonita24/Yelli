# Project State — Yelli

# Auto-generated by Claude Code after every task. Never edit manually.

# Updated: 2026-05-13 by CLAUDE_CODE

PHASE: Phase 4 Part 5c complete
LAST_DONE: Part 5c — apps/web tRPC v11 server + call initiation router + Socket.IO server skeleton scaffolded (11 files via Architect-Execute: 2 Sonnet sub-dispatches + Opus governance + recovery). 5c-1 tRPC plumbing (8 files): src/server/trpc/{trpc.ts (initTRPC.context<Context>().create + superjson + errorFormatter masking INTERNAL_SERVER_ERROR in prod + authMiddleware UNAUTHORIZED-on-null + tenantMiddleware wrapping next() in runWithTenantContext for L6 + apiRateLimitMiddleware applying rateLimiters.api.check + publicProcedure/protectedProcedure exports), context.ts (FetchCreateContextFnOptions + auth() Session + Context type), router.ts (appRouter root — departmentsRouter only in 5c-1; callsRouter added in 5c-2), routers/departments.ts (list query trusts L6 — no explicit organization_id filter)}; src/app/api/trpc/[trpc]/route.ts (fetchRequestHandler runtime=nodejs GET+POST); src/lib/trpc/{react.tsx ("use client" TRPCReactProvider — QueryClient staleTime 30s + httpBatchLink + superjson + loggerLink dev-only), server.ts ("server-only" createServerCaller for RSC)}. 5c-2 call signaling (3 files + 1 router + modifications): src/lib/socket/{types.ts (ServerToClientEvents + ClientToServerEvents + InterServerEvents + SocketData + callIncomingRoom/callerRoom helpers — re-imports IncomingCallPayload from lib/livekit/types), server.ts (Server<...> generic + globalThis-cached singleton against HMR + initSocketServer(httpServer) idempotent + presence:subscribe/heartbeat/call:reject handlers + emitIncomingCall helper + Phase-6 handshake-auth TODOs)}; src/app/api/socket/route.ts (Route Handler GET+POST returning 503 with explanatory message — actual WebSocket upgrade requires custom Next.js server in Phase 6); src/server/trpc/routers/calls.ts (initiate mutation: Zod.strict{recipientDepartmentId} → prisma.department.findUnique L6-scoped → NOT_FOUND generic on null → randomUUID callId + roomName=`call-${callId}` → mintLiveKitToken try/catch → SERVICE_UNAVAILABLE 503 if env unset → emitIncomingCall via getIO (null io tolerated — caller still gets token, recipient won't ring until Phase 6) → return {callId, roomName, token, wsUrl, recipientDepartmentName} as const; reject mutation: io emit, no CallLog persistence). Modifications: src/app/layout.tsx wrapped {children} in <TRPCReactProvider>; src/server/trpc/router.ts re-registered for callsRouter; apps/web/package.json + socket.io ^4.8.1 server peer; .eslintrc.js + apps/web/.eslintrc.cjs Rule 13 server-side @yelli/db exemption dual-declared. Branch handling: 5c-1 Sonnet over-stepped scope and squash-merged to main as commit 5d82835 — Opus recovered by recreating scaffold/part-5c from main HEAD with explicit ABSOLUTE RULES prohibiting merge/push/branch/checkout in 5c-2 dispatch prompt; 5c-2 obeyed (commit 52dc5da on branch). pnpm install +socket.io ^4.8.1 ✓. pnpm --filter @yelli/web typecheck PASS (0 errors). pnpm --filter @yelli/web lint PASS (0 errors, 4 advisory non-null-assertion warnings on ctx.session!.user pattern accepted — parallel to Auth.js v5 JWT defensive narrowing in auth.ts; refactor to propagate narrowed `user` via next({ctx:{...ctx, user}}) deferred to Part 5d cleanup).
NEXT: Phase 4 Part 5d — open NEW Claude Code session, say "Start Part 5d". Scope per execution-plan: Meeting Management UI — /app/meetings list (DataTable on desktop, card list on mobile) + /app/meetings/new create form + /app/meeting/:id multi-participant LiveKit room (up to 50, screen share, mute/unmute, host controls, moderator promotion) + wire speed-dial-button onClick to trpc.calls.initiate mutation + CallLog persistence at call end (use status enum completed|missed|failed at termination). Cleanup item: refactor tRPC authMiddleware to return narrowed `user` via next({ctx:{...ctx, user}}) so downstream procedures read ctx.user.id instead of ctx.session!.user.id (eliminates 4 advisory warnings). Estimated 10-12 files / ~75K → Tier 3, split into 2 Sonnet sub-dispatches: 5d-1 meetings list+new pages + speed-dial-button wiring + ctx narrowing refactor; 5d-2 meeting room multi-participant LiveKit page + moderator controls + CallLog persistence helper.
BLOCKERS: ⏳ CREDENTIALS.md placeholders must be filled before Phase 5 — GitHub PAT, Docker Hub token, SMTP, Komodo URL, Xendit keys, Turnstile prod keys, LiveKit keys. Parts 5d-8 proceed without them (Socket.IO server is skeleton until Phase 6 custom server; LiveKit token endpoint returns 503 gracefully when env unset; tRPC initiate mutation returns SERVICE_UNAVAILABLE gracefully).
GIT_BRANCH: main (scaffold/part-5c about to squash-merge + delete)
PORTS: APP=43512 DB=43502 CACHE=43504 LIVEKIT_SIGNAL=43532 LIVEKIT_TURN_UDP_START=43537 COTURN=43542 (dev — base 43502)
TOKEN_ESTIMATE: ~120K consumed this session (Opus orientation + ctx_batch_execute for integration-point extraction + 2 Sonnet dispatches + governance writes + branch recovery — well under 200K Opus 4.7 budget, no Opus-side autocompact)
CHECKPOINT_TYPE: full
FILES_TOUCHED_PART_5C: [apps/web/src/server/trpc/trpc.ts, apps/web/src/server/trpc/context.ts, apps/web/src/server/trpc/router.ts, apps/web/src/server/trpc/routers/departments.ts, apps/web/src/server/trpc/routers/calls.ts, apps/web/src/app/api/trpc/[trpc]/route.ts, apps/web/src/lib/trpc/react.tsx, apps/web/src/lib/trpc/server.ts, apps/web/src/lib/socket/types.ts, apps/web/src/lib/socket/server.ts, apps/web/src/app/api/socket/route.ts, apps/web/src/app/layout.tsx, apps/web/package.json, .eslintrc.js, apps/web/.eslintrc.cjs, pnpm-lock.yaml, docs/CHANGELOG_AI.md, docs/IMPLEMENTATION_MAP.md, .cline/STATE.md, .cline/memory/agent-log.md, .cline/memory/lessons.md]
TIER_CLASSIFICATION: 3 — heavy (11 new files + 5 modifications, 1 module, depth 3 — split via Architect-Execute Model into 2 Sonnet sub-sessions each ≤30K per memory-governance.md §1 Step 2.5; 5c-1 returned DONE but over-merged scope — recovered cleanly; 5c-2 returned DONE clean with explicit absolute-rules prohibition — new 🟤 decision logged: dispatch prompts MUST include explicit shell-command-level prohibitions, implicit boundaries are not boundaries)
MODELS:
planning: claude-code (Opus 4.7 — V31 primary, Architect role)
execution: claude-sonnet-4-6 via Claude Code (2 dispatches: 5c-1 DONE-but-over-merged ~20min/46 tools, 5c-2 DONE clean ~14min/25 tools — Opus performed governance + squash-merge)
governance: gemini-2.5-flash-lite (non-critical doc writes — not invoked this session; Opus inline for all governance)
