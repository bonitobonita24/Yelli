# Project State — Yelli

# Auto-generated by Claude Code after every task. Never edit manually.

# Updated: 2026-05-19 23:20 GMT+8 by CLAUDE_CODE

PHASE: Task #21 (admin-users-list-tenant-scope) — COMPLETE 2026-05-19, squash-merged to main as `ab711da`. Confirmed the Rule 16 smoke finding was a real cross-org leak, NOT intentional super-admin visibility. Root cause was a super-admin bypass embedded inside the L6 tenant-guard Prisma extension (`packages/db/src/client.ts:44` originally read `if (!ctx || ctx.isSuperAdmin) return query(args)`) — every tenant_admin who also carried `is_super_admin=true` (the seeded webmaster + any platform-staff dual-role account) silently skipped tenant filtering on EVERY route using the guarded prisma client. Leak surface spanned admin.users.list (where Mallory surfaced in the smoke), plus admin.dashboard.stats counts, admin.reports.exportCallLogsCsv, and the equivalent list queries across calls/meetings/departments/chat/recordings/billing routers. Fix is two-layer per security.md §SUPERADMIN AND PLATFORM-LEVEL ROLES: (a) ROOT CAUSE — removed the `ctx.isSuperAdmin` bypass at the L6 extension; pass-through now only fires for `!ctx` (genuine no-context bootstrap/seed scripts). Cross-tenant code must use the dedicated `platformPrisma` client (`packages/db/src/platform-client.ts`) from a `superAdminProcedure` router — superadmin.ts already does this exclusively, no callers of the bypass exist. (b) DEFENSE-IN-DEPTH — added explicit `where: { organization_id: ctx.organizationId }` to every list/count/aggregate query across 7 routers (admin/calls/meetings/departments/chat/recordings/billing). Also dropped the vestigial `isSuperAdmin` field from `TenantContext` (no longer consumed) and from the `runWithTenantContext` call in trpc.ts. 6 new cross-org isolation tests on admin.test.ts (now 173/173 total). Tier 2 inline execution per user choice (5-12 files, 2-3 modules; final stats: 11 files modified, 1 file added, ~93 insertions / ~39 deletions). Closes the (admin-users-list-tenant-scope) ticket from the Rule 16 cleanup queue.

LAST_DONE: 2026-05-19 Rule 16 cleanup smoke pass (Phase 7 follow-up) — Tier 2 single-session Opus 4.7 controller (~110K context across the full pass given multi-tool browser automation overhead — Playwright MCP browser_snapshot + browser_evaluate + ctx_execute SQL probes + git operations). Driver: Playwright MCP with system Chrome (user installed google-chrome-stable via Google's apt repo since Playwright MCP defaults to /opt/google/chrome/chrome, not the Playwright bundled chromium). Fixtures: `prisma migrate reset --force --skip-generate` to drop+remigrate clean DB, then `pnpm db:seed` (required user-shared WEBMASTER_PASSWORD via line-selection from CREDENTIALS.md), then manual one-shot tsx fixture script (`/tmp/yelli-fixtures.ts`) to create 1 additional org `evil` + 4 test users in system org (alice/bob/carol/david, all `host` role, password `TestPassword12345678AB`, bcrypt cost 12) + 1 user in evil org (mallory) + 3 additional departments (Sales/Support/Front Desk, joining Reception) + bindings (alice→Reception, bob→Sales, david→Front Desk AND Support multi-binding, carol→unbound). PASS RESULTS (6): #8e Socket.IO handshake (curl → HTTP 200 with sid + websocket upgrade + 25s/20s ping config + CORS for :43512; browser session via SocketProvider connects + id verified via React fiber introspection), #10 session:invalidated (bumping security_version via direct SQL triggered redirect to /login in ~120s — note worst case is 2x the 60s loop interval, not the ≤60s the .whatsnext smoke description claims; sign-out alone does NOT trigger session:invalidated, only security_version/role/status changes do), #4 forgot-password (non-enumerating message + MailHog email with reset link + 1hr expiry + reset → new password → login works), #11 presence engine multi-tab (presence:snapshot captured with carol's id appearing once despite 2 sockets open in tabs A+B — multi-tab coalescing verified at server roster level; all 5 expected listeners attached: presence:snapshot=1, presence:user=1, call:active=1, call:active-snapshot=1, session:invalidated=1; closing tab B did not disconnect tab A — isLast=false path verified), #12 Speed Dial green-dot (alice signs in → Reception bound to alice goes "online"+clickable; Sales/Support/Front Desk bound to non-signed-in bob/david remain "offline"+disabled; React fiber state confirmed useUserPresence's onlineSet contains alice's id; minor caveat: first post-sign-in render showed Reception OFFLINE briefly then corrected after socket.disconnect+connect probe — possible initial-mount race, not consistently reproducible), #13 admin-binding-ui (sidebar Dashboard/Departments/Users/Billing/Reports/Settings/Super Admin renders for webmaster; departments table shows all 4 fixtures with bindings; Default user combobox opens with options; non-admin alice → redirected from /admin to /app). PARTIAL (1): #3 /app/meetings/new (form + creation + LiveKit token mint all PASS after env fix; WebRTC peer connection fails with "could not establish pc connection" — coturn restarting [255], blocks all in-call testing). BLOCKED (5): #14 in-call yellow-dot + #15 incoming-call dialog + #16 dept-filter routing — all need (a) 2+ distinct user sessions concurrently which Playwright MCP single-context cookie-sharing cannot provide AND (b) working WebRTC which coturn blocks; #7c subdomain redirect — /t/{slug}/* dev URL pattern always returns Next.js 404 because no route handlers exist under apps/web/src/app/t/ and middleware doesn't rewrite to strip the prefix; #14/#15/#16 underlying code IS covered at unit-test level by Phase 7 #14 (16 cases), #15 (16 cases), #16 (7 cases) — manual e2e smoke deferred until multi-browser rig + working coturn available. ONE CODE FIX SHIPPED INLINE: discovered apps/web/src/env.ts schema declared `LIVEKIT_WS_URL` but every .env.* + every compose file uses `LIVEKIT_URL` → `mintLiveKitToken`'s wsUrl was always undefined → `meetings.getJoinToken` returned 503 on EVERY call attempt. Five-line rename in env.ts + one in lib/livekit/client.ts on branch `fix/livekit-url-env-rename` (commit `417ed97`); two-stage review PASS; squash-merged to main as `f9f88bf`; pnpm test 167/167 ✓, typecheck/lint/build all clean.

LAST_DONE_PRIOR: 2026-05-19 Phase 7 #16 ticket (department-binding-filter) — Tier 1 (5 files, ~138 net insertions, 7 RED→GREEN), inline controller execution (Opus 4.7, no subagent dispatch). Plan-phase + execution + governance all in one session. Squash SHA `fdfc3ca`. Two governance commits before execution: `a74f8fd` design spec (100 lines, 8 locked decisions), `68f5a45` implementation plan (319 lines, 7-task TDD breakdown). End-to-end call routing now correct: caller initiates → `emitToOrg` fans out → only the bound user's dialog rings.

LAST_DONE_TWO_PRIOR: 2026-05-19 Phase 7 #15 (legacy-socket-retirement) — Tier 2 (score 61.5) subagent-driven execution per user choice. 12 atomic branch commits squash-merged to main as `af43276`. Final stats: 12 files / 269 deletions / 536 insertions. Closes `[[parallel-socket-servers-coexistence]]` from Phase 7 #11.

NEXT: 3 follow-up tickets remaining from Rule 16 cleanup findings (one closed by Task #21) + the prior Phase 7 #17 backlog:
  (t-slug-dev-routes-broken) — Add either Next.js route handlers under apps/web/src/app/t/[slug]/(app|admin|superadmin)/ that re-render the parent equivalents OR (preferred) a middleware rewrite that strips /t/{slug} after extracting the tenant slug to x-tenant-slug header. Tier 1. Currently /t/{slug}/anything always 404s; unit tests for buildTenantRedirectUrl pass because they only verify URL strings, never route serving.
  (coturn-config-fix) — Fix the COTURN_STATIC_AUTH_SECRET ⏳ placeholder in CREDENTIALS.md so coturn container stops restarting (exit 255). STATE.md previously claimed "dev video calls work without it on localhost" — Rule 16 #3 smoke proved that's WRONG; WebRTC peer connection fails even on localhost without coturn. Blocks all in-call manual smoke (#14/#15/#16). Tier 1 (fill credential, restart container).
  (rule-16-followup-multi-user) — When (a) coturn is fixed AND (b) a multi-browser testing rig is available (e.g. 2 incognito Chrome profiles, or 2 machines, or Playwright extended to multi-context), re-run #14 in-call yellow-dot + #15 incoming-call dialog + #16 dept-filter routing as proper e2e smoke. Unit-test coverage for all 3 already shipped — this is the visual verification.
  (i) Periodic cleanup of expired/consumed password_reset_tokens (BullMQ scheduled job) — pairs with Phase 7 #6's count-query pattern; Tier 2. UNCHANGED from prior queue.
  (d) join/[token] tRPC — still needs full LiveKit + coturn working (now depends on (coturn-config-fix)); unit tests OK against mocked mint. UNCHANGED from prior queue.
  (f) Root landing page — verify scope first; likely Tier 1. UNCHANGED from prior queue.
  (binding-realtime-invalidate) — Real-time bound-dept cache invalidation; Phase 7 #16 decision 8 explicitly deferred. UNCHANGED from prior queue.
All tickets must write failing test first (Rule 25; vitest infra alive since Phase 7 #2; coverage gate live since Phase 7 #5). PERMANENT for any work touching `apps/web/src/instrumentation.ts` or transitively-imported modules: MUST run `pnpm build` as part of validation per [[instrumentation-edge-stub-required]].

CURRENT_BUILD: 22 static + dynamic routes ✓ compiled + middleware 141kB. Test suite: 19 files, 167 tests (unchanged — env rename had no test impact since no tests referenced the old name). `pnpm audit --audit-level=critical` exit 0 ✓ (1 high = documented nodemailer mitigation, unchanged). Schema: 3 migrations unchanged (no migration in Rule 16 pass).

BLOCKERS:
- CREDENTIALS.md still has ~52 ⏳ placeholders across 8 REQUIRED sections — blocks full Phase 5 + Phase 6, does NOT block Phase 7 work on existing code. UNCHANGED.
- Coturn container restarting (255) — needs COTURN_STATIC_AUTH_SECRET. **CORRECTED**: STATE.md previously claimed dev video calls work without it on localhost; Rule 16 #3 smoke proved that's WRONG. WebRTC peer connection fails even on localhost. Now queued as (coturn-config-fix) ticket. Blocks all in-call manual smoke.
- ✅ LIVEKIT_WS_URL → LIVEKIT_URL env naming mismatch — RESOLVED via Rule 16 cleanup `f9f88bf` (5-line code-side rename to match env files).
- ✅ Rule 16 Visual QA accumulated debt — PASS or KNOWN-BLOCKED-WITH-TICKET for all 12 items (see LAST_DONE).
- 🔴 NEW: /t/{slug}/* dev URL pattern always 404s — queued as (t-slug-dev-routes-broken).
- 🔴 NEW: cross-org user visible in admin.users.list dropdown when is_super_admin=true — queued as (admin-users-list-tenant-scope) for verification.
- 🟡 NEW: Login form silent-fails when workspace field required and missing (no error toast). Worth a UX polish ticket.
- 🟡 NEW: SocketProvider initial-mount race may lose first presence:snapshot (state catches up via subsequent broadcasts; not consistently reproducible). Worth a follow-up if real complaints surface.
- ✅ nodemailer HIGH CVE — RESOLVED via Phase 7 #9 documented-acceptance path (c). Phase 7 #10 corrected enforcement (CLI flag).
- ✅ Phase 7 #8e build regression — RESOLVED via Phase 7 #10's Edge-runtime webpack alias.
- ✅ parallel-socket-servers-coexistence — RESOLVED via Phase 7 #15. Single auth-gated server now handles all socket events.
- ✅ Phase 7 #15 dept-filter TODO — RESOLVED via Phase 7 #16. Dialog now filters by user's bound department(s).

GIT_BRANCH: main (Rule 16 cleanup fix squash-merged 2026-05-19 as `f9f88bf`). Prior main tip: `3e5eafc` (Phase 7 #16 governance record commit). Before: `fdfc3ca` (Phase 7 #16 squash). Implementation branch `fix/livekit-url-env-rename` deleted post-squash. One task commit captured in the squash: `417ed97` (env + client rename, 2 files / 5 insertions / 5 deletions).

PORTS: APP=43512 DB=43502 PGBOUNCER=43503 CACHE=43504 MINIO=43505 MINIO_CONSOLE=43506 MAILHOG_SMTP=43507 MAILHOG_UI=43508 PGADMIN=43509 SOCKET=43515 LIVEKIT_SIGNAL=43532 LIVEKIT_TURN_UDP_START=43537 COTURN=43542 (unchanged).

TOKEN_ESTIMATE: ~110K Opus 4.7 controller across the full Rule 16 cleanup session. Decomposition: ~15K infra startup + fixture creation (docker compose status checks + prisma migrate reset + db seed + fixture script + verification queries) · ~20K Playwright MCP browser automation across 12 smoke tasks (browser_navigate + browser_snapshot + browser_evaluate + browser_console_messages + multi-tab orchestration) · ~10K env.ts/client.ts diagnosis + fix (read meeting router source + grep all LIVEKIT_WS_URL refs + verify env.dev/compose values + apply rename + restart dev) · ~5K validation (typecheck + lint + test + build for the env rename) · ~8K two-stage review + squash-merge to main · ~25K governance writes (this STATE.md rewrite + lessons.md 3 new typed entries + CHANGELOG_AI.md fix entry + agent-log.md paragraph + .whatsnext close + IMPLEMENTATION_MAP.md update) · ~27K conversational overhead + task tracking + ToolSearch loads + system reminders. Tier 2 score: (~25 files explored × 2.5) + (4 modules × 5) + (depth 3 × 3) = 91.5 — though most "files explored" were Read-once + no edit; effective scope was 1-module fix + 5-file governance writes. Inline controller throughout — no subagent dispatch needed (validation + governance overhead doesn't merit per-task subagent dispatch).

CHECKPOINT_TYPE: full (Rule 16 cleanup smoke pass complete; 1 inline fix shipped; 4 follow-up tickets queued; 3 lessons added)

FILES_TOUCHED_THIS_SESSION (Rule 16 cleanup smoke pass):
  created:
    /tmp/yelli-fixtures.ts                                                  (test-fixture seeder — ephemeral, not committed)
  modified (code, committed via squash-merge `f9f88bf`):
    apps/web/src/env.ts                                                     (5 refs renamed LIVEKIT_WS_URL → LIVEKIT_URL + NEXT_PUBLIC_LIVEKIT_WS_URL → NEXT_PUBLIC_LIVEKIT_URL)
    apps/web/src/lib/livekit/client.ts                                      (1 ref renamed env.LIVEKIT_WS_URL → env.LIVEKIT_URL)
  governance (this session):
    docs/CHANGELOG_AI.md                                                    (fix entry for env rename + Rule 16 smoke summary)
    docs/IMPLEMENTATION_MAP.md                                              (Rule 16 smoke entry at top of Project Status)
    .cline/STATE.md                                                         (this file — full rewrite)
    .cline/memory/agent-log.md                                              (Rule 16 cleanup paragraph appended)
    .cline/memory/lessons.md                                                (3 new typed entries: 🔴 livekit-env-name-mismatch / 🔴 t-slug-dev-routes-broken / 🟤 rule-16-cleanup-2026-05-19)
    .whatsnext                                                              (close rule-16-cleanup, promote 4 new tickets + 4 prior queue items)

LESSONS_ADDED (3 this session):
  🔴 [[livekit-env-name-mismatch]] — `LIVEKIT_WS_URL` in env.ts vs `LIVEKIT_URL` in every env/compose file caused silent 503 on every meeting/call. Lesson: any cross-file identifier rename MUST be applied to ALL source-of-truth files in one PR; add Rule 16 follow-up: run pnpm dev + curl critical tRPC endpoints to catch latent 503s with generic try/catch messages.
  🔴 [[t-slug-dev-routes-broken]] — /t/{slug}/* dev URL pattern has no Next.js route handlers under apps/web/src/app/t/; middleware extracts slug + redirects but never rewrites the path; tenant-redirect unit tests pass because they only verify URL strings. Two fix paths: route handlers OR middleware rewrite (prefer rewrite).
  🟤 [[rule-16-cleanup-2026-05-19]] — Rule 16 cleanup smoke pass findings + locked decision: Playwright MCP single-context cookie-sharing limits multi-user smoke testing; tickets touching multi-user realtime flows MUST have handler-level unit coverage + queue manual e2e smoke as Rule 16 follow-up when a multi-browser rig is available.

TIER_CLASSIFICATION: 2 — moderate. Per memory-governance §1: scope was effectively 1 module (env naming fix) + 12 smoke verifications across multiple modules. Score: (10 files inspected × 2.5) + (3 modules touched × 5) + (1 depth × 3) = 43. Just over Tier 2 threshold. Single-session inline controller was the right call given (a) the fix itself was Tier 1 (5 lines), (b) the smoke pass is fundamentally exploratory and depends on cumulative observations, (c) subagent dispatch overhead would have exceeded the saving on a fix + 12 sequential smokes.

MODELS:
  planning: claude-code (Opus 4.7 — V31 primary; Rule 16 cleanup smoke pass + inline fix triage)
  execution: inline controller (Opus 4.7) — no Sonnet dispatch; smoke pass + governance writes are inherently sequential controller work.
  governance: gemini-2.5-flash-lite (non-critical doc writes — not invoked; Opus 4.7 inline for governance file edits per session discipline)
