# Project State — Yelli

# Auto-generated by Claude Code after every task. Never edit manually.

# Updated: 2026-05-22 22:18 GMT+8 by CLAUDE_CODE

PHASE: **Phase 7 realtime engines CLOSED 2026-05-22 PM.** All six realtime tickets — #11 user-presence engine, #12 dept-binding green-dot, #14 in-call yellow-dot, #15 incoming-call dialog, #16 dept-filter, #17 Rule 16 cleanup — are stamped CLOSED per the new 🟤 [[phase-7-realtime-engine-closeout-criterion]] decision locked this session in lessons.md. Close-out criterion: source-code fixes shipped to main + full unit-test coverage of changed code paths (270/270 ✓ on `dc9bb3a` baseline, including server-side socket handlers presence/in-call/calls + colocated pure client handlers + tRPC routers + middleware) + one of two visual-QA outcomes (FULL multi-context Playwright OR PARTIAL with documented blocker recipe). This session satisfies the **PARTIAL** path: the 2026-05-22 PM Playwright smoke (shipped as `fc9395b` chore/governance) confirmed backend + DB + socket layer healthy and deferred the UI auth-gated layer with the 🔴 [[playwright-smoke-auth-configuration-blocker]] recipe for unblocking (stop stale yelli_dev_app container OR add signInForTest helper). Cascade evidence chain: `5718a3d` (2026-05-22 second-pass smoke — 4 PASS / 3 BLOCKED on diagnosed root cause) → `d364462` (fix/web — presence:ready handshake + SocketProvider StrictMode reconnect; +173/-22; 261 → 270 tests; addresses all 3 BLOCKED root causes in one bundled fix) → `e832987` (governance squash for d364462) → `dc9bb3a` (docs/trpc — resolves trpc-client-procedure-type-missing as not-a-bug; the third "blocker" was a DevTools eval inspection trap on the typed proxy, NOT a runtime bug) → `fc9395b` (governance — 2026-05-22 PM Playwright smoke PARTIAL outcome + recipe) → THIS SESSION (governance close-out — IMPLEMENTATION_MAP Phase header rewritten to stamp the close-out + 🟤 criterion locked in lessons.md). Zero outstanding source-code work on the realtime engines. Validation matrix re-confirmed on `dc9bb3a` baseline (no source diff in this session or in the prior fc9395b chore): pnpm lint ✓ 0 errors / typecheck ✓ 0 errors across 8 packages / test 270/270 ✓ / build ✓ Middleware 141 kB / audit --audit-level=critical ✓ exit 0 (1 HIGH pre-documented per [[nodemailer-cve-mitigation]]). Phase 7 active overall — many other realtime / dev-infra / guest-meeting / auth tickets remain in the merged-count tally; this close-out only addresses the realtime-engine cluster (#11/#12/#14/#15/#16/#17).

LAST_DONE: 2026-05-22 22:18 GMT+8 — Phase 7 realtime-engine close-out governance landed. Files modified (4): `.cline/memory/lessons.md` (+1 NEW entry — 🟤 [[phase-7-realtime-engine-closeout-criterion]] near top of file documenting the close-out criterion, rationale, operational impact, and cross-references to the cascade evidence chain), `docs/IMPLEMENTATION_MAP.md` (Project Status section — prepended new Phase header announcing realtime-engine close-out + cited cascade evidence chain + appended "Phase 7 realtime engines CLOSED with PARTIAL smoke acceptance 2026-05-22 PM" to the merged-list paragraph), `.cline/STATE.md` (this rewrite — supersedes the 21:27 GMT+8 PARTIAL state), `docs/CHANGELOG_AI.md` (+1 entry capturing the close-out + validation matrix). Zero source diff, zero test diff, zero schema diff — pure governance. Working tree clean except the 4 staged governance files + 2 pre-existing unrelated metadata files (`.claude/scan-results.json`, `.specstory/.project.json`).

NEXT: Realtime engines closed; queue moves to (a) `(admin-bounce-prefix-symmetry)` Tier 1 cosmetic — preserve `/t/{slug}` URL prefix on RBAC bounces (filed in [[rule-16-cleanup-2026-05-22]] queue; small, contained, no realtime/auth orchestration; good momentum-builder), OR (b) optional infrastructure investment — land a `AUTH_BYPASS_FOR_E2E` env-gated test-auth helper in apps/web/src/server/auth.ts (~20-line one-time investment that unblocks ALL future Playwright smoke tests permanently; converts the PARTIAL→FULL recipe from "stop the container" to "just run it"), OR (c) survey Phase 8 candidate work — PRODUCT.md vs current IMPLEMENTATION_MAP for next feature batch.

BLOCKERS: None. Phase 7 realtime engines fully closed with documented close-out criterion. Outstanding work is forward-looking, not regression-blocked.

GIT_BRANCH: main (HEAD = `fc9395b`). Working tree: 4 unstaged governance file edits (lessons.md, IMPLEMENTATION_MAP.md, STATE.md, CHANGELOG_AI.md) + 2 pre-existing unrelated metadata file edits (`.claude/scan-results.json`, `.specstory/.project.json`) which remain untouched per blast-radius. NEXT STEP: chore branch + squash-merge per Rule 23.

PORTS: APP=43512 (host docker-proxy → container :3000 — STALE container; stop before next Playwright smoke or export APP_PORT=43512 before pnpm dev to bind canonical port to fresh source) DB=43502 CACHE=43504 MINIO=43505 MINIO_CONSOLE=43506 SMTP=43507 SMTP_UI=43508 PGADMIN=43509 LIVEKIT=43532 LIVEKIT_UDP=43537 COTURN=43542 SOCKET=43515 — currently the dev stack is DOWN (this session ran `start.sh dev down` after the smoke PARTIAL closed out; volumes preserved so seed data persists for the next session).

LESSONS_ADDED: 1 — 🟤 [[phase-7-realtime-engine-closeout-criterion]] (locks the gate criterion as a framework decision; future Phase 7 realtime / non-realtime tickets can reference this entry as precedent for PARTIAL smoke acceptance when the unit-test layer provides full code-path coverage AND the visual-QA blocker is non-feature-environmental).

TIER_CLASSIFICATION: 1 — governance-only close-out. Per memory-governance §1: scope = 4 governance files (lessons.md + IMPLEMENTATION_MAP.md + STATE.md + CHANGELOG_AI), 0 source modules, ~65K context (read IMPLEMENTATION_MAP Project Status + cascade evidence + write 🟤 + STATE.md rewrite + CHANGELOG_AI append). Inline Opus 4.7 controller — no Sonnet dispatch warranted; governance writes are sequential and require precise cross-referencing.

CHECKPOINT_TYPE: full (4 governance files modified — IMPLEMENTATION_MAP touched because the Phase header changed).

FILES_TOUCHED: .cline/memory/lessons.md (+1 entry near top — 🟤 [[phase-7-realtime-engine-closeout-criterion]]), docs/IMPLEMENTATION_MAP.md (Project Status Phase header rewrite), .cline/STATE.md (this rewrite), docs/CHANGELOG_AI.md (+1 entry).

TOKEN_ESTIMATE: ~65K consumed this session (Tier 1 — IMPLEMENTATION_MAP indexed read + cascade SHA chain inspection + 🟤 decision write + STATE.md rewrite + CHANGELOG_AI append). Within the 80K SAFE zone.

MODELS:
  planning: claude-code (Opus 4.7 — V31 primary; governance close-out)
  execution: inline controller (Opus 4.7) — no Sonnet dispatch; governance writes require precise cross-referencing and cannot be safely parallelized
  governance: gemini-2.5-flash-lite (non-critical doc writes — not invoked; Opus 4.7 inline per session discipline)
