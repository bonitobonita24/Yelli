# Project State — Yelli

# Auto-generated by Claude Code after every task. Never edit manually.

# Updated: 2026-05-13 by CLAUDE_CODE

PHASE: Phase 4 Part 5b complete
LAST_DONE: Part 5b — apps/web Speed Dial Board + 1:1 Video Call UI scaffolded (14 files via Architect-Execute: 2 Sonnet sub-dispatches + Opus inline import-fix + global IncomingCallDialog stitch). 5b-1 Speed Dial Board (6 files DONE clean): app/app/layout.tsx auth guard + global IncomingCallDialog mount; app/app/page.tsx server-only Prisma findMany on departments scoped to organizationId; speed-dial/speed-dial-grid.tsx adaptive cols 1/2→2/3→2/3/4→2/3/4/5 + group_label bucketing + admin-gated empty CTA; speed-dial/speed-dial-button.tsx 88/120/140 min-h + presence dot + blue ⚡ Auto badge + disabled offline/in_call; lib/presence/use-presence.ts Socket.IO at /api/socket + 30s heartbeat + graceful no-server fallback; lib/presence/types.ts. 5b-2 Video Call UI (8 files partial-thrash but all written): lib/livekit/types.ts; lib/livekit/client.ts server-only HS256 JWT minter via crypto.createHmac (no livekit-server-sdk); lib/livekit/use-livekit-room.ts status state-machine ringing→connecting→active→ended/failed + useRef Room + RoomEvent listeners; app/app/call/[id]/page.tsx Next.js 15 async params + auth + notFound; components/call/intercom-call.tsx RoomContext.Provider bridging manual Room into @livekit/components-react GridLayout+ParticipantTile+useTracks; components/call/call-controls.tsx 48×48 mic/cam/hangup with inline SVG; components/call/incoming-call-dialog.tsx Dialog + Socket.IO "call:incoming" + Web Audio API 2-tone 440/523Hz ringtone + Accept/Reject; app/api/livekit/token/route.ts POST Route Handler with manual auth + rateLimiters.api + Zod.strict + 401/429/503 generic per security.md §AGENT PROHIBITIONS item 11. Opus stitched IncomingCallDialog into app/app/layout.tsx so it listens globally on /app/* routes. Opus inline fix: intercom-call.tsx import order (livekit-client must precede next/navigation + react alphabetically — auto-fix couldn't resolve, manual reorder). pnpm install +28 packages (5.7s). pnpm typecheck PASS (7 packages, 0 errors). pnpm lint PASS (7 packages, 0 errors, 0 warnings — 10 auto-fixed + 2 manual reorder).
NEXT: Phase 4 Part 5c — open NEW Claude Code session, say "Start Part 5c". Scope: tRPC server (initTRPC + appRouter + context with auth + tenant + rateLimit) + tRPC client provider mounted in app/layout.tsx + call initiation router (callInitiation: validate recipient department in same org → mint LiveKit token → emit Socket.IO "call:incoming" to recipient → return callId + roomName) + Socket.IO server skeleton at /api/socket/route.ts (presence subscriptions + heartbeat + call:incoming/call:reject channels). Estimated 8-10 files / ~55K → Tier 3, split into 2 Sonnet sub-dispatches per Part 5b discipline.
BLOCKERS: ⏳ CREDENTIALS.md placeholders must be filled before Phase 5 — GitHub PAT, Docker Hub token, SMTP, Komodo URL, Xendit keys, Turnstile prod keys, LiveKit keys. Parts 5c-8 proceed without them (token endpoint returns 503 when LiveKit env unset — handled gracefully by use-livekit-room hook).
GIT_BRANCH: scaffold/part-5b (about to squash-merge to main + delete)
PORTS: APP=43512 DB=43502 CACHE=43504 LIVEKIT_SIGNAL=43532 LIVEKIT_TURN_UDP_START=43537 COTURN=43542 (dev — base 43502)
TOKEN_ESTIMATE: ~150K consumed this session (Opus orientation + ctx_batch_execute + 2 Sonnet dispatches + 5b-2 thrash diagnosis + Opus inline fixes + governance writes — well under 200K Opus 4.7 budget, no Opus-side autocompact)
CHECKPOINT_TYPE: full
FILES_TOUCHED_PART_5B: [apps/web/package.json, apps/web/src/app/app/layout.tsx, apps/web/src/app/app/page.tsx, apps/web/src/app/app/call/[id]/page.tsx, apps/web/src/app/api/livekit/token/route.ts, apps/web/src/components/speed-dial/speed-dial-grid.tsx, apps/web/src/components/speed-dial/speed-dial-button.tsx, apps/web/src/components/call/intercom-call.tsx, apps/web/src/components/call/call-controls.tsx, apps/web/src/components/call/incoming-call-dialog.tsx, apps/web/src/lib/presence/use-presence.ts, apps/web/src/lib/presence/types.ts, apps/web/src/lib/livekit/types.ts, apps/web/src/lib/livekit/use-livekit-room.ts, apps/web/src/lib/livekit/client.ts, pnpm-lock.yaml, docs/CHANGELOG_AI.md, docs/IMPLEMENTATION_MAP.md, .cline/STATE.md, .cline/memory/agent-log.md, .cline/memory/lessons.md]
TIER_CLASSIFICATION: 3 — heavy (14 files, 1 module, depth 2, score 43.5 — split via Architect-Execute Model into 2 Sonnet sub-sessions each ≤30K per §1 Step 2.5; 5b-1 returned cleanly, 5b-2 thrashed on validation step but all 8 files were complete on disk so Opus completed validation in-place — new 🟤 decision logged on thrashing-recovery without re-dispatch)
MODELS:
planning: claude-code (Opus 4.7 — V31 primary, Architect role)
execution: claude-sonnet-4-6 via Claude Code (2 dispatches: 5b-1 DONE clean ~10min/23 tools, 5b-2 partial-thrash all-files-on-disk ~8min/11 tools — Opus completed validation in-place)
governance: gemini-2.5-flash-lite (non-critical doc writes — not invoked this session; Opus inline for all governance)
