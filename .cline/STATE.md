# Project State — Yelli

# Auto-generated by Claude Code after every task. Never edit manually.

# Updated: 2026-05-22 11:25 GMT+8 by CLAUDE_CODE

PHASE: (fresh-client-presence-snapshot-race) + (socketprovider-strictmode-reconnect) bundled — squash-merged to `main` as `d364462` on 2026-05-22 11:17:27 GMT+8. Branch `feat/fresh-client-presence-snapshot-race` deleted post-squash. Governance docs already updated as part of `d364462` (CHANGELOG_AI +64 lines, IMPLEMENTATION_MAP +19 lines, DECISIONS_LOG +45 lines, lessons.md +28 lines containing both 🔴 [[strictmode-socket-disconnect-permanent]] + 🔴 [[trpc-client-procedure-type-missing]]). This follow-up commit records the squash SHA in STATE.md + agent-log.md per the prior `bdb14e5` governance-record pattern. Working tree clean; on `main`, 18 commits ahead of `origin/main` (pre-existing — none pushed this session). Phase 7 status: #11 (user-presence) + #12 (dept-binding) + #13 (call-page) + #16 (dept-filter) now end-to-end verified across multi-context Playwright rig. **#14 (in-call yellow-dot) + #15 (incoming-call dialog) remain BLOCKED** on the third independent bug discovered during smoke: `(trpc-client-procedure-type-missing)` — bob's `trpc.departments.myBoundDepartmentIds.useQuery()` in `IncomingCallDialog` is stuck `status:pending` indefinitely despite server returning 200 with correct payload `["cmpfmyva2000fa8k1drezs7bw"]` via plain `fetch('/api/trpc/...', { credentials: 'include' })`. Manual `queryClient.getQueryCache().getAll().filter(q => q.queryKey.includes('myBound'))[0].fetch()` throws `TypeError: client[procedureType] is not a function at @trpc/client/dist/index.mjs:184:31`. Filed as separate Tier 1-2 queue item — needs dedicated session on a new branch.

LAST_DONE: 2026-05-22 11:17 GMT+8 — squash-merged `feat/fresh-client-presence-snapshot-race` to `main` as `d364462`; branch deleted. Governance record commit (this session) updates STATE.md timestamp + adds `agent-log.md` entry capturing the SHA. No source-code changes in this governance commit. Validation prior to merge: pnpm lint ✓ 0 errors, pnpm typecheck ✓ across all 8 packages, pnpm test 267 → 270 ✓ (+3 RED→GREEN this session, +6 prior session = +9 total across the bundle), pnpm build ✓ (Middleware 141 kB unchanged — types.ts/ensure-connected.ts not on edge bundle per [[instrumentation-edge-stub-required]]), pnpm audit --audit-level=critical ✓ (1 HIGH = pre-documented nodemailer per [[nodemailer-cve-mitigation]]). Two-stage review Rule 25 PASS on both bundled fixes.

NEXT: File and start `(trpc-client-procedure-type-missing)` ticket on a new branch `fix/trpc-client-procedure-type-missing` in a separate session. Open questions to investigate: (1) Why does the SpeedDial tRPC query (`departments.list*` or similar) work but `myBoundDepartmentIds` not? — both use the same `trpc.<router>.<procedure>.useQuery()` pattern. Compare working vs broken procedure shape. (2) Is `IncomingCallDialog`'s mount context different (rendered as portal/sibling outside main tRPC provider tree)? — check `apps/web/src/app/app/layout.tsx`. (3) Is it a React-StrictMode-x-tRPC-react-query-v5 dev-mode-only interaction (similar shape as [[strictmode-socket-disconnect-permanent]])? Hard constraint: do NOT "fix" by removing the `boundDeptIds === undefined` guard — that would let arbitrary tenants ring random users while the query is loading.

BLOCKERS: #14 + #15 smoke verification blocked on `(trpc-client-procedure-type-missing)` — separate ticket, separate branch. No blockers for the current governance commit.

GIT_BRANCH: main (post-squash-merge of `feat/fresh-client-presence-snapshot-race` as `d364462`)

PORTS: APP=43512 DB=43502 CACHE=43504 MINIO=43505 MINIO_CONSOLE=43506 SMTP=43507 SMTP_UI=43508 PGADMIN=43509 LIVEKIT=43532 LIVEKIT_UDP=43537 COTURN=43542 SOCKET=43515

LESSONS_ADDED: None new this commit. Prior session shipped 🔴 [[strictmode-socket-disconnect-permanent]] + 🔴 [[trpc-client-procedure-type-missing]] (both already in lessons.md via `d364462`).

TIER_CLASSIFICATION: 0 — governance bookkeeping only. Per memory-governance §1: scope = 2 governance files (STATE.md + agent-log.md), 0 modules, 0 dependency depth. Score: 5 (below Tier 1 threshold of 15). Inline Opus 4.7 controller — no Sonnet dispatch warranted.

CHECKPOINT_TYPE: lightweight

FILES_TOUCHED: .cline/STATE.md (this rewrite — post-merge state), .cline/memory/agent-log.md (+1 entry capturing `d364462` SHA + commit summary).

TOKEN_ESTIMATE: ~25K consumed this session (lightweight — just STATE.md read + git verify + governance writes; no source-code investigation).

MODELS:
  planning: claude-code (Opus 4.7 — V31 primary; governance record only)
  execution: inline controller (Opus 4.7) — no Sonnet dispatch; trivial governance writes
  governance: gemini-2.5-flash-lite (non-critical doc writes — not invoked; Opus 4.7 inline per session discipline)
