# Implementation Map — Yelli

# Current build state. Rewritten after every phase or feature update (Rule 3).

# ---

## Latest Update (2026-05-29)

**Overlay Cluster A-group SHIPPED — sharedFiles + whiteboardSnapshots tRPC routers in PR #3.** Branch `feat/shared-files-router` pushed with 2 commits (Sub-A1 `5fce2a6` + Sub-A2 `cd3c653`). Both routers registered in `appRouter`; feature-router keys remain alphabetical; admin/superadmin preserved at end. Sub-A1 added 5 plan-gated procedures (requestUpload/commit/listByMeeting/getDownloadUrl/softDelete) on `requirePlanCapability("filePersistence")` with direct-to-storage upload pattern; Sub-A2 added 2 procedures (save/getLatest) on `requirePlanCapability("whiteboardPersistence")` with NOT_FOUND enumeration guard on cross-org meeting access. Schema: `SharedFile.deleted_at DateTime?` + index added (migration `20260529000000_add_shared_file_deleted_at`); `WhiteboardSnapshot` model was pre-existing. Storage: `@yelli/storage` now exports `getPresignedUploadUrl()` — keeps `@aws-sdk/*` out of web app workspace. Middleware: `plan-limit.ts` MiddlewareOpts.next typed as `Promise<MiddlewareResult<unknown>>` — zero-cast `.use(<guard>)` for any plan-capability middleware. Test suite locally: 25/25 router tests pass (19 sharedFiles + 6 whiteboardSnapshots). tsc 0, eslint 0/0 on changed files. No `as any`, no `@ts-ignore`. Single canonical `as Prisma.InputJsonValue` cast on Json column write (whiteboardSnapshots.save). Governance branch `chore/governance-overlay-a-group` lands this entry + CHANGELOG_AI + 5 typed lessons.md entries (🟤×2 decisions, 🔴×2 gotchas, 🟢×1 change). 4 Sonnet executor dispatches per Sub-A2 (1 router+register, 1 tests, 2 micro-fixes for import path + lint) — exactly the V32.1 op-note pattern of recovery via re-decomposition and Opus-side `ctx_execute` verification.

**Overlay Cluster status (post-A-group):**
- Sub-A1 (sharedFiles tRPC router):           ✅ SHIPPED 2026-05-29 (`5fce2a6` in PR #3)
- Sub-A2 (whiteboardSnapshots tRPC router):   ✅ SHIPPED 2026-05-29 (`cd3c653` in PR #3)
- Sub-B (whiteboard realtime sockets):        ⬜ NEXT (parallel-eligible with Sub-B′)
- Sub-B′ (file-share socket transport):       ⬜ NEXT (parallel-eligible with Sub-B; free-tier ≤2MB base64 broadcast)
- Sub-C (in-call-file-dropzone.tsx wiring):   ⬜ PENDING — needs A-group (DONE) + Sub-B′ events
- Sub-D (in-call-whiteboard.tsx wiring):      ⬜ PENDING — needs A-group (DONE) + Sub-B events

**A-group artifacts:**
- `apps/web/src/server/trpc/routers/sharedFiles.ts` (315L) — Sub-A1 router
- `apps/web/src/server/trpc/routers/sharedFiles.test.ts` (467L, 19/19 ✓) — Sub-A1 tests
- `apps/web/src/server/trpc/routers/whiteboardSnapshots.ts` (118L) — Sub-A2 router
- `apps/web/src/server/trpc/routers/whiteboardSnapshots.test.ts` (238L, 6/6 ✓) — Sub-A2 tests
- `apps/web/src/server/trpc/router.ts` — both routers registered
- `apps/web/src/server/trpc/middleware/plan-limit.ts` — `next` return type tightened
- `packages/db/prisma/schema.prisma` — `SharedFile.deleted_at` + index added
- `packages/db/prisma/migrations/20260529000000_add_shared_file_deleted_at/migration.sql` — migration
- `packages/storage/src/client.ts` (+53L) + `src/index.ts` (+3L) — `getPresignedUploadUrl()` helper

# ---

## Latest Update (2026-05-28)

**CI Recovery Sprint CLOSED — 8/8 CI jobs green on main.** Sprint started morning at 2/8 (only governance + audit green); closed at 8/8 after 8 commits + 2 PRs landed on `origin/main` today. Three layers fixed: (a) CI workflow plumbing — Prisma generate ordering + Postgres + Valkey services for E2E job (`3e60007`, `7f86a43`); (b) test pollution + auth path — static `vi.mock` + `vi.hoisted` replaced `vi.doMock` + dynamic import in LiveKit webhook tests, bypass auth provider wired into `auth.setup.ts` (`d827397`, `223f1ef`, `5354aa1`); (c) E2E fixture contracts + Turbo env allowlist — fixture `file_path` prefix dropped to match `verifyKeyOwnership` org-id derivation, spec response shape switched from snake_case `{ id, egress_id }` to camelCase `{ recordingId, egressId }` matching tRPC at `apps/web/src/server/trpc/routers/recordings.ts:309`, `turbo.json` globalEnv expanded to allowlist env-validated vars (PR #1 `1f783bb`, PR #2 `62fab35`). Final sprint commits: `06dec12` (local/CI parity docs in `.env.test.e2e.example`) + `a50ab77` (+7 typed gotchas to lessons.md) + `6d642a7` (STATE.md checkpoint follow-ups 1+2). Test suite stays at 473/473 ✓. CI suite 2/8 → 8/8. No source-feature changes — sprint was bug-fix + governance only.

**Phase 8 Batch B status (post-CI-recovery):**
- sub-1 (Real-time chat over Socket.IO):           ✅ CLOSED 2026-05-25 PM (`85ea101`)
- sub-2 (Reports CSV/PDF generation):              ✅ CLOSED 2026-05-26 AM (`2955eae`)
- sub-3 (LiveKit Egress recording feed):           ✅ CLOSED 2026-05-26 AM (`1053729`)
- sub-3 polish (recordings UX):                    ✅ CLOSED 2026-05-26 PM (`2175f61`)
- sub-4 (Playwright E2E pipeline):                 ✅ CLOSED 2026-05-27 (branch tip `a1e3351`, on main)
- CI recovery sprint (post-sub-4 stabilisation):   ✅ CLOSED 2026-05-28 (8 commits + 2 PRs, final `62fab35`; governance `06dec12`/`a50ab77`/`6d642a7` + this entry)
- overlay cluster (File Sharing + Whiteboard):     🟡 IN PROGRESS — A-group shipped 2026-05-29 (PR #3); B/B′/C/D pending

# ---

## Latest Update (2026-05-27)

**Phase 8 Batch B sub-3 polish CLOSED** — squash commit `2175f61` on main. 11 files changed (+447/-18). 5 atomic sub-commits squashed: scan-results chore + `View recordings →` nav link from /app/meetings + /app/history + soft-delete UI (shadcn AlertDialog primitive in @yelli/ui + `RecordingDeleteButton` + 5 vitest cases for pure copy/payload) + governance updates. Test suite 465 → 470 passing. Typecheck 0 errors (@yelli/ui + @yelli/web). Lint 0 new errors. Recordings module now reachable from app shell with full create/download/delete flow shipping. Three architect spec errors discovered + corrected during execution (see lessons.md entries [[yelli-vitest-node-env-no-rtl]], [[credentials-md-gitignored-never-in-pr]], [[polish-spec-verify-codebase-before-assuming]]). Two Sonnet subagent thrashes recovered inline by architect — see [[sonnet-subagent-thrash-budget-pattern]]. Spec: `docs/superpowers/specs/2026-05-26-livekit-egress-polish-design.md` (`45e71a3`); Plan: `docs/superpowers/plans/2026-05-26-recordings-polish.md` (`3c63f37`); Parent: `1053729` (sub-3 LiveKit Egress recording feed).

**Phase 8 Batch B status (post-polish):**
- sub-1 (Real-time chat over Socket.IO):           ✅ CLOSED 2026-05-25 PM (`85ea101`)
- sub-2 (Reports CSV/PDF generation):              ✅ CLOSED 2026-05-26 AM (`2955eae`)
- sub-3 (LiveKit Egress recording feed):           ✅ CLOSED 2026-05-26 AM (`1053729`)
- sub-3 polish (recordings UX):                    ✅ CLOSED 2026-05-26 PM (`2175f61`)
- sub-4 (Playwright E2E pipeline):                 ✅ CLOSED 2026-05-27 (squash on main; branch tip `a1e3351`)
- overlay cluster (File Sharing + Whiteboard):     🟡 IN PROGRESS — A-group shipped 2026-05-29 (PR #3); B/B′/C/D pending

**Files added in this polish (3):**
- `apps/web/src/components/recordings/recording-delete-button.tsx` — client component with shadcn AlertDialog confirmation + softDelete mutation + invalidate-and-refresh on success + inline error on failure
- `apps/web/src/components/recordings/recording-delete-button.test.tsx` — 5 vitest cases for pure copy/payload constants (node env)
- `apps/web/src/components/recordings/recording-delete-copy.ts` — pure constants + `buildDeletePayload(id)` extracted for node-env testability
- `packages/ui/src/components/alert-dialog.tsx` — shadcn AlertDialog primitive (Radix-based, 11 exports), first destructive-action confirmation in @yelli/ui

**Files modified in this polish (5):**
- `apps/web/src/app/app/meetings/page.tsx` — header gains `View recordings →` link
- `apps/web/src/app/app/history/page.tsx` — header gains `View recordings →` link
- `apps/web/src/app/app/recordings/page.tsx` — actions column adds `RecordingDeleteButton` next to download
- `packages/ui/src/index.ts` — barrel re-export added for AlertDialog
- `packages/ui/package.json` + `pnpm-lock.yaml` — `@radix-ui/react-alert-dialog ^1.1.2` added

**Deferred (own brainstorm sessions):**
- E. Filter UI (status + meeting + date-range) on `/app/recordings`
- G. Playwright install + first e2e for host-only recording start/stop + soft-delete behavioral coverage

# ---

## Project Status

Phase: **Phase 8 Batch B sub-session 2 (Reports CSV/PDF generation) SHIPPED 2026-05-26 AM — second Batch B item closed via Tier 2 → Tier 1 gap-find pivot per [[existing-surface-scan-before-scaffold]] (this is the 9th data point for the ~20K Sonnet effective-budget pattern + 2nd consecutive sub-session where the pre-flight scan reduced the tier). Branch `feat/phase8-batchB-reports-csv-pdf` squash-merged to main this session. Single atomic commit `feat(reports): CSV + PDF exports for admin reports + superadmin revenue`. 6 new source files (apps/web/src/lib/reports/{csv,pdf}.ts + apps/web/src/server/trpc/routers/reports.ts + apps/web/src/app/superadmin/revenue/page.tsx + apps/web/src/lib/reports/{csv,pdf}.test.ts) + 4 modified source files (apps/web/src/server/trpc/routers/admin.ts — relocated inline reports sub-router to `./reports` import + apps/web/src/server/trpc/routers/superadmin.ts — added `revenue` sub-router + apps/web/src/app/admin/reports/page.tsx — wired 6 buttons + apps/web/src/app/superadmin/layout.tsx — added Revenue nav link) + 2 new test files (apps/web/src/server/trpc/routers/{reports,superadmin-revenue}.test.ts) + 1 dep added (pdfkit + @types/pdfkit). 40 new tests (9 csv + 6 pdf + 14 reports router + 11 revenue). Suite 392 → 432 / typecheck 0 / lint 0. Server pattern (admin.reports.*): 6 procedures (CDR + usage summary + dept activity × CSV + PDF) all gated by `adminProcedure`, all enforce explicit `organization_id` filter on top of L6 defense-in-depth per security.md §DATABASE SAFETY rule 10 + lessons.md 2026-05-19 super-admin bypass trap. Server pattern (superadmin.revenue.*): 2 procedures (CSV + PDF) gated by `superAdminProcedure`, use `platformPrisma` (no L6) cross-tenant by design, audit-logged `PLATFORM:EXPORT_REVENUE_{CSV,PDF}`. **Critical security boundary applied per [[xendit-internal-id-in-api-wire]]**: revenue queries use explicit `select` clauses on BOTH the Invoice row AND the nested Subscription — Invoice select enumerates `{id, amount_cents, currency, status, issued_at, subscription: {select: {plan_tier, payment_method}}}` and EXCLUDES `xendit_invoice_id`, `xendit_customer_id`; nested Subscription select EXCLUDES `xendit_subscription_id`. Three layers of defense: (1) Prisma select shape (no xendit_* column reaches the JS object), (2) explicit CsvColumn[] / PdfColumn[] arrays in `lib/reports/{csv,pdf}.ts` (the serializers only emit declared columns regardless of source-row shape — so even if a future regression widens the select, the wire output stays clean), (3) regression test asserts `select` shape lacks xendit_*_id, CSV bytes never contain `xendit_`, and PDF bytes never contain a marker secret. Pure serializer pattern: `apps/web/src/lib/reports/csv.ts` — RFC 4180 CRLF + double-quote escape, typed CsvColumn[] accessor array, accepts string|number|Date|null|undefined cells; `apps/web/src/lib/reports/pdf.ts` — pdfkit-based A4 landscape table with auto-pagination, header (title + subtitle + org name top-right) + footer (timestamp + page number), typed PdfColumn[] accessor array. The accessor-array design IS the security boundary: shared between admin and superadmin routers, the only path to ship a column is to add it to the declared list. tRPC wire shape: CSV procs return `{filename, content: string, row_count}`, PDF procs return `{filename, contentBase64: string, row_count}`; client decodes base64 → Uint8Array → Blob → URL.createObjectURL + anchor.click. UI: `/admin/reports/page.tsx` rewritten — replaced single CDR CSV card with 3 cards (CDR + Usage Summary + Dept Activity) × 2 buttons each + shared date-range picker; "future release" placeholder card removed. `/superadmin/revenue/page.tsx` new — CSV + PDF buttons + date-range picker, mirrors admin pattern. `/superadmin/layout.tsx` — added Revenue nav link between Organizations and Platform Settings. Architect-Execute trail per memory-governance.md §4: **Tier 2 initial estimate (8 files net new + 2-3 modules) → Tier 1 actual after pre-flight scan revealed (a) `/admin/reports/page.tsx` already had a date-range picker + tRPC mutation pattern + CSV download trigger, (b) `admin.ts:301-399` already had a working `reports` sub-router with `exportCallLogsCsv` + an RFC 4180 `csvEscape` helper, (c) `superadmin.ts` already had `superAdminProcedure` + `platformPrisma` + `PLATFORM:*` audit pattern locked in. Scope reduction: from "build all reports infra from scratch" to "relocate existing CSV procedure into shared module + add 5 new procedures mirroring it + add PDF format using shared pdfkit util + wire UI". Opus 4.7 inline solo session per [[opus-inline-escalation-pattern]] — no Sonnet dispatch this session. Initial estimate ~25-30K Sonnet budget (well above effective ~20K threshold from [[sonnet-effective-budget-20k]]); final actual ~70K total session context including pre-flight scans + reading 5 patterns + writing 6 new files + modifying 4 existing files + governance close-out. Ninth data point for the ~20K Sonnet budget refinement.** Reports and Export module now ✅ depth-complete in the 13-module gap matrix (flipped from 🟡 → ✅). Validation: pnpm --filter @yelli/web typecheck ✓ 0 errors / lint ✓ 0 errors (6 warnings, ALL pre-existing in chat-message-handler.test.ts + calls.test.ts + chat.test.ts + layout.tsx no-css-tag — 0 new warnings introduced) / test ✓ 432/432 across 47 test files (392 → 432, +40 cases). Two-stage review (Rule 25) PASS: Stage 1 spec compliance vs PRODUCT.md L117-121 (4 report types × 2 formats × tenant/platform separation) + L127-128 (tenant admin scope vs super admin scope) + L113 (revenue by period/plan/payment); Stage 2 quality — zero `any`, blast radius matches the 12 touched files (no scope creep), tests written alongside (RED→GREEN evidenced — csv.test.ts ran BEFORE csv.ts existed; pdf.test.ts ran BEFORE pdf.ts; reports.test.ts mocked then router built). No new migrations. No deployment checklist additions — uses existing pdfkit npm dep + existing tRPC + existing audit log infrastructure. CDR CSV behavior preserved (id column + DESC order + 10000-row cap) for backward compat with any downstream tooling. The `lib/reports/{csv,pdf}.ts` factory pattern is a derivative of [[xendit-internal-id-in-api-wire]] — the explicit-column-list-as-security-boundary principle applied at the serializer layer rather than the Prisma layer, providing a second independent check. No new lessons.md entries — this work APPLIED two existing decisions ([[xendit-internal-id-in-api-wire]] + [[existing-surface-scan-before-scaffold]]) without surfacing new ones. Recommended next session: Batch B sub-session 3 (LiveKit Egress recording feed) — apply [[existing-surface-scan-before-scaffold]] pre-flight before scoping; or pivot to Batch B item 4 (File Sharing in-call overlay 🟡) / item 5 (Whiteboard in-call overlay 🟡) / item 6 (Recording library + playback 🟡) which all live in `/app` overlay and may share substantial scaffold.** Prior phase 8 detail: **Phase 8 Batch B sub-session 1 (Real-time chat over Socket.IO) SHIPPED 2026-05-25 PM — first Batch B item closed via Tier 2 → Tier 1 gap-find pivot per [[existing-surface-scan-before-scaffold]]. Branch `feat/phase8-batchB-realtime-chat` squash-merged to main this session. Single atomic commit `feat(chat): realtime chat over Socket.IO — replaces 3s polling`. 4 new files (apps/web/src/server/socket/chat.ts + apps/web/src/lib/chat/{chat-message-handler.ts, use-chat-messages.ts, chat-message-handler.test.ts} + apps/web/src/server/trpc/routers/chat.test.ts) + 4 modified files (apps/web/src/server/trpc/routers/chat.ts + apps/web/src/server/socket/server.ts + apps/web/src/lib/socket/types.ts + apps/web/src/components/meeting/in-call-chat.tsx). 11 new tests (8 router + 3 pure handler). Suite 381 → 392 / typecheck 0 / lint 0. Server pattern: `chatRouter.send` now calls `getIO()` + `emitToOrg(io, ctx.organizationId, CHAT_MESSAGE_EVENT, {meetingId, message})` after `prisma.chatMessage.create` — mirrors `calls.ts:58` pattern with null-io guard. New `apps/web/src/server/socket/chat.ts` exposes `attachChatHandlers({socket})` (no roster, no client→server events — chat send goes through tRPC) that joins the auth-gated socket to `${organizationId}:chat:message` via `joinOrgChannel`; wired into `server.ts` connection handler alongside the existing 3 attach calls. Client pattern: pure handler `apps/web/src/lib/chat/chat-message-handler.ts` mirrors `user-presence-handler.ts` shape (MinimalChatSocketEventTarget narrowed interface); React hook `useChatMessages(meetingId, onMessage)` composes `useSocketOptional()` + filters payloads by `meetingId` (org channel broadcasts every chat message in the org; one socket per user serves every meeting tab) + captures callback via ref so inline closures do not churn subscriptions. `in-call-chat.tsx` removed `refetchInterval`, uses `useChatMessages(meetingId, handleChatMessage)` where handleChatMessage is a `useCallback`-stable invalidate of the listByMeeting query. TypedSocket `ServerToClientEvents` registers the new `"chat:message"` event with `ChatMessageEventPayload` type — required so the strict typed-socket contract satisfies `MinimalChatSocketEventTarget` without cast (caught by typecheck on first run, fixed inline). Send-mutation onSuccess invalidate kept as defensive fallback (idempotent; TanStack dedupes concurrent fetches; covers socket-disconnect cases). Architect-Execute trail per memory-governance.md §4: **Tier 2 initial estimate → Tier 1 pivot after pre-flight scan revealed Phase 7 realtime infra (`getIO`/`emitToOrg`/`attachX`/`useSocketOptional`/`attach*Handler` pure-helper pattern) had scaffolded 80%+ of the work. Opus 4.7 inline solo session per [[opus-inline-escalation-pattern]] — initial estimate ~19K Sonnet budget at borderline §1 Step 2.5; final actual ~50K total session context including pre-flight scans. No Sonnet dispatch this session. Eighth data point for the ~20K Sonnet budget refinement (5 from Item 3 + 1 from Item 2 + 1 from this) — Tier 2 tasks pivoting to Tier 1 via pre-flight scan and fitting in <8 files of established-pattern mirroring are most efficient as Opus inline at <50K total.** In-Call Chat module now ✅ depth-complete in the 13-module gap matrix (flipped from 🟡 scaffolded-logic-stub per Phase 4 inline JSDoc). Validation: pnpm --filter @yelli/web typecheck ✓ 0 errors / lint ✓ 0 errors (6 warnings, 4 new are the established `vi.mocked(emitToOrg).mock.calls[0]!` non-null pattern from calls.test.ts:112) / test ✓ 392/392 across 43 files. Two-stage review (Rule 25) PASS: Stage 1 spec compliance vs PRODUCT.md L173-180 + DECISIONS_LOG line 173 channel-naming + security.md realtime connection safety rule (cross-org channel isolation via auth-middleware-sourced organizationId in `joinOrgChannel` — no client-controlled org id); Stage 2 quality — zero `any`, blast radius matches the 8 touched files (no scope creep), tests written alongside (RED→GREEN evidenced via the typecheck failure on chat:message before adding to ServerToClientEvents). No new migrations. No deployment checklist additions — Socket.IO infra already shipped via Phase 7 #8e-1. Recommended next session: Batch B sub-session 2 (Reports CSV/PDF generation — apply [[existing-surface-scan-before-scaffold]] before scoping; may pivot to Tier 1 if `/admin/reports` page already has data-feed wiring).** Prior phase 8 detail: **Phase 8 Batch 1 Item 3 (Xendit payment + billing flow) SHIPPED 2026-05-25 — closes Batch A end-to-end. Branch `feat/phase8-item3-xendit-billing-flow` squash-merged to main this session. 6 atomic commits landed across 2 days (2026-05-24 → 2026-05-25): 1b936e3 (3a checkout enrichment) → 5046397 (3b-i webhook route + queue scaffold) → 247275e (3b-ii webhook worker + ProcessedWebhookEvent idempotency + Redis SETNX fast-path) → 58238d6 (3c-i invoices.list security fix removing xendit_invoice_id from wire shape per security.md AGENT PROHIBITIONS #13 + empty-state copy) → f3b9798 (3c-ii-b grace period suspension state machine — migration adds grace_period_end DateTime? + 'suspended' enum value, xendit-webhook writes grace_period_end=now+7d on invoice.expired, BullMQ grace-sweeper cron at every 6h scans + transitions past_due → suspended with PLATFORM:GRACE_PERIOD_EXPIRED audit + organization downgrade to free tier) → 2c22921 (3c-ii-a PastDueBanner UI mounted at /app + /admin layout shells with destructive Alert + formatted deadline + tRPC subscription.current hook + Rule 13 local SubscriptionStatus union). Test suite 339 → 381 (+42 cases: 7 checkout-helper + 6 webhook-route + 9 webhook-worker + 6 BullMQ worker + 5 grace-sweeper + 5 banner-state + extensions to invoices.list + extensions to billing router subscription.current). 2 new migrations ship together: 20260524100300_xendit_webhook_idempotency (ProcessedWebhookEvent table) + 20260524140200_grace_period_suspension (enum extension + nullable timestamp column). Architect-Execute trail per memory-governance.md §4 — **6 of 6 sub-sessions: 5 Opus-inline per §2.5b after ~20K Sonnet re-estimate (3a/3b-i/3b-ii/3c-i/3c-ii-b at ~18-30K), 1 clean Sonnet dispatch (3c-ii-a at ~13K)**. This is the **fifth, sixth, and seventh data point** for the ~20K Sonnet effective-budget pattern (Item 2 2a-i thrashed at ~20K → Item 3 3a/3b-i/3b-ii/3c-i/3c-ii-b all escalated → Item 3 3c-ii-a at ~13K cleared comfortably). lessons.md proposes amendment to memory-governance.md §1 Step 2.5: "Sonnet 4.6 effective subagent budget is ~20K (not §1's theoretical 30K). Tasks estimated ≥18K should escalate to Opus inline per §2.5b regardless of nominal Sonnet eligibility, OR split below 15K to keep coordination overhead below 25% of task work." Billing module now ✅ depth-complete in the module gap matrix (flipped from 🟡 scaffolded-logic-stub); 3 ⏸ deferred items (invoice PDF receipts / 80%-100% threshold alerts / 5-minute call duration warning) routed to a future "billing polish" batch — they are usage-metering concerns, not payment-flow concerns. Batch A end-to-end CLOSED: Item 1 (pre-flight hardening) + Item 2 (plan-tier enforcement) + Item 3 (Xendit billing flow) all shipped. Deployment checklist additions for Item 3: XENDIT_WEBHOOK_TOKEN must be populated in .env.staging + .env.prod from CREDENTIALS.md Section 4.5 before exposing the webhook route on production traffic (handler has graceful 503 degradation when unset so dev/CI work fine); 2 pending migrations ship together via `prisma migrate deploy`. Two-stage review (Rule 25) PASS across all 6 commits: Stage 1 spec compliance vs PRODUCT.md L94-130 + security.md (XENDIT PAYMENT WEBHOOK SECURITY, SUPERADMIN, AGENT PROHIBITIONS #13, cron rule #7) verified; Stage 2 quality — zero `any`, TDD RED→GREEN evidenced on every commit, blast radius matches diffs, no scope creep. Recommended next session: confirm or revise Phase 8 Batch B proposal (real-time chat over Socket.IO + Reports CSV/PDF + LiveKit Egress recording feed — 3 Tier 2 sessions); apply ~20K Sonnet budget refinement as default Tier 2 split heuristic.** Prior phase 7 detail: **(fix/socket-io-singleton-globalthis) SLOT 2 SHIPPED 2026-05-23 PM — closes (incoming-call-dialog-not-rendered) ticket from 2026-05-23 PM handoff via globalThis-Symbol singleton hoist on the Socket.IO server module. Empirical Playwright multi-context smoke (alice + bob via auth-bypass, per-frame WS instrumentation on bob's page, bob.dialog.count post-click verification) confirmed STATE.md H3 hypothesis: alice's `calls.initiate` tRPC mutation returned 200 with full call payload, bob's socket connected and joined org channels (presence + in-call frames flowed; presence:snapshot delivered correctly), bob's `myBoundDepartmentIds` tRPC returned the correct Sales dept id (cmpfmyva2000fa8k1drezs7bw) — yet bob's WebSocket received ZERO `call:incoming` frames in the 3.5s post-click window and his dialog never rendered (H2 + H1 ruled out by trpc payload + presence flow respectively; H3 = "frame never reaches bob's socket" confirmed). Bundle forensics on `apps/web/.next/server/chunks/` proved the structural cause: Next.js + webpack splits `apps/web/src/server/socket/server.ts` into TWO module copies — chunk 7947 (module ID 67947) reached via instrumentation.ts's dynamic `await import("@/server/socket/server")` exposes `createSocketServer:()=>l` (this copy assigns the live ioInstance at boot); chunk 9865 reached via calls.ts's static `import { getIO }` exposes `H:()=>d` (this copy's module-local `let ioInstance = null` is NEVER assigned because tree-shaking stripped `createSocketServer` from it; verified empirically — `grep -c 67947 chunks/9865.js` returns 0). Result: every `getIO()` call from any tRPC route returned null, the `if (io !== null)` guard at calls.ts:59 silently no-op'd the emit, and `emitToOrg` was effectively dead code from any tRPC origin since the auth-gated socket server landed on 2026-05-19 (Phase 7 #15). Presence + in-call rosters appeared to work because their broadcasts originate from inside `io.on("connection")` in the instrumentation-chunk copy where the io is live; only the tRPC-originated org-channel emit was broken. Fix: store the singleton on `globalThis[Symbol.for("yelli.socket.io")]` — `Symbol.for(...)` returns the same symbol value across module copies (same realm) so both chunks read/write the same slot. Code change is minimal: replaced `let ioInstance: IOServer | null = null` with a typed globalThis Symbol slot accessor; `getIO()` reads from it, `createSocketServer` reads then assigns on first call. Idempotency contract preserved across copies (a route-chunk copy that accidentally invokes createSocketServer now returns the live io instead of building a second one that orphans the live connections). 2 files (1 source modified +29/-9, 1 test new with 5 cases: returns-null-when-unset, returns-io-after-create, uses-symbol-for-slot, **survives-module-duplication via vi.resetModules**, idempotent-across-copies — 3 RED → 5 GREEN). 294 → 299 ✓. Lint ✓ 0 errors (2 pre-existing warnings unchanged) / typecheck ✓ 0 errors / Playwright empirical re-verification: bob receives `42["call:incoming",{...recipientDeptId:"cmpfmyva2000fa8k1drezs7bw"...}]` frame at tRel=7064ms (41ms after alice's click), bob.dialog.count=1, alice navigates to `/app/call/971f919a-...`, callIncomingFrames=1. New 🔴 [[webpack-module-duplication-singleton-trap]] joins the trap family ([[webpack-define-plugin-trap]] is the sibling — DefinePlugin inlines NODE_ENV at build; this trap duplicates entire modules at build). Operational audit-grep rule: `grep -rn "^let [a-zA-Z_]* *= *null" apps/web/src/server/` flags module-local singletons; each must be either accessed from one chunk only OR hoisted to globalThis. Latent risk sites flagged: `createPresenceRoster()`/`createInCallRoster()` are passed cross-chunk only at construction (inside `createSocketServer`) so safe today, but any future tRPC route needing roster reads would hit the same trap. Closes SLOT 2 of 2026-05-23 PM handoff. Phase 7 realtime-engine close-out per 🟤 [[phase-7-realtime-engine-closeout-criterion]] now ELIGIBLE to convert PARTIAL → FULL: full unit-test correctness coverage + empirical multi-context Playwright smoke PASSED end-to-end (alice initiates → bob receives → bob's dialog renders).** Prior phase 7 detail: **(fix/turnstile-app-env-guard) HOTFIX shipped 2026-05-23 PM — third (and final known) remediation site for [[webpack-define-plugin-trap]]. Extracted pure helper `shouldEnforceTurnstileHostnameMatch({ APP_ENV })` in `apps/web/src/server/lib/turnstile.ts` matching `isE2EBypassEnabled` shape; rewired line-46 hostname-mismatch gate from `env.NODE_ENV === "production"` (DefinePlugin-inlined to constant true in containerized builds) to APP_ENV. 2 files (1 source modified + 1 test new); 3 RED→GREEN cases (dev/staging/prod). 291 → 294 ✓. Lint ✓ 0 errors (2 pre-existing warnings unchanged) / typecheck ✓ 0 errors / build ✓ Middleware 141 kB UNCHANGED. Bundle grep on `.next/standalone/.../chunks/9865.js` confirms `APP_ENV:d._.APP_ENV}` + `APP_ENV?{success:!1` PRESERVED as runtime property access; zero `NODE_ENV` references in the turnstile path. New 🟡 [[turnstile-app-env-guard]] catalog entry locks the third remediation site and the grep-based audit rule for future server-code reviews. Closes SLOT 1 of 2026-05-23 PM handoff. SLOT 2 closed in the immediately-prior entry above.** Prior phase 7 detail: **(fix/auth-bypass-app-env-guard) HOTFIX shipped 2026-05-23 AM — switched the (auth-bypass-for-e2e) prod-guard from NODE_ENV to APP_ENV after first-attempted smoke discovered webpack's DefinePlugin inlines `process.env.NODE_ENV` as the literal `"production"` at `next build` time, defeating the dual-gate in any containerized dev build. 3 files modified (env.ts +9 APP_ENV schema entry, auth-bypass.ts ~6 predicate signature, auth-bypass.test.ts replace_all NODE_ENV→APP_ENV). 287/287 ✓ unchanged. 2 lessons.md updates — NEW 🔴 [[webpack-define-plugin-trap]] documenting the inlining behavior with empirical bundle-grep proof, UPDATED 🔴 [[auth-bypass-prod-guard]] reflecting APP_ENV semantics. Provider verification + smoke retry next.** Prior phase 7 detail: **Phase 7 — (auth-bypass-for-e2e) infrastructure ticket shipped 2026-05-23 AM: env-gated NextAuth Credentials provider that bypasses Turnstile + bcrypt for Playwright smoke tests. Permanently replaces the manual "stop yelli_dev_app container" recipe documented in [[playwright-smoke-auth-configuration-blocker]]. Dual-gate guard (`AUTH_BYPASS_FOR_E2E=true` AND `NODE_ENV !== "production"`) in pure predicate `isE2EBypassEnabled(env)` — locked as 🔴 [[auth-bypass-prod-guard]] gotcha. Provider id `"e2e-bypass"` (distinct from real `"credentials"` so prod UI cannot accidentally route to it). 2 NEW source files (`apps/web/src/server/auth-bypass.ts` 130 lines + `apps/web/src/server/auth-bypass.test.ts` 140 lines, 9 RED→GREEN cases covering all NODE_ENV combinations + happy-path + 4 rejection paths); 2 modified (`apps/web/src/env.ts` +9, `apps/web/src/server/auth.ts` +9). Suite 278 → 287 ✓. Lint ✓ 0 errors / typecheck ✓ 0 errors across 8 packages / build ✓ Middleware 141 kB UNCHANGED (Edge surface stable — bypass code is Node-only) / audit --audit-level=critical ✓ exit 0. Two-stage review Rule 25 PASS. Inline Opus 4.7 controller (Tier 1; ~50K context). Next Playwright smoke session can now set `AUTH_BYPASS_FOR_E2E=true` in dev env and call `signIn("e2e-bypass", { email })` — no container surgery needed.** Prior phase 7 detail: **Phase 7 realtime engines (#11 user-presence / #12 dept-binding green-dot / #14 in-call yellow-dot / #15 incoming-call dialog / #16 dept-filter / #17 Rule 16 cleanup) CLOSED 2026-05-22 PM with full unit-test correctness coverage (270/270 ✓) + visual-QA gate PARTIAL. Follow-up `(admin-bounce-prefix-symmetry)` Tier 1 cosmetic also shipped 2026-05-22 PM — closes the last open Tier 1 from [[rule-16-cleanup-2026-05-22]] queue. NEW pure helper `buildTenantBouncePath(target, prefix)` in `apps/web/src/server/tenant-redirect.ts` (8 RED→GREEN cases; suite 270 → 278 ✓). NEW `x-tenant-path-prefix` request header set by middleware on `wasPathStripped` (the path-pattern dev route only; subdomain pattern in prod keeps prefix empty so the helper is a no-op). All 3 symmetric RSC bounce sites — `admin/layout.tsx` (host RBAC + auth fallback), `superadmin/layout.tsx` (non-platform + auth fallback), `app/call/[id]/page.tsx` (invalid-callId + auth fallback) — now read the header via `next/headers` and route through the helper before `redirect(...)`, so /t/system/admin → /t/system/app instead of bare /app. Middleware 141 kB UNCHANGED (Edge surface stable per [[instrumentation-edge-stub-required]]). Architecture reuses the established header-bridge pattern between Edge middleware and Node RSC ([[guest-meeting-layout-bypass]] precedent). 6 source files / 132 insertions / 7 deletions; two-stage review PASS.** Close-out criterion locked as 🟤 [[phase-7-realtime-engine-closeout-criterion]] in lessons.md: a Phase 7 realtime ticket is CLOSED when (a) all source-code fixes have shipped to main, (b) the unit-test layer fully covers the changed code paths (server-side socket handlers + pure client handlers + React hooks via fiber-mock; jsdom RTL deferred per established pattern), and (c) one of the following visual-QA outcomes is satisfied: (i) FULL — a multi-context Playwright smoke passes end-to-end, OR (ii) PARTIAL — backend + DB + socket layer verified healthy, UI auth-gated layer deferred with a 🔴 gotcha documenting the environmental blocker and the recipe to unblock it. The PARTIAL path is acceptable when the unit-test layer provides full code-path coverage and the environmental blocker is non-feature (env-config / dev-stack confound), because (a) the unit tests are the authoritative correctness gate and (b) the PARTIAL outcome is fully reproducible by following the documented recipe. Cascade evidence for closure: `5718a3d` (2026-05-22 second-pass smoke matrix — 4 PASS / 3 BLOCKED on diagnosed root cause) → `d364462` (fix/web — presence:ready handshake + SocketProvider StrictMode reconnect, +173/-22, 261 → 270 tests, ships both root cause + the strictmode masking bug uncovered during verification) → `e832987` (governance squash) → `dc9bb3a` (docs/trpc-client-procedure-type-missing as not-a-bug — proves the third "blocker" was a DevTools eval inspection trap, not a runtime bug) → `fc9395b` (governance — 2026-05-22 PM Playwright smoke PARTIAL outcome + 🔴 [[playwright-smoke-auth-configuration-blocker]] recipe). End state: zero outstanding source-code work on the realtime engines; the next Playwright smoke session (when a `signInForTest` helper lands or the stale `yelli_dev_app` container is removed) is verification-only, not correctness-blocking. Phase 7 active — sixteen Feature Updates merged + Rule 16 cleanup smoke pass complete + Task #21 cross-org tenant-scope hardening + (coturn-config-fix) + (t-slug-dev-routes-broken) + (root-landing-page) + (dev-app-redis-url) all shipped 2026-05-20 + (join-token-trpc) + (guest-meeting-page-render) + (guest-meeting-layout-bypass) shipped 2026-05-20 + (meeting-room-guest-disconnect-redirect) + (guest-meeting-livekit-peer-disconnect) instrumentation + (disconnect-reason-dual-meaning) + (guest-meeting-coturn-pc-connection) + (dev-app-healthcheck-route) + (disconnect-reason-description-refine) + (admin-users-list-tenant-scope) + (guest-meeting-livekit-turn-stage-prod) + (csp-allow-dev-cross-port-socket) shipped 2026-05-21 + (rule-16-cleanup) second pass shipped 2026-05-22 + (fresh-client-presence-snapshot-race) + (socketprovider-strictmode-reconnect) bundled and shipped 2026-05-22 + (trpc-client-procedure-type-missing) RESOLVED as not-a-bug 2026-05-22 + Phase 7 realtime engines (#11/#12/#14/#15/#16/#17) CLOSED with PARTIAL smoke acceptance 2026-05-22 PM.** (trpc-client-procedure-type-missing) closed on 2026-05-22 PM as a misdiagnosis. Investigation on `fix/trpc-client-procedure-type-missing` (branched from `e832987`) showed the underlying symptom — bob's `trpc.departments.myBoundDepartmentIds.useQuery()` stuck pending on fresh clients — was downstream of [[fresh-client-presence-snapshot-race]] and ALREADY fixed by `d364462`'s presence:ready handshake (per that 🟡 entry's cascade narrative). The `TypeError: client[procedureType] is not a function` observed post-fix was the tRPC typed-proxy responding to a non-procedure property access (`getQueryCache`, `fetch`, etc.) during DevTools fiber/queryClient inspection — NOT a runtime error in the application code. Reproduction attempt 2026-05-22 ~8:00 PM GMT+8: freshly registered user + bound department + loaded /app → `trpc.departments.myBoundDepartmentIds.useQuery()` returned `status: "success"` with correct data in 502ms (verified via fiber-walk with strict QueryClient instance guard that avoids touching the proxy); self-call via speed-dial tile → calls.initiate 70ms → LiveKit room joined cleanly. **#14 in-call yellow-dot + #15 incoming-call dialog UNBLOCKED.** Zero source-code changes ship on this branch — resolution is purely lessons.md update + a new 🔴 [[trpc-proxy-debug-inspection-trap]] entry documenting how createFlatProxy + createRecursiveProxy crash on ANY non-procedure property access via `clientCallTypeMap[pathCopy.pop()] === undefined` → `client[undefined](...)` (so future debuggers don't refile the same false positive). pnpm lint ✓ / typecheck ✓ / test 270/270 ✓ / build ✓ middleware 141 kB unchanged / audit --audit-level=critical ✓ exit 0 — all green on the empty source diff. Inline Opus 4.7 controller, no Sonnet dispatch (Tier 1 investigation; ~90K context: dev server reproduction + @trpc/client + @trpc/server bundle source review + Playwright fresh-user E2E + lessons.md write). (rule-16-cleanup) second pass closed on 2026-05-22: re-ran the Rule 16 cleanup smoke matrix via a Playwright multi-context rig that supersedes the 2026-05-19 single-context limitation. Pre-flight discovered the entire dev stack down (yelli_dev_* services exited ~1h, native pnpm dev orphaned with stale .next vendor chunks), recovered via process-kill + .next purge + service restart + DB reset/seed/fixtures (3 migrations + webmaster seed + /tmp/yelli-fixtures.ts with 4 system-org host users + 1 evil-org host user + 4 departments + bindings). 1 INLINE SOURCE-CODE FIX shipped during pre-flight: `(csp-allow-dev-cross-port-socket)` `ad8e090` — see PRIOR_PHASE_1 record for full ticket detail; without this fix even single-user presence verification was blocked. SMOKE MATRIX (3-user rig via `browser_run_code_unsafe { page.context().browser().newContext() }` — supersedes [[rule-16-cleanup-2026-05-19]] single-context lock; key constraint: globalThis is reset between unsafe calls so ALL multi-context orchestration must happen in ONE block): **PASS #3** /app/meetings/new — meeting created (`cmpfolebu0001y7zbr6v36g8j`), full meeting room rendered (heading + "1 participant · 00:21" ticking + alice's tile + full toolbar + Host badge), WebRTC PC connection succeeded, closes 2026-05-19 PARTIAL on coturn restarting; **PASS #7c** /t/{slug} subdomain redirect — 5 path patterns verified post (t-slug-dev-routes-broken): /t/system/app renders Speed Dial (rewrite), /t/evil/app redirects to /t/system/app (cross-org guard), /t/system/admin → /app (RBAC bounces host role — minor /t/ prefix loss noted as `(admin-bounce-prefix-symmetry)` Tier 1 cosmetic), /t/{slug}/api/health → 200 (public rewrite), unauthenticated /t/{*} → 307 to /login with callbackUrl preserving /t/{slug}/...; **PASS #11+#12 (alice-side)** — alice's onlineSet grew 1 → 3 (alice+bob+carol) as bob/carol logged in via ctx2/ctx3, her Speed Dial transitioned to Reception(online) + Sales(online) + FrontDesk(offline) + Support(offline) matching fixture bindings; **BLOCKED #14 in-call yellow-dot + #15 incoming-call dialog + #16 dept-filter** — bob's/carol's OWN React onlineSet stayed Set(0) even when alice saw them in HER set; IncomingCallDialog never rendered on bob when alice clicked Call Sales (his bound dept); diagnosed provisionally as fresh-client presence:snapshot race (server emits snapshot socket-direct on connect BEFORE the React useUserPresence hook's useEffect attaches the listener), cascades into bob's myBoundDepartmentIds query also not resolved when call:incoming arrives → `selectIncomingCall(payload, undefined) = false` per Phase 7 #16 design; filed `(fresh-client-presence-snapshot-race)` Tier 1-2 for dedicated investigation. Single-session Opus 4.7 inline orchestration (~115K context — no Sonnet dispatch; verification + browser-driven smoke + multi-context Playwright rig orchestration is sequential controller work). 1 NEW typed lesson: 🟤 [[rule-16-cleanup-2026-05-22]] documenting the multi-context rig technique (browser_run_code_unsafe + newContext()), globalThis-reset constraint, workspace-field-silent-fail confirmation, networkidle/HMR gotcha (use domcontentloaded for Next.js dev), smoke matrix outcomes, and snapshot-race hypothesis. Sibling 🟡 [[csp-dev-cross-port-socket-blocked]] from the inline fix. NEW QUEUE ITEMS: `(fresh-client-presence-snapshot-race)` Tier 1-2 (3 fix candidates: presence:ready ack from client; server delayed snapshot retry; tRPC-bootstrapped snapshot — lock candidate in DECISIONS_LOG.md first per Rule 10); `(admin-bounce-prefix-symmetry)` Tier 1 cosmetic (preserve /t/{slug} prefix on RBAC bounces). Two-stage review (Rule 25): Stage 1 spec — smoke matrix complete with PASS/BLOCKED labels + diagnosed root causes + filed tickets + 1 inline fix shipped per 2026-05-19 LIVEKIT_URL precedent. Stage 2 quality — verification-only pass for the smoke; the inline (csp-allow-dev-cross-port-socket) DID follow full Rule 25 TDD. SKIPPED vercel-plugin auto-suggestions throughout (next-forge, turbopack, next-cache-components, next-upgrade, vercel-storage, posttooluse-validate false positive on async headers()) per Rule 28. END STATE: 4 of 7 smoke targets PASS this session, 3 BLOCKED on one diagnosed new bug, 1 source-code fix shipped, 2 queue items filed, framework rig technique locked. Closes (rule-16-cleanup) second pass. Recommended next ticket: `(fresh-client-presence-snapshot-race)`.

Prior phase 7 detail: (fresh-client-presence-snapshot-race) + (socketprovider-strictmode-reconnect) bundled and closed on 2026-05-22. The (fresh-client-presence-snapshot-race) fix was implemented and unit-tested in the prior session (5 files: `apps/web/src/server/socket/presence.ts` + presence test + `lib/presence/user-presence-handler.ts` + handler test + `lib/socket/types.ts`; +173 / -22; 261 → 267 tests; the server now defers `presence:snapshot` emission until the client emits `presence:ready` after attaching listeners — idempotent guard against StrictMode + reconnect-resume duplicates). This session's smoke verification of #14/#15/#16 against that fix initially showed every dept (offline) on every page including alice — symptom worse than pre-fix. React fiber introspection found `SocketProvider.socket.connected: false` + `sendBufferLen: 2` (the queued `presence:ready` emits waiting for a connect that never came). Manually calling `socket.connect()` from the probe restored connection and Reception+Sales tiles immediately matched fixture bindings — proving the presence:ready fix is correct but MASKED by a separate `SocketProvider` lifecycle bug. Root cause: `SocketProvider`'s `useEffect` cleanup calls `socket.disconnect()`; Socket.IO does NOT auto-reconnect after explicit `.disconnect()` (its `reconnectionAttempts: 5` budget is transport-drop only). Under `reactStrictMode: true` dev double-fire (mount → cleanup → remount), the second mount attaches listeners to a permanently-dead socket. Fix: extracted `ensureSocketConnected(socket)` pure helper per [[pure-helper-extraction-pattern]]; invoked at top of `SocketProvider`'s effect to recover from any prior cleanup-induced disconnect. 3 RED→GREEN tests on the pure helper (267 → 270 total). Two-stage review Rule 25 PASS. End-to-end smoke verified post-fix: fresh-login alice + bob both report `socketConnected: true` automatically (no manual `.connect()` needed), `sendBufferLen: 0`, all Speed Dial tiles match fixture bindings — **#16 dept-filter UNBLOCKED**. THIRD BUG DISCOVERED during the same smoke: bob's `trpc.departments.myBoundDepartmentIds.useQuery()` in `IncomingCallDialog` is stuck in `status: "pending"` indefinitely despite the server returning 200 with correct data via plain `fetch()`. `queryClient.getQueryCache()` refetch throws `TypeError: client[procedureType] is not a function at @trpc/client/dist/index.mjs:184`. Independent of presence + socket — **filed as `(trpc-client-procedure-type-missing)` Tier 1-2 queue item** for separate investigation; blocks #14 + #15 until fixed. Branch: `feat/fresh-client-presence-snapshot-race` (squash-merge SHA recorded in agent-log after merge). 2 NEW typed lessons: 🔴 [[strictmode-socket-disconnect-permanent]] + 🔴 [[trpc-client-procedure-type-missing]]. Skipped: next-forge / nextjs / react-best-practices / verification plugin auto-suggestions per Rule 28.

Prior phase 7 detail: (csp-allow-dev-cross-port-socket) closed on 2026-05-21: discovered during the first action of (rule-16-cleanup) Phase 7 #17 smoke pass when alice's bound Reception department stayed offline despite successful login. React fiber introspection showed `SocketProvider.socket.connected: false` + `onlineSet = Set(0)`; 11 console errors of the form `Connecting to 'http://localhost:43515/socket.io/...' violates Content Security Policy directive "connect-src 'self' https://challenges.cloudflare.com wss: ws:"`. Root cause: cross-port HTTP polling probe in EIO=4 handshake — dev runs Next.js on APP_PORT=43512 + Socket.IO on SOCKET_PORT=43515 (separate listener via instrumentation.ts); browser treats these as different origins; CSP's `connect-src` baseline allowed only same-origin HTTP + WebSocket (ws:/wss:) but socket.io-client v4 issues an HTTP polling probe during handshake even with `transports: ['websocket', 'polling']`. Fix: extracted security headers into `apps/web/src/lib/security-headers.ts` as `buildSecurityHeaders({isDev})`; dev branch appends `http://localhost:* http://127.0.0.1:*` to connect-src; prod returns the V18 + V27 baseline unchanged. next.config.ts now imports the builder and calls it with `isDev: process.env.NODE_ENV !== "production"`. 11 RED→GREEN cases lock the dev-vs-prod split (dev includes both localhost forms, prod includes neither, baseline tokens preserved, frame-ancestors stays 'none', non-CSP headers identical). Tier 1 inline Opus 4.7 controller (no Sonnet dispatch — 4 files / 172 insertions / 44 deletions / 2 new files). Tests 250 → 261 (+11); pnpm typecheck ✓ / pnpm lint ✓ / pnpm build ✓ 22 routes + middleware 141 kB UNCHANGED (MANDATORY per [[instrumentation-edge-stub-required]]); pnpm audit --audit-level=critical ✓ exit 0. Squash-merged to main as `ad8e090`. NEW typed lesson: 🟡 [[csp-dev-cross-port-socket-blocked]] documenting the cross-port HTTP polling block + the wider rule (any Next.js app spawning auxiliary HTTP listeners on host ports other than APP_PORT MUST widen `connect-src` in dev; prod is safe via Traefik + wss:). End-to-end verified post-restart: `socket.connected: true`, socket.id assigned, onlineSet contains alice's userId, Reception button shows "Call Reception (online)" + enabled, other 3 depts correctly offline. Closes `(csp-allow-dev-cross-port-socket)` from Phase 7 #17 emergent queue. Unblocks resumption of #14/#15/#16 multi-user smoke and presence verification within (rule-16-cleanup).

Prior phase 7 detail: (guest-meeting-livekit-peer-disconnect) instrumentation closed on 2026-05-21: direct Tier 1 follow-up to (meeting-room-guest-disconnect-redirect) `24101c6`. That ticket gated the user-visible symptom (sessionless guests no longer 302 to /login on meeting end). This ticket instruments the underlying ~3s post-signal-connect LiveKit peer disconnect on the guest path so a future smoke can confirm empirically which of the three pre-ranked hypotheses is the root cause: (a) coturn/ICE/TURN failure on guest path despite `75ab34f` container fix; (b) StrictMode double-mount with same JWT identity → DUPLICATE_IDENTITY; (c) `useMeetingRoom` effect re-run due to unstable `guestCredentials` object reference from `guest-meeting-room-loader.tsx:91-94` (fresh object literal every render) → CLIENT_INITIATED. FIX (3 files, 2 new, 1 modified): NEW `apps/web/src/lib/livekit/disconnect-reason.ts` (+207) — pure helper `describeDisconnectReason(reason): { reason, label, hypothesis, description }` exhaustively mapping all 17 LiveKit DisconnectReason enum values + undefined to one of six hypothesis tags (`client-cleanup` | `duplicate-identity` | `transport-failure` | `media-failure` | `server-initiated` | `unknown`). NEW `apps/web/src/lib/livekit/disconnect-reason.test.ts` (+112, 9 RED→GREEN — first vitest run = `ERR_MODULE_NOT_FOUND`, second after impl = 9/9 passed). MODIFIED `apps/web/src/lib/livekit/use-meeting-room.ts` (+15/-2) — RoomEvent.Disconnected handler now receives `(reason?: DisconnectReason)`, computes diagnosis, console.warn's a structured one-line `[livekit] RoomEvent.Disconnected — reason={LABEL} hypothesis={HYPOTHESIS} :: {description}` before flipping status to "ended". Pure instrumentation — hook API surface unchanged, downstream consumers see zero behavioral change. Validation: pnpm vitest run 246/246 ✓ (was 237, +9); pnpm typecheck ✓; pnpm lint ✓ 0 errors (fixed initial `consistent-type-imports` error by switching to `type DisconnectReason` mixed-mode syntax); pnpm build ✓ 22 routes + middleware 141 kB unchanged. Two-stage review (Rule 25): Stage 1 spec PASS — hypothesis field in output IS the verdict (smoke output directly maps to next fix); Stage 2 quality PASS — TDD RED→GREEN evidenced, zero `any`, scope = 3 files matching plan, no scope creep, conventional commit `dbaa90c`. Hypothesis (c) confirmed by CODE INSPECTION but not yet by runtime — `guest-meeting-room-loader.tsx:91-94` creates fresh object literal every render; `useMeetingRoom`'s effect dep array includes `guestCredentials`; parent re-render = effect re-run = cleanup `room.disconnect()` = CLIENT_INITIATED. Per Rule 29 the BROWSER SMOKE remains the required step before committing the loader memo fix (deferred ticket `(guest-meeting-loader-memo-stability)`). 0 new typed lessons. Skipped next-forge / react-best-practices / nextjs plugin auto-suggestions per Rule 28. Closes the INSTRUMENTATION phase of `(guest-meeting-livekit-peer-disconnect)`; OBSERVATION phase queued.

Prior phase 7 detail: (meeting-room-guest-disconnect-redirect) shipped on 2026-05-21 as `24101c6`. (dev-app-redis-url) closed on 2026-05-20: dev environment fully restored — yelli_dev_app boots healthy, browser smoke confirmed via `curl /` → HTTP 200 with all 5 expected unauthed landing strings ("Yelli", "Real-time team comms", "Get started", "Sign in", "Powerbyte"). SCOPE EXPANDED mid-ticket from REDIS_URL-only to REDIS_URL + Prisma binary because REDIS_URL alone left the app crashing on first DB query (Prisma engine binary mismatch surfaced once instrumentation cleared) — same in-scope-by-necessity pattern as [[coturn-config-fix]] precedent. ROOT CAUSE A: Phase 3 generated REDIS_PASSWORD via `openssl rand -base64 32 | head -c 22` which includes `/+=`; the actual value contained a literal `/`. compose/dev/docker-compose.app.yml:34 constructs `REDIS_URL: redis://:${REDIS_PASSWORD}@${COMPOSE_PROJECT_NAME}_valkey:6379` by raw string interpolation — `/` broke URL parse + Zod `.url()` rejected → instrumentation crash. ROOT CAUSE B: host (debian/glibc) generated debian-only Prisma client; Dockerfile uses node:22-alpine (musl); .prisma/client copied from host shipped the wrong .so.node into container. FIX: (a) regenerated REDIS_PASSWORD in .env.dev (gitignored) via `openssl rand -hex 16` → 32-char URL-safe hex; updated REDIS_URL field in lockstep; (b) added `binaryTargets = ["native", "linux-musl-openssl-3.0.x"]` to `generator client` in packages/db/prisma/schema.prisma; `pnpm db:generate` produced both libquery_engine .so.node variants. Tracked diff: 1 file (schema.prisma) / 6 insertions / 1 deletion. pnpm test 192/192 ✓ / pnpm typecheck ✓ / pnpm build ✓ 22 routes unchanged. Inline Opus 4.7 controller (no Sonnet dispatch — Tier 1, scope expanded mid-ticket per stacked-bug rule). Two-stage review (Rule 25): Stage 1 spec PASS (browser smoke now unblocked, the actual ticket goal), Stage 2 quality PASS (no `any`, no scope creep beyond what's needed to boot, conventional commit ahead). NEW typed lesson: ⚖️ [[url-unsafe-chars-in-interpolated-passwords]] documenting base64 `/+=` × inline-compose-URL footgun + recovery + Phase 3 generation policy gap + Prisma musl binary as related Alpine gotcha + STACKED-BUG RULE codifying when to expand ticket scope. Three new follow-up tickets queued: (phase-3-password-url-safety), (compose-url-encoding-pattern), (staging-prod-env-audit). BONUS: this session also confirmed (root-landing-page) renders correctly in browser — blocked smoke from prior session now done. SKIPPED bootstrap + vercel-storage skill suggestions — Prisma binaryTargets is standard Prisma API, not Vercel-specific. Phase 7 UI loop unblocked going forward. Closes `(dev-app-redis-url)` from queue.

Prior phase 7 detail: (root-landing-page) closed on 2026-05-20: replaced the 10-line auth-redirect at `apps/web/src/app/page.tsx` (self-hosted style — `if (session?.user) redirect("/app"); else redirect("/login")`) with a minimal public landing matching PRODUCT.md line 233 (`/` = "Public landing page (SaaS) or login redirect (self-hosted)"). Yelli is the SaaS deployment so public-landing is the correct branch. NEW pure helper `apps/web/src/lib/landing/cta.ts` (~28 lines — `getLandingCTAs({isAuthed}): {primary, secondary?}` returning plain-data CTA descriptors). NEW colocated test (~50 lines, 6 RED→GREEN — covers both branches, locks funnel priority primary=/register-not-/login for unauthed, asserts plain-data shape). REPLACED page.tsx (~75 lines) — Server Component awaits `auth()` once, calls `getLandingCTAs({ isAuthed: Boolean(session?.user) })`, renders header (Yelli wordmark + nav with secondary + primary CTAs) + hero (h1 + lede + duplicated primary CTAs) + thin footer. Authed visitors see "Go to app" → /app primary CTA only (per user choice over force-redirect — matches Vercel/Stripe pattern). Unauthed see "Get started" → /register + "Sign in" → /login. `<Button asChild>` + `<Link>` from `@yelli/ui/button` + `next/link` per established login-page convention. Tailwind utility classes only via shadcn/ui CSS variables (Rule 21 graceful degradation — no design-system/MASTER.md present). Tests 186 → 192 (+6). pnpm lint ✓ 0 errors / pnpm typecheck ✓ clean / pnpm build ✓ 22 routes + middleware 141kB UNCHANGED, `/` route now ƒ 1.75kB / 115kB first-load. BROWSER SMOKE BLOCKED on PRE-EXISTING dev infra issue: yelli_dev_app container was already `Up (unhealthy)` for 2h+ failing instrumentation hook with `Invalid server environment variables: REDIS_URL: Invalid url`; ALL routes (/, /login, /api/health) return HTTP 500 from the stale container build. Risk-mitigated by: build compilation success + full unit coverage of CTA selection + Server Component with no client interactivity + Tailwind only + battle-tested `<Button asChild>` pattern. Inline Opus 4.7 controller (no Sonnet dispatch — Tier 1, 3 files / ~118 net insertions). Two-stage review (Rule 25): Stage 1 PASS (PRODUCT.md `/` is public + matches user-confirmed scope), Stage 2 PASS (no `any`, TDD verified, scope = 3 files matching plan exactly, no scope creep, shadcn primitives only per UI Rule #1). NEW typed lesson: 🔴 [[dev-app-redis-url-invalid]] documenting the `Up (unhealthy)` + uniform-HTTP-500 + instrumentation-crash pattern and the `docker logs ... | grep -B2 -A8 "Invalid server"` recovery diagnostic. NEW queue finding (NOT fixed here): (dev-app-redis-url) — recovery path documented in lessons.md. SKIPPED next-cache-components skill — out of scope for a redirect → minimal stub. Closes `(f) Root landing page` from Phase 7 #17 backlog queue.

Prior phase 7 detail: (t-slug-dev-routes-broken) closed on 2026-05-20: middleware now rewrites `/t/{slug}/<rest>` → `/<rest>` via `NextResponse.rewrite` so the existing /app, /admin, /superadmin route tree serves localhost dev tenant URLs. New pure helper `stripTenantPathPrefix(path, slug)` in `apps/web/src/server/tenant-redirect.ts` (12 cases: happy path + single-segment + no-trailing + /-trailing + admin/superadmin/api paths + no-/t/-prefix no-op + empty-slug no-op + slug-arg-mismatch no-op + /tenants/* non-match + /t/yelli-other non-match + regex-special-char defensive escape). Middleware now computes `effectivePath` ONCE and uses it for: (a) `isProtected` check (so unauthenticated users on `/t/yelli/app/foo` now get redirected to /login instead of silently passing through), (b) `resolveTenantRedirect.path` (so the /superadmin slug-bypass works on dev URLs too), (c) the `NextResponse.rewrite(effectivePath, { request: { headers: requestHeaders } })` call when `wasPathStripped`. Headers (x-tenant-slug, x-user-id, x-organization-id, x-organization-slug) DO survive the rewrite when passed via `{ request: { headers } }`. Unauthenticated `callbackUrl` preserves the ORIGINAL `/t/{slug}/...` so login returns the user to the same tenant-prefixed URL. Validation: 13 new RED→GREEN tests; suite 173 → 186. pnpm typecheck ✓ 0 errors / lint ✓ 0 errors (2 pre-existing warnings unchanged) / build ✓ 22 routes + middleware 141kB unchanged (MANDATORY per [[instrumentation-edge-stub-required]] — middleware is the Edge surface). Inline Opus 4.7 controller (no Sonnet dispatch — Tier 1, 3 files / 146 insertions / 8 deletions). Two-stage review (Rule 25): Stage 1 PASS (all 4 path families — /app, /admin, /superadmin, /api — rewrite to canonical paths; headers preserved; tenant cross-check uses effective path), Stage 2 PASS (no `any`, TDD verified, scope = 3 files matching plan exactly, no scope creep). New typed lesson: 🟡 [[t-slug-dev-routes-broken]] documenting the "middleware extracts BUT doesn't rewrite → pure tests pass but 404 in browser" anti-pattern and the heuristic "if pure-function tests pass but the route still 404s, the integration layer (middleware/rewrites/route resolution) is the gap." Unblocks (#7c) subdomain redirect smoke (was blocked on path-pattern dev URL serving). Skipped routing-middleware skill's `proxy.ts` rename suggestion — project on Next.js 15, that's a separate `next-upgrade` ticket. Closes `(t-slug-dev-routes-broken)` from Rule 16 cleanup queue.

Prior phase 7 detail: (coturn-config-fix) closed on 2026-05-20: dev/staging/prod `COTURN_STATIC_AUTH_SECRET` filled with 3 independent 48-char openssl secrets; deprecated `--no-tlsv1_1` flag removed from all 3 docker-compose.media.yml files (`coturn/coturn:latest` image dropped the deny-list scheme in favor of minimum-version flags). Two latent bugs were blocking coturn — placeholder string for the HMAC secret AND the deprecated flag — both fixed in one Tier 1 ticket. Validation: `bash deploy/compose/start.sh dev up -d` brought stack clean; STUN protocol test via `turnutils_stunclient -p 43542 127.0.0.1` returned `IPv4. UDP reflexive addr: 172.25.0.1:49796` (PASS); container `status=running, restarts=0, exitCode=0` stable for 30+ seconds. Inline Opus 4.7 controller (no Sonnet dispatch, no source-code changes — YAML-only diff). No source-code touched → skipped pnpm typecheck/lint/test/build per blast-radius minimum; validated via direct functional STUN test instead. New typed lesson: 🟡 [[coturn-no-tlsv1-1-flag-dropped]] (flag-rename pattern + critical `docker logs | head` diagnostic for coturn crashes — `--tail` shows only help-text, the real error is always on lines 1-2). New CREDENTIALS.md section added (gitignored) documenting per-env Coturn realm + length + port + generation command + rotation guidance — closes a governance gap where Bootstrap Step 18 predated WebRTC scope. WebRTC peer connection now functional on localhost; `(rule-16-followup-multi-user)` (e2e re-smoke for #14/#15/#16) is no longer blocked from the coturn side — still needs multi-browser rig per [[rule-16-cleanup-2026-05-19]]. Closes `(coturn-config-fix)` from Rule 16 cleanup queue.

Prior phase 7 detail: Task #21 (admin-users-list-tenant-scope) shipped 2026-05-19. Confirmed the Rule 16 smoke finding was a real cross-org leak, not intentional super-admin visibility. Root cause: L6 tenant-guard Prisma extension in `packages/db/src/client.ts` carried an `if (!ctx || ctx.isSuperAdmin) return query(args)` bypass at the `$allOperations` interceptor — every tenant_admin who also held `is_super_admin=true` silently skipped tenant filtering on EVERY route using the guarded prisma client. Fix is two-layer per security.md §SUPERADMIN AND PLATFORM-LEVEL ROLES: (a) ROOT — removed the `ctx.isSuperAdmin` bypass; only `!ctx` pass-through remains for bootstrap/seed scripts; dropped vestigial `isSuperAdmin` field from `TenantContext` interface and from the `runWithTenantContext` payload in trpc.ts. (b) DEFENSE-IN-DEPTH — added explicit `where: { organization_id: ctx.organizationId }` to every list/count/aggregate query in 7 routers (admin/calls/meetings/departments/chat/recordings/billing). NEW `apps/web/src/server/trpc/routers/admin.test.ts` (~220 lines, 6 RED→GREEN — explicit-org assertion under both regular AND super-admin sessions; dashboard.stats per-count org filter assertion; exportCallLogsCsv org filter assertion; RBAC short-circuit). 1 existing test updated (departments.test.ts myBoundDepartmentIds assertion shape). Tier 2 inline Opus 4.7 controller execution (no Sonnet dispatch — single coherent security fix across 12 files, ~93 insertions / ~39 deletions). Test suite 167 → 173. pnpm typecheck ✓ / lint ✓ / build ✓ 21 routes + middleware 141kB / audit ✓ exit 0. Two-stage review (Rule 25) PASS: Stage 1 spec compliance vs security.md §SUPERADMIN rules 1+4 verified; Stage 2 quality — zero `any`, TDD RED→GREEN evidenced, blast radius matches diff exactly. 1 new typed lesson: 🔴 [[l6-super-admin-bypass-leak]] documenting the pattern + recognition heuristic ("any phrase like 'bypass for super-admin' inside a Prisma extension/middleware/shared helper is invisible at the call site — fix at the extension layer, not by patching every resolver"). End state: cross-org isolation now guaranteed for every L6-guarded query regardless of caller's platform role. Super-admin cross-org access remains via `superAdminProcedure` + `platformPrisma` path (organizations.list/byId/suspend/unsuspend, platformSettings.get/update) — explicit, gated, logged with `PLATFORM:*` action prefix. Closes `(admin-users-list-tenant-scope)` from Rule 16 cleanup queue.

Prior phase 7 detail: Rule 16 cleanup smoke pass shipped 2026-05-19. First full end-to-end visual-QA pass through the four realtime engines (Phase 7 #11 user-presence + #12 dept-binding green dots + #14 in-call yellow dots + #15+#16 incoming-call dialog with dept filter) plus accumulated carryover (#3, #4, #7c, #8e, #10, #13). 12 smoke tasks executed via Playwright MCP with system Chrome: **6 PASSED** (#8e socket handshake, #10 session:invalidated via security_version path ~120s = 2× loop interval correcting prior ≤60s spec claim, #4 forgot-password full flow, #11 presence multi-tab coalescing, #12 Speed Dial green-dot, #13 admin-binding-ui), **1 PARTIAL** (#3 /app/meetings/new — form + creation + LiveKit token mint OK after env fix, but WebRTC peer connection fails on coturn restarting 255 — corrects prior STATE.md claim that dev video calls work without coturn), **5 BLOCKED** (#14/#15/#16 need 2+ distinct user sessions concurrently that Playwright MCP single-context cookie-sharing cannot provide + working WebRTC; #7c /t/{slug}/* always 404 because no Next.js route handlers exist under apps/web/src/app/t/ and middleware doesn't rewrite the path). ONE CODE FIX SHIPPED INLINE: discovered apps/web/src/env.ts schema declared `LIVEKIT_WS_URL` but every .env.* + every compose file uses `LIVEKIT_URL` → mintLiveKitToken's wsUrl always undefined → meetings.getJoinToken returned 503 on every meeting/call attempt. 5-line code-side rename in env.ts + 1-line in lib/livekit/client.ts. Branch `fix/livekit-url-env-rename` commit `417ed97`, squash-merged to main as `f9f88bf`. Validation: pnpm test 167/167 ✓ unchanged, typecheck/lint/build all green. Two-stage review (Rule 25) PASS. THREE FINDINGS QUEUED AS SEPARATE TICKETS: `(t-slug-dev-routes-broken)` (middleware unit tests pass but the constructed URL doesn't actually serve a page — needs route handlers OR middleware path-rewrite, rewrite preferred), `(admin-users-list-tenant-scope)` (Mallory from evil org appears in admin.users.list dropdown when current user is_super_admin=true — may be intentional super-admin visibility but security.md §SUPERADMIN forbids inline if (isSuperadmin) skip bypass, needs verification with plain tenant_admin), `(coturn-config-fix)` (fill COTURN_STATIC_AUTH_SECRET ⏳ placeholder so coturn stops restarting + WebRTC works locally). FOURTH FOLLOW-UP: `(rule-16-followup-multi-user)` to re-run #14/#15/#16 e2e once coturn fixed AND multi-browser rig available. 3 new typed lessons logged: 🔴 [[livekit-env-name-mismatch]], 🔴 [[t-slug-dev-routes-broken]], 🟤 [[rule-16-cleanup-2026-05-19]] (locks Playwright MCP single-context multi-user testing limitation as framework decision). 0 new DECISIONS_LOG entries — all findings are bugs or observations, not architectural decisions.

Prior phase 7 detail: Phase 7 #16 (department-binding-filter) squash-merged 2026-05-19 as `fdfc3ca`. Closes the only deferred-scope item from Phase 7 #15: the IncomingCallDialog now filters incoming `call:incoming` broadcasts by the current user's bound department(s) instead of ringing every org member. NEW `apps/web/src/lib/calls/select-incoming-call.ts` (22 lines — pure filter helper per [[pure-helper-extraction-pattern]], `selectIncomingCall(payload, boundDeptIds): boolean`; returns false on `undefined` (query loading) and `[]` (user unbound) per design decisions 3+4; `Array.prototype.includes` for multi-binding correctness per decision 2). NEW `departments.myBoundDepartmentIds: protectedProcedure.query` (~18 lines — read-only reverse lookup user → dept ids; returns `string[]` because `Department.default_user_id` has no `@unique` constraint, so one user may legitimately man multiple departments; L6 scopes to caller's org via `runWithTenantContext`; no L5 AuditLog because read-only). MODIFIED `apps/web/src/components/call/incoming-call-dialog.tsx` (+16/-6 — wires `trpc.departments.myBoundDepartmentIds.useQuery()` near existing useState declarations; `handleIncoming` short-circuits on `!selectIncomingCall(incoming, boundDeptIds)`; `useEffect` dep array now `[socket, boundDeptIds, startRingtone, stopRingtone]` so listener re-binds when query resolves; drops Phase 7 #15 TODO block). Total: 5 files / 6 deletions / 138 insertions. Test suite 160 → 167 (+7 RED→GREEN: 4 helper + 3 router). pnpm typecheck ✓ 0 errors 8 packages. pnpm lint ✓ 0 errors (2 pre-existing warnings unchanged). pnpm build ✓ 21 routes + middleware 141kB (MANDATORY per [[instrumentation-edge-stub-required]] — departments.ts joins the tRPC bundle transitively imported by Edge middleware). pnpm audit --audit-level=critical ✓ exit 0 (1 HIGH = documented nodemailer per [[nodemailer-cve-mitigation]]). Two-stage review (Rule 25): Stage 1 spec PASS (8/8 — tRPC query exists not session-encoded / string[] return type with multi-binding test / empty array → false / undefined → false / pure helper extracted / no audit log on read / no socket type changes / no invalidate wiring) + Stage 2 quality PASS (6/6 — zero `any` in new code / only `as never` test-mock casts per established Phase 7 #13/#14/#15 pattern / TDD RED→GREEN evidenced in T1.a + T2.a / 5-file blast radius matches spec inventory exactly / conventional commits on all 3 task commits / useEffect deps correct). Inline controller execution (Opus 4.7) — no Sonnet dispatch this ticket; Tier 1 surface (score 20.5) didn't justify per-task subagent overhead. 0 new lessons logged — patterns covered by existing typed entries. End-to-end flow now correct: caller initiates → `emitToOrg` fans out → only the bound user's dialog passes the filter and rings. Cross-org isolation still guarded by `joinOrgChannel` (since Phase 7 #15); per-dept isolation now guarded client-side. Closes `(department-binding-filter)`.

Prior phase 7 detail: **fifteen Feature Updates merged through #15.** Phase 7 #15 (legacy-socket-retirement) squash-merged 2026-05-19 as `af43276`. Retires the unbootstrapped legacy Socket.IO scaffold and wires `call:incoming` + `call:rejected` through the existing auth-gated server. NEW `apps/web/src/server/socket/calls.ts` (53 lines — `attachCallHandlers({io, socket})` joins both org-scoped channels on connect via `joinOrgChannel`, listens for client-emitted `call:reject` with server-resolved caller identity from `socket.data.session.userId`, broadcasts `call:rejected {callId, reason: "declined"}` org-scoped via `emitToOrg`; NO ROSTER unlike presence + in-call — calls are ephemeral per-event). NEW `apps/web/src/lib/calls/incoming-call-handler.ts` (43 lines — pure helper mirroring `lib/presence/in-call-handler.ts` byte-for-byte; exports `MinimalIncomingCallSocketTarget` + `RejectedPayload` + `IncomingCallCallbacks` + `IncomingCallDisposer`). NEW module-level `getIO()` singleton in `server/socket/server.ts` (+17 lines: ioInstance + idempotent guard for HMR safety + getIO accessor + attachCallHandlers wiring). REWIRED `server/trpc/routers/calls.ts` — switched import from legacy `@/lib/socket/server` to `@/server/socket/server` + `@/server/socket/channels`; inlined `emitToOrg(io, ctx.organizationId, "call:incoming", {callId, callerName, callerDepartment, roomName, recipientDeptId})` replacing legacy `emitIncomingCall`; DELETED entire `reject` mutation (was lines 81-98, zero call sites). MIGRATED `incoming-call-dialog.tsx` from raw `io()` connection to a 503 placeholder → `useSocketOptional()` + `attachIncomingCallHandler` for both `call:incoming` and `call:rejected` listeners; reject button emits via shared socket. Added `recipientDeptId: string` field to `IncomingCallPayload` (lib/livekit/types.ts). DELETED legacy server.ts (159 lines — `initSocketServer` had ZERO call sites in apps/web/src; the phantom `server/custom-server.ts` referenced in module comments never existed) + api/socket/route.ts (39 lines — 503 placeholder). DELETED 17 lines of dead types in lib/socket/types.ts: `presence:subscribe` + `presence:heartbeat` + `presence:update` events, `subscribedDepartmentIds` field on SocketData, `callIncomingRoom` + `callerRoom` helpers, `PresenceState` import. Total: 12 files / 269 lines deleted / 536 inserted. Test suite 144 → 160 (+16: 8 server handler + 5 pure client handler + 3 router initiate). pnpm typecheck ✓ 0 errors 8 packages. pnpm lint ✓ 0 errors. pnpm build ✓ 22 routes + middleware 141kB (MANDATORY per [[instrumentation-edge-stub-required]] — calls.ts is in createSocketServer import chain). pnpm audit --audit-level=critical ✓ exit 0 (1 HIGH = documented nodemailer per [[nodemailer-cve-mitigation]]). Two-stage review (Rule 25): Stage 1 spec PASS (10/10) + Stage 2 quality PASS (8/8). Subagent-driven execution per user choice. Two Sonnet thrash recoveries during the ticket — Task 5 (router rewire + reject deletion + test creation, 3+ files in one dispatch) and Task 12 (governance docs across 2217 lines) — controller (Opus 4.7) absorbed both inline by pre-baking context aggressively. 0 new typed lessons logged. Closes `[[parallel-socket-servers-coexistence]]` from Phase 7 #11 — auth-gated server is now the single source of truth for all socket events. First REAL end-to-end implementation of incoming-call (prior production state had the dialog connecting to a 503 placeholder).

Prior phase 7 detail: **fourteen Feature Updates merged through #14.** Phase 7 #14 (in-call-state) squash-merged 2026-05-18 as `6d3b6f8`. Adds the yellow `in_call` dot derivation on the Speed Dial Board — a twin-roster + twin-hook engine parallel to Phase 7 #11's user-level presence. NEW `apps/web/src/server/socket/in-call.ts` (149 lines — `createInCallRoster()` returns `Map<orgId, Map<userId, Set<socketId>>>` with `{wasFirst}/{isLast}` coalescing; `attachInCallHandlers({io, socket, roster})` joins `${orgId}:call:active` via `joinOrgChannel`, emits `call:active-snapshot` socket-direct on connect, listens for `call:joined`/`call:left` events from the browser, and broadcasts `call:active {userId, in_call: boolean}` org-scoped via `emitToOrg` only on 0↔1 transitions; disconnect cleanup handles crashed browsers). NEW `apps/web/src/lib/presence/in-call-handler.ts` (77 lines — pure client handler mirroring `user-presence-handler.ts` byte-for-byte; node-testable). NEW `apps/web/src/lib/presence/use-users-in-call.ts` (69 lines — React hook returning `ReadonlySet<userId>`; consumes shared `useSocketOptional()` from Phase 7 #10; jsdom test deferred per #11 precedent). NEW `apps/web/src/lib/livekit/use-emit-call-participation.ts` (52 lines — composable hook wiring `socket.emit("call:joined"|"call:left")` to `RoomEvent.Connected`/`Disconnected`; invoked from BOTH `useLiveKitRoom` (intercom 1:1) AND `useMeetingRoom` (multi-participant meetings) so both call flows feed the same roster). MODIFIED `selectDepartmentPresence(dept, online, inCall)` adds 3rd `ReadonlySet<string>` parameter with locked precedence: unbound → offline, inCall.has → in_call (wins), online → online, else → offline. UI was already wired since Phase 4 Part 5b (`PresenceState` union includes `"in_call"`, `<SpeedDialButton>` renders yellow dot + disabled state) — this ticket adds the missing data plane. Test suite 124 → 144 (+20 RED→GREEN: 5 roster + 6 server-handler + 5 client-handler + 4 helper). pnpm build ✓ 22 static + dynamic routes + middleware 140kB (Edge gate per [[instrumentation-edge-stub-required]] — in-call.ts is in createSocketServer import chain). pnpm audit --audit-level=critical ✓ exit 0 (Phase 7 #9 acceptance + #10 CLI flag still in effect). Two-stage review (Rule 25) PASS: 8/8 spec items + 8/8 quality items. Fully inline execution (Opus 4.7 controller, no subagent dispatch) per user choice after Phase 7 #13's Sonnet thrash. Closes second deferred-scope from Phase 7 #12. No new lessons logged — existing patterns covered everything.

Prior phase 7 detail: **thirteen Feature Updates merged through #13.** Phase 7 #13 (admin-binding-ui) squash-merged 2026-05-17 as `77d94b2`. Adds tenant-admin self-service for `Department.default_user_id` via a new "Default user" column in /admin/departments. New tRPC `departments.setDefaultUser({ departmentId, userId | null })` mutation under `adminProcedure` reuses L3 RBAC + L5 AuditLog + L6 tenant scope; rejects inactive users with `BAD_REQUEST` (defense-in-depth race guard). New `<DepartmentUserPicker>` component (121 lines, shadcn `<Select>`) per row sources options from `admin.users.list` filtered client-side to active users; handles deactivated-user edge case cosmetically. Edit dialog untouched — binding flow is column-level and atomic. Currently bindings could only be set via direct pgAdmin UPDATE; this closes the deferred-scope from Phase 7 #12. 7 RED→GREEN cases added (test suite 117 → 124): happy path bind, clear-binding (userId:null), non-admin FORBIDDEN, cross-org dept NOT_FOUND, cross-org user NOT_FOUND, inactive user BAD_REQUEST, audit-log shape assertion. pnpm build ✓ 22 static + dynamic routes. pnpm audit --audit-level=critical ✓ exit 0 (Phase 7 #9 acceptance + #10 CLI flag still in effect). Two-stage review (Rule 25) PASS. Final whole-branch reviewer subagent APPROVED FOR MERGE with 12/12 spec items + 8/8 quality items passing. Controller pivoted from subagent-driven to inline execution on Task 3 after a Sonnet 4.6 autocompact-thrashing event; reviewer subagents continued to provide independent verification at every commit + the final whole-branch gate.

Prior phase 7 detail: **twelve Feature Updates merged through #12.** Phase 7 #12 (department-binding presence) squash-merged 2026-05-17 as `446a97f`. Adds nullable `default_user_id` FK on Department → User (`@@index`, `ON DELETE SET NULL`) and rewires `<SpeedDialGrid>` to compose two pure helpers (`extractBoundUserIds` + `selectDepartmentPresence`) over `useUserPresence(boundUserIds)` from the Phase 7 #11 engine. Legacy `apps/web/src/lib/presence/use-presence.ts` deleted (sole consumer migrated; closes the cleanup started in #11). Speed Dial Board now shows real per-department green dots when the designated user is connected; unbound or offline-user departments render gray + disabled (matches existing UX for unconfigured cells). Migration `20260517075117_add_department_default_user_id` is additive + non-destructive — existing rows keep `default_user_id = NULL` and render as offline. Test suite 108 → 117 (+9 RED→GREEN cases for the pure helpers; node-testable per [[pure-helper-extraction-pattern]] — no jsdom/RTL added to the project). pnpm build ✓ 27 routes (per [[instrumentation-edge-stub-required]] — schema change → prisma generate → bundle impact verified). pnpm audit ✓ exit 0. Two-stage review (Rule 25) PASS. Closes (department-binding) candidate. First end-to-end realtime UX surface on the user-level presence engine — `selectDepartmentPresence(department, online)` is the framework precedent for any future "derive single-resource state from user-presence map" pattern (e.g. meeting host online indicators, department-call routing, user-availability filters in dashboards).

Prior phase 7 detail: **eleven Feature Updates merged through #11.** Phase 7 #11 (user-level presence engine) squash-merged 2026-05-17 as `81356e9`. NEW `apps/web/src/server/socket/presence.ts` (118 lines — `createPresenceRoster()` returns an in-memory `Map<orgId, Map<userId, Set<socketId>>>` with `{wasFirst}`/`{isLast}` coalescing so multi-tab connections only emit ONE online/offline transition per real user-state change; `attachPresenceHandlers({io, socket, roster})` joins `${orgId}:presence:user` via `joinOrgChannel`, emits `presence:user {userId, online:true}` iff wasFirst, sends `presence:snapshot {userIds[]}` socket-direct, registers a disconnect handler that emits `presence:user {online:false}` iff isLast). Wired into `createSocketServer` AFTER the auth middleware so `socket.data.session` is guaranteed present. NEW `apps/web/src/lib/presence/user-presence-handler.ts` (74 lines — pure handler `attachUserPresenceHandlers(socket, {onRoster, onUpdate})` returning disposer; overloaded `MinimalSocketEventTarget` interface so real TypedSocket consumers get strict payload types). NEW `apps/web/src/lib/presence/use-user-presence.ts` (60 lines — "use client" React hook `useUserPresence(userIds): Record<userId, boolean>` consuming the shared `useSocketOptional()` from Phase 7 #10's SocketProvider; uses `useState<Set<userId>>` for the authoritative online set + `useMemo` to project onto the caller's requested id list; graceful degradation when socket null). MIGRATED legacy `apps/web/src/lib/presence/use-presence.ts` (-98/+46 lines) — gutted the broken `io(NEXT_PUBLIC_APP_URL)` connection that was opening wrong-origin sockets per page, the `presence:subscribe`/`presence:heartbeat` emits, and the 30s heartbeat interval. Now consumes `useSocketOptional()` (no new socket) and returns frozen all-offline for every requested departmentId. Public `<SpeedDialGrid>` contract preserved exactly so no consumers had to change. Department-binding (userId↔departmentId mapping needed to feed real per-department state from the engine into Speed Dial) is the natural follow-up ticket. Test suite 85→108 (+23 RED→GREEN cases). 2 new lessons logged: 🟤 [[presence-roster-coalesce-pattern]], 🟤 [[parallel-socket-servers-coexistence]]. Unblocks every realtime org-scoped feature — broadcast-on-connect/disconnect pattern is now the framework precedent (`emitToOrg(io, organizationId, eventType, payload)` with the auth middleware + `joinOrgChannel` enforcing zero cross-org leakage).

Prior phase 7 detail: **ten Feature Updates merged through #10.** Phase 7 #10 (socket-client — client-side Socket.IO provider + useSocket hook) squash-merged 2026-05-17 as `9d21461`. NEW `apps/web/src/lib/socket/client.ts` (60 lines — pure factory `createSocketClient(opts)` wrapping `io()` with withCredentials:true so the Auth.js cookie flows from APP_PORT=43512 to SOCKET_PORT=43515 cross-origin; transports websocket+polling; reconnectionAttempts:5). NEW `apps/web/src/lib/socket/session-invalidation.ts` (35 lines — pure handler `attachSessionInvalidationHandler(socket, onInvalidated)` subscribing to the server-emitted `session:invalidated` event from the Phase 7 #8e-2 revalidation loop; returns disposer; node-testable). NEW `apps/web/src/lib/socket/socket-context.tsx` (109 lines — "use client" SocketProvider composing factory + handler with `useRouter().push("/login") + .refresh()` as the invalidation callback; exports `useSocket()` + `useSocketOptional()`). Wired into `apps/web/src/app/app/layout.tsx` so every authenticated page can consume the typed socket. ALSO FIXED two pre-existing regressions caught by #10's full validation pass: (a) Phase 7 #8e build break — `pnpm build` was failing with `Module not found: 'http'` because webpack statically analyses dynamic-import string literals in instrumentation.ts for both Node AND Edge graphs; fixed via `webpack: (config, { nextRuntime }) => { if (nextRuntime === "edge") config.resolve.alias = { "socket.io": false, "@/server/socket/server": false, "@/server/socket/revalidation": false }; config.resolve.fallback = { http: false, https: false, crypto: false, path: false } }` in next.config.ts. (b) Phase 7 #9 audit-threshold false claim — `pnpm audit` was still exiting 1 on HIGH advisories despite `.npmrc audit-level=critical` because pnpm 10's `pnpm audit` doesn't honor the .npmrc threshold for its exit code (only `pnpm config get` reads it); restored `--audit-level=critical` CLI flag in ci.yml. Test suite 75→85 (+10). 3 new lessons logged: 🔴 [[pnpm10-audit-level-ignored]], 🔴 [[instrumentation-edge-stub-required]], 🟤 socket-client provider pattern (pure factory + pure handler + thin React shell — extends [[pure-helper-extraction-pattern]] to React-incompatible test environments). Unblocks (presence) — the next end-to-end realtime feature can now consume `useSocket()` from any client component under `/app/*`.

Prior phase 7 detail: **eight Feature Updates merged through #9.** Phase 7 #8 (Socket.IO realtime foundation) squash-merged 2026-05-16: e-1 `dd57c9c` adds instrumentation.ts hook + Socket.IO server factory + JWT-verify auth middleware on a separate port (SOCKET_PORT=43515 dev); e-2 `c399b43` adds org-scoped channel helpers (joinOrgChannel takes NO orgId param so cross-org subscription is API-impossible) + 60s session revalidation loop per security.md §Realtime Connection Safety; e-3 governance. Test suite 42→75 (+33). New DECISIONS_LOG entry locks hosting topology = option B (instrumentation + separate port). Phase 7 #9 (nodemailer HIGH CVE acceptance) squash-merged 2026-05-17 as `03f74f8` — documented-acceptance pattern locked, but the .npmrc-only threshold-lift approach was caught by Phase 7 #10 as not working on pnpm 10; restored CLI flag preserves #9's policy intent.

Prior phase 7 detail: **seven Feature Updates merged.** Post-Phase-4 dev bring-up complete; production build passes end-to-end. Phase 7 #1 (auth.register tRPC wire-up + register page submit) squash-merged as `ce709ff`. Phase 7 #2 (vitest@4 infrastructure + 5-case auth.register smoke coverage) squash-merged as `81c0279` — closes Rule 25 TDD deferral. Phase 7 #3 (/app/meetings/new — 7-case meetings.test.ts + react-hook-form + zodResolver port + shadcn Form primitive in @yelli/ui + shared MeetingCreateClientInputSchema) squash-merged as `8709595` — closes UI Rule #4 gap for the meetings creation flow. Phase 7 #4 (forgot-password + reset-password tRPC + UI — new PasswordResetToken Prisma model + migration, 8 new test cases bringing the suite to 20/20, /forgot-password page wired to mutation, new /reset-password/[token] server + client form using shadcn Form + RHF + Zod, lazy nodemailer transport wrapper, security_version invalidation on successful reset) squash-merged as `2cdc3c3` — closes the auth quartet (register/login/forgot/reset). Phase 7 #5 (coverage threshold gate — added `thresholds` block to apps/web/vitest.config.ts with global floor 12/6/12/12 and per-file gate src/server/trpc/routers/auth.ts at 100/75/100/100, new `test:coverage` script + turbo task, new `coverage` CI job that runs the gate and uploads HTML/lcov artifact for 14 days) squash-merged as `78fc022` — locks the 20-test safety net against silent regression before Phase 8. Phase 7 #6 (per-user 24h reset-request cap on authRouter.requestPasswordReset — adds 5/24h cap as defence-in-depth on top of the per-email LRU rate limit; cap-met returns ok silently and skips mint + email send, preserving the no-enumeration response shape) squash-merged as `5325b8c` — closes the email-bomb gap where attackers passing Turnstile could space requests past the 10/min LRU and still flood a known user. Phase 7 #7 (JWT org-slug encoding + middleware URL↔session cross-check — Tier 3 ticket decomposed into 3 sub-sessions; (c)-1 `0e0a892` adds `organizationSlug: string` to all 4 next-auth module augmentations and wires it through the jwt callback, both session callbacks, and the authorize return; (c)-2 `740cdd2` extracts pure decision/URL-building helpers to apps/web/src/server/tenant-redirect.ts and wires them into middleware.ts to enforce URL↔session slug match with super-admin path-scoped bypass on /superadmin/* only; (c)-3 governance batch) — closes the security.md §TENANT MIDDLEWARE SAFETY gap and the TODO Part 5b+ at middleware.ts:96; unblocks Phase 7 #8 candidate (e) Socket.IO auth which needs the JWT-encoded slug. Sub-Phase-5 validation passes (8 of 9) — full Phase 5/6 gated on CREDENTIALS.md ⏳ placeholders.

Prior status: **4 Part 8 complete — Phase 4 fully done.** CI/CD workflows + MANIFEST.txt + README.md
+ final IMPLEMENTATION_MAP rewrite all generated. Repository is now structurally complete and
ready for Phase 5 validation. `.github/workflows/ci.yml` runs governance gates (validate-inputs,
check-product-sync, hydration-lint), the Turbo quality matrix (lint/typecheck/test/build), and a
HIGH+CRITICAL CVE audit on every push to main and every PR. `.github/workflows/docker-publish.yml`
pushes three Docker Hub tags per merge to main — `:latest`, `:staging-latest`, `:sha-<short>` —
sized for the V27 Komodo flow (staging auto_update: true polls Docker Hub; prod is manual Deploy
from Komodo UI). `MANIFEST.txt` enumerates every tracked file across all 8 Phase-4 Parts, with
[GIT]/[LOCAL]/[SOFT] tags and a grand total of ~255 tracked files. `README.md` is the
project-root developer onboarding document — quick start, daily commands, Phase 7 loop,
architecture summary, codebase intelligence (SocratiCode/Context7/shadcn MCP), file structure,
phase status, and the Phase-5 credential checklist. SocratiCode initial index trigger is
documented as a post-merge human action (MCP not loaded in this session — see Next Step below).

App: Yelli (instant video intercom SaaS + self-hosted)
Framework: Spec-Driven Platform V31

## Built So Far

- ✅ Phase 7 Feature Update — fresh-client presence:snapshot race fix (2026-05-22, ticket (fresh-client-presence-snapshot-race), Tier 2)
  - Race: server `attachPresenceHandlers` emitted `presence:snapshot` synchronously inside `io.on("connection")` immediately after `roster.addSocket(...)`. Client `useUserPresence` hook attaches its listener inside a `useEffect` (post-React-commit, often post-`socket.connected`). Fresh clients dropped the snapshot → `onlineSet` stayed `Set(0)` even though the server-side roster was correct. Cascade blocked #14 in-call yellow-dot + #15 incoming-call dialog + #16 dept-filter (because `useEmitCallParticipation.myBoundDepartmentIds` never resolved on fresh clients → `selectIncomingCall(payload, undefined) = false` → `IncomingCallDialog` never rendered). Diagnosed in [[rule-16-cleanup-2026-05-22]] smoke matrix.
  - FIX (option a from DECISIONS_LOG lock): client emits `presence:ready` from `attachUserPresenceHandlers` AFTER both `socket.on(...)` listeners register. Server defers the snapshot emit until receiving `presence:ready`, idempotent via `snapshotEmitted` flag (defends against React StrictMode double-fire + reconnect-resume duplicate ready). Three candidates evaluated (server timeout-retry + tRPC-bootstrapped snapshot rejected as fragile / two-sources-of-truth respectively).
  - MODIFIED `apps/web/src/server/socket/presence.ts` (+15 / -3) — added `const PRESENCE_READY_EVENT = "presence:ready"`. Replaced synchronous `socket.emit(PRESENCE_SNAPSHOT_EVENT, ...)` with `socket.on(PRESENCE_READY_EVENT, () => { if (snapshotEmitted) return; snapshotEmitted = true; socket.emit(...); })` gate.
  - MODIFIED `apps/web/src/server/socket/presence.test.ts` (+62 / -18) — added `fireReady(socket)` + `findSnapshotCall(socket)` helpers. 3 NEW RED→GREEN cases: "does NOT emit presence:snapshot on connect alone — waits for presence:ready (race fix)", "registers a presence:ready handler on the socket", "snapshot emission is idempotent on duplicate presence:ready (defensive)". 2 existing tests updated to fire `presence:ready` between attach and assertion.
  - MODIFIED `apps/web/src/lib/presence/user-presence-handler.ts` (+11 / 0) — extended `MinimalSocketEventTarget` with `emit(event: "presence:ready"): unknown` overload. Added `socket.emit("presence:ready")` to `attachUserPresenceHandlers` AFTER both `socket.on(...)` calls register.
  - MODIFIED `apps/web/src/lib/presence/user-presence-handler.test.ts` (+78 / -11) — `makeFakeSocket` gained `emit` method + `emittedToServer` capture; the test-side dispatcher was renamed `fireFromServer` to disambiguate server→client delivery from client→server emits. 3 NEW RED→GREEN cases: "emits presence:ready to the server AFTER attaching listeners (handshake)", "emits presence:ready ONCE per attach call (a second attach is a separate handshake)", "emits presence:ready AFTER both listeners have been registered (ordering)".
  - MODIFIED `apps/web/src/lib/socket/types.ts` (+7 / 0) — added `"presence:ready": () => void` to `ClientToServerEvents` with cross-reference comment.
  - Validation: pnpm lint ✓ 0 errors; pnpm typecheck ✓ clean across 8 packages; pnpm test 261 → 267 ✓ (+6 RED→GREEN); pnpm build ✓ 22 routes + Middleware 141 kB UNCHANGED (socket types are NOT on the middleware edge — types.ts is consumed by socket-server.ts in the Node.js server runtime); pnpm audit --audit-level=critical ✓ exit 0 (1 HIGH = pre-documented nodemailer per [[nodemailer-cve-mitigation]]).
  - Two-stage review (Rule 25): Stage 1 spec PASS — implementation matches DECISIONS_LOG option (a) verbatim. Stage 2 quality PASS — zero `any` types added, TDD RED→GREEN evidenced (6 RED then 6 GREEN), blast radius exactly 5 files / +185 / -22 matching pre-declared plan, conventional commit.
  - Single-session Opus 4.7 inline implementation. Tier 2 score 23.5 (5 files × 2.5 + 1 module × 5 + depth 2 × 3) — below 40 mandatory-split threshold. Session aggregate ~55K context including investigation + TDD + validation + governance.
  - NEW typed lesson: 🟡 [[fresh-client-presence-snapshot-race]] documenting the race + chosen fix + wider rule for socket bootstrap patterns + applicability audit for sibling `call:active-snapshot` (in-call.ts) if multi-user in-call smoke shows the same symptom.
  - SKIPPED skill prompts: next-forge + nextjs + verification (auto-suggested on Read/pnpm-build/echo pattern matches) — task is socket.io protocol gating, not Next.js framework migration, build optimization, or Vercel project config. Rejected per Rule 28 priority order.
  - Branch `feat/fresh-client-presence-snapshot-race` (pre-squash). End-to-end browser smoke deferred — requires dev-server bounce (instrumentation.ts is not HMR-aware so the new presence:ready handler needs a fresh socket-server boot) + multi-context Playwright re-run of #14/#15/#16. Recommended as next-session action before squash-merge.

- ✅ Phase 7 Feature Update — LiveKit DisconnectReason instrumentation (2026-05-21, ticket (guest-meeting-livekit-peer-disconnect) — INSTRUMENTATION phase, Tier 1)
  - NEW `apps/web/src/lib/livekit/disconnect-reason.ts` (+207 lines) — pure helper `describeDisconnectReason(reason: DisconnectReason | undefined): { reason, label, hypothesis, description }`. Exhaustive switch over all 17 LiveKit `DisconnectReason` enum values (UNKNOWN_REASON, CLIENT_INITIATED, DUPLICATE_IDENTITY, SERVER_SHUTDOWN, PARTICIPANT_REMOVED, ROOM_DELETED, STATE_MISMATCH, JOIN_FAILURE, MIGRATION, SIGNAL_CLOSE, ROOM_CLOSED, USER_UNAVAILABLE, USER_REJECTED, SIP_TRUNK_FAILURE, CONNECTION_TIMEOUT, MEDIA_FAILURE, AGENT_ERROR) + `undefined`, returning one of six hypothesis tags: `client-cleanup` | `duplicate-identity` | `transport-failure` | `media-failure` | `server-initiated` | `unknown`. Each case carries a short diagnostic description text pointing at the suspected root cause and the diagnostic next-step. Pure function (no I/O, no React, no side effects); matches established pure-helper pattern alongside `end-of-call-policy.ts` / `guest-bypass.ts` / `guest-credentials.ts`.
  - NEW `apps/web/src/lib/livekit/disconnect-reason.test.ts` (+112 lines, 9 RED→GREEN tests) — coverage spans hypothesis-tag assertions for each major reason (CLIENT_INITIATED, DUPLICATE_IDENTITY, CONNECTION_TIMEOUT, SIGNAL_CLOSE, JOIN_FAILURE, MEDIA_FAILURE), server-initiated cluster (SERVER_SHUTDOWN, ROOM_DELETED, PARTICIPANT_REMOVED), unknown/undefined fallback, and an exhaustive enum sweep verifying every value yields a non-empty description + label. First vitest run = `ERR_MODULE_NOT_FOUND` (RED); GREEN 9/9 after impl.
  - MODIFIED `apps/web/src/lib/livekit/use-meeting-room.ts` (+15 / -2) — mixed-mode `import { Room, RoomEvent, type DisconnectReason } from "livekit-client"` plus value import of `describeDisconnectReason`. The `RoomEvent.Disconnected` handler signature changed from `() =>` to `(reason?: DisconnectReason) =>`; body computes `diagnosis = describeDisconnectReason(reason)` and `console.warn`'s a structured one-line `[livekit] RoomEvent.Disconnected — reason={LABEL} hypothesis={HYPOTHESIS} :: {description}` BEFORE the existing `setStatus("ended")` call. Pure instrumentation — hook API surface unchanged, downstream consumers see zero behavioral change.
  - Validation: pnpm vitest run 246/246 ✓ (was 237, +9 helper tests). pnpm typecheck ✓ clean. pnpm lint ✓ 0 errors (initial `consistent-type-imports` error on `DisconnectReason` value-mode import resolved inline by switching to mixed-mode `type` syntax; 2 pre-existing warnings unchanged outside diff). pnpm build ✓ 22 routes; **middleware bundle 141 kB UNCHANGED** (Edge surface guard per [[instrumentation-edge-stub-required]] — `use-meeting-room.ts` is client-only, not transitively imported by middleware/instrumentation; mandatory build also rules out indirect graph regression).
  - Two-stage review (Rule 25): Stage 1 spec PASS — every `RoomEvent.Disconnected` fire now console.warn's a structured diagnosis at warn-level. The `hypothesis=` value IS the verdict that selects the next fix (client-cleanup → loader memo fix; transport-failure → coturn/ICE investigation; duplicate-identity → StrictMode mitigation). Stage 2 quality PASS — TDD RED→GREEN evidenced, zero `any` types, scope = 3 files matching pre-declared plan, no scope creep, conventional commit `dbaa90c`. Hypothesis (c) is confirmed by CODE INSPECTION (`guest-meeting-room-loader.tsx:91-94` creates fresh object literal every render; effect dep includes `guestCredentials`) but NOT yet by runtime observation. Per Rule 29 the browser smoke remains the required step before committing the loader memo fix in `(guest-meeting-loader-memo-stability)`.
  - Security (security.md compliance): console.warn logs ONLY enum label + hypothesis tag + canned description text — NO credentials/JWT/PII/session data. Hook API surface byte-for-byte unchanged. Pure helper has no I/O so cannot leak credentials via reason values.
  - Browser smoke: NOT YET RUN. Per user's explicit choice, instrumentation ships first; smoke runs as separate verification step. Dev Docker services are UP; Next dev server is NOT running. Next session: start `pnpm dev`, host login + meeting create, fresh Playwright MCP context (no cookies), navigate to `/app/meeting/{id}?guest=1`, match the `hypothesis=` value to the corresponding fix.
  - 0 new typed lessons — pure-helper-extraction pattern is well-established. Closes the INSTRUMENTATION phase of `(guest-meeting-livekit-peer-disconnect)`; OBSERVATION phase queued.

- ✅ Phase 7 Feature Update — department-binding presence (2026-05-17, ticket (department-binding), Tier 2)
  - NEW `apps/web/src/components/speed-dial/department-presence.ts` (30 lines) — two pure helpers tested in node env per [[pure-helper-extraction-pattern]] (extends Phase 7 #7c-2 / #8e / #10 / #11 precedent). `extractBoundUserIds(departments): string[]` filters nulls + preserves order (does NOT dedupe — caller responsibility if needed). `selectDepartmentPresence(department, online): PresenceState` returns `"online"` only when `default_user_id !== null AND online[default_user_id] === true`, else `"offline"`. Treats undefined presence entries as offline (defensive). No `"in_call"` emission — that state is deferred to a follow-up ticket that would derive from an active-call query or `call:active` broadcast.
  - NEW `apps/web/src/components/speed-dial/department-presence.test.ts` (75 lines, 9 RED→GREEN cases) — 4 for extractBoundUserIds (empty input, null-filtering with three departments, order preservation, no-dedup) + 5 for selectDepartmentPresence (bound+online, bound+offline, bound+missing-from-map-as-offline, unbound (null FK) renders offline, defensive undefined presence entry as offline). RED→GREEN proven inline (Task 1 stub threw `not implemented`; Task 2 impl made all 9 GREEN in 270ms).
  - NEW `packages/db/prisma/migrations/20260517075117_add_department_default_user_id/migration.sql` (8 lines) — `ALTER TABLE "departments" ADD COLUMN "default_user_id" TEXT` + `CREATE INDEX "departments_default_user_id_idx"` + FK to `"users"(id)` with `ON DELETE SET NULL ON UPDATE CASCADE`. TEXT type matches the cuid() id convention across all 13 models (NOT UUID — plan's `@db.Uuid` annotation was caught wrong at pre-flight read of schema.prisma; correction documented in lessons).
  - MODIFIED `apps/web/src/components/speed-dial/speed-dial-grid.tsx` (+9 / -4 lines) — local `Department` interface widened with `default_user_id: string | null`. `usePresence(ids)` replaced with `useUserPresence(extractBoundUserIds(departments))`. `presenceState={presence[dept.id] ?? "offline"}` prop replaced with `selectDepartmentPresence(dept, online)`. `<SpeedDialButton>` untouched — same prop shape, same `PresenceState` union (we just never emit `"in_call"` in this ticket).
  - MODIFIED `packages/db/prisma/schema.prisma` (+5 lines on Department, +1 inverse on User) — `default_user_id String?` field + `default_user User? @relation("DepartmentDefaultUser", fields:[default_user_id], references:[id], onDelete: SetNull)` + `@@index([default_user_id])`; inverse `default_for_departments Department[] @relation("DepartmentDefaultUser")` on User. Named relation disambiguates from any future Department→User relations.
  - MODIFIED `apps/web/src/app/app/page.tsx` (+1 line) — `default_user_id: true` added to the `prisma.department.findMany({ select: {...} })` clause so the SC's typed prop flows the new field through to `<SpeedDialGrid>` without prop-type narrowing.
  - DELETED `apps/web/src/lib/presence/use-presence.ts` (-55 lines) — legacy department-level stub. Sole consumer (`<SpeedDialGrid>`) migrated to `useUserPresence` via the new pure helpers. Closes the migration started in Phase 7 #11 — pre-#11 the hook was opening wrong-origin unauthenticated sockets per consumer; #11 made it a no-op stub; #12 deletes it entirely. The legacy /api/socket server's never-fired `presence:update` event in lib/socket/types.ts is now orphaned but stays in place per [[parallel-socket-servers-coexistence]] until (legacy-socket-retirement) lands.
  - **Locked decisions added** (from spec): binding model = `default_user_id` FK on Department (rejected `device_binding_token` separate-device-login + many-to-many `department_users` table as YAGNI); resolution path = client-side via `useUserPresence(boundUserIds)` (rejected option (a) server-side `presence:department` emit — would have required a new type-map event + emit-time roster lookup); `in_call` state deferred to follow-up ticket; admin UI deferred to follow-up ticket; unbound departments render as offline (rejected new `"unconfigured"` `PresenceState` variant — more state to maintain for marginal UX value).
  - Verification: tests 108 → 117 (+9 RED→GREEN cases) / typecheck 0 errors across 8 packages (FULL TURBO cache after schema → prisma generate) / lint 0 errors (2 pre-existing warnings unchanged) / `pnpm build` 27 routes ✓ in 1m26s (build was MANDATORY per [[instrumentation-edge-stub-required]] — schema change → Prisma client regen → potential type-import bundle impact; Phase 7 #10's Edge-runtime webpack alias remained load-bearing and untouched) / `pnpm audit --audit-level=critical` exit 0 ✓ (Phase 7 #9 CVE acceptance + Phase 7 #10 CLI flag enforcement still in effect) / `prisma migrate status` "Database schema is up to date!" with 3 migrations. Two-stage review (Rule 25): Stage 1 spec PASS (all 5 locked spec decisions verified) / Stage 2 quality PASS (no `any`, no type assertions, RED→GREEN proven, scope = 7 files matching plan inventory exactly, conventional commits, pure helpers have no React imports).
  - Tier 2 single-session, ~55K Opus context (under the 80K SAFE zone; no Sonnet dispatch, no §2.5b escalation justification needed). 7 files touched (3 new + 3 modified + 1 deleted).
  - Plan was executed inline via superpowers:executing-plans with TaskCreate tracking. 11 plan tasks all completed: Task 0 (pre-flight) → Tasks 1-2 (RED + GREEN helpers, committed as `f9e6aef`) → Task 3 (grid wiring, `84951a2`) → Task 4 (legacy hook deletion, `39f0037`) → Task 5 (page.tsx callsite, `73d5886`) → Task 6 (schema + migration, `940680b`) → Task 7 (seed update SKIPPED — no seeded departments to bind) → Task 8 (full validation suite) → Task 9 (two-stage review) → Task 10 (this governance batch) → Task 11 (squash-merge).
  - Lessons added: 0 — nothing novel emerged. The cuid/UUID schema-type discovery was a pre-flight catch (caught by reading schema.prisma BEFORE generating the migration), not a runtime gotcha. Existing patterns ([[pure-helper-extraction-pattern]] for helper extraction, [[parallel-socket-servers-coexistence]] for leaving the legacy event in place) covered everything.

- ✅ Phase 7 Feature Update — user-level presence engine (2026-05-17, ticket (presence), Tier 2)
  - NEW `apps/web/src/server/socket/presence.ts` (118 lines) — `createPresenceRoster()` returns a pure in-memory `Map<orgId, Map<userId, Set<socketId>>>` with `addSocket → {wasFirst}` / `removeSocket → {isLast}` semantics. Multi-tab presence coalescing: only the 0→1 transition emits "online", only the 1→0 transition emits "offline" — tab refresh / HMR reconnect / navigation never flicker the user's state. `attachPresenceHandlers({io, socket, roster})` is the wiring layer — joins `${orgId}:presence:user` via `joinOrgChannel(socket, "presence:user")`, emits `presence:user {userId, online:true}` to the org channel iff wasFirst, sends `presence:snapshot {userIds[]}` socket-direct to the new connection (including self for visual confirmation), registers a disconnect handler that emits `presence:user {online:false}` iff isLast. Defensive bail when `socket.data.session` is missing (post-auth-middleware should never happen).
  - NEW `apps/web/src/server/socket/presence.test.ts` (260 lines, 18 RED→GREEN cases) — full contract proven: roster math (9 cases for empty default, wasFirst/isLast transitions, cross-org isolation, multi-user same org, defensive unknowns, idempotent same-(user,socket) re-add) + wiring (9 cases for join channel, emit on first / suppress on multi-tab, snapshot includes self, emit-offline on last disconnect / suppress on non-last, cross-org disconnect isolation, no-session defensive bail, two-user roster propagation).
  - NEW `apps/web/src/lib/presence/user-presence-handler.ts` (74 lines) — pure `attachUserPresenceHandlers(socket, {onRoster, onUpdate})` returning disposer. Overloaded `MinimalSocketEventTarget` interface discriminates by event name so consumers get strict payload types from a real TypedSocket. No React import → node-testable per [[pure-helper-extraction-pattern]] (extends Phase 7 #7c-2 + #8e + #10 precedent).
  - NEW `apps/web/src/lib/presence/user-presence-handler.test.ts` (103 lines, 5 RED→GREEN cases) — register both listeners, fire onRoster on `presence:snapshot`, fire onUpdate on `presence:user` (true then false), dispose removes both, ignore unrelated events. Uses boundary-cast pattern (`loose as unknown as MinimalSocketEventTarget`) per established test convention (revalidation.test.ts `as never`, channels.test.ts `as unknown as IOServer`).
  - NEW `apps/web/src/lib/presence/use-user-presence.ts` (60 lines) — "use client" `useUserPresence(userIds: string[]): Record<userId, boolean>` hook. Consumes `useSocketOptional()` from Phase 7 #10's SocketProvider (no new socket — shares the SocketProvider connection); composes the pure handler with `useState<Set<userId>>` for the server-authoritative online set + `useMemo` to project onto the caller's requested id list. Graceful degradation: socket null → every requested id maps to `false`. Stable boolean for every requested id, even ids not present in the org roster.
  - MODIFIED `apps/web/src/server/socket/server.ts` (+13 lines) — `createPresenceRoster()` instantiated once per server lifecycle (HMR-safe since the IOServer factory is itself called once per process); `io.on("connection", socket => attachPresenceHandlers({io, socket, roster}))` registered AFTER `io.use(socketAuthMiddleware)` so `socket.data.session` is guaranteed populated.
  - MODIFIED `apps/web/src/lib/socket/types.ts` (+11 lines) — added `"presence:user": (payload: {userId, online}) => void` + `"presence:snapshot": (payload: {userIds[]}) => void` to ServerToClientEvents. Legacy `"presence:update": ({departmentId, state})` kept verbatim for backwards compat with the still-live `lib/socket/server.ts` /api/socket path (Phase 5b code consumed by `app/api/socket/route.ts` + the calls router's `emitIncomingCall`). Event names are distinct so the two servers' presence events DO NOT collide in the shared type map.
  - MODIFIED `apps/web/src/lib/presence/use-presence.ts` (-98 / +46 lines) — legacy department-level hook gutted of its broken connection. Pre-#11: opened its own `io(clientEnv.NEXT_PUBLIC_APP_URL)` connection to the legacy /api/socket path with no auth, no shared connection (N consumers = N sockets per page), and listened for `presence:update` events that nothing on the legacy server ever emitted (silently all-offline since Phase 5b). Post-#11: consumes `useSocketOptional()` (shares the SocketProvider's auth-gated connection on SOCKET_PORT), returns a frozen `Record<departmentId, "offline">` for every requested id. JSDoc documents the stub status — real wiring comes in the Department-Binding follow-up ticket which adds the userId↔departmentId resolution. `<SpeedDialGrid>` public contract preserved exactly so no consumers needed to change.
  - **Locked decisions added**: presence-roster topology (process-local in-memory, single-process per deployment env, Redis adapter migration documented inline for post-Phase-6 multi-instance). Channel name `${orgId}:presence:user` derived from `joinOrgChannel(socket, "presence:user")` — cross-org subscription remains API-impossible (no orgId param surface).
  - Verification: tests 85 → 108 (+23 RED→GREEN cases) / typecheck 0 errors across 8 packages / lint 0 errors (2 pre-existing warnings unchanged) / `pnpm build` 27 routes ✓ in 63s (build was MANDATORY per [[instrumentation-edge-stub-required]] — new presence module is in createSocketServer → instrumentation.ts chain; Phase 7 #10's Edge-runtime webpack alias remained load-bearing and untouched) / `pnpm audit --audit-level=critical` exit 0 ✓ (Phase 7 #9 CVE acceptance + Phase 7 #10 CLI flag enforcement still in effect). Two-stage review (Rule 25): Stage 1 spec PASS / Stage 2 quality PASS.
  - Tier 2 single-session, ~70K Opus context (under the 80K SAFE zone; no Sonnet dispatch, no §2.5b escalation justification needed). 8 files touched (5 new + 3 modified).
  - Lessons added: 2 new 🟤 decision entries — [[presence-roster-coalesce-pattern]] (multi-tab `{wasFirst}`/`{isLast}` coalescing pattern), [[parallel-socket-servers-coexistence]] (two-server reality + distinct-event-name discipline for the shared type map).

- ✅ Phase 7 Feature Update — nodemailer HIGH CVE mitigation (2026-05-17, ticket (j), Tier 1)
  - NEW `.npmrc` at project root — `audit-level=critical`. Header comment documents the rationale (GHSA-rcmh-qjqh-p98v, nodemailer 6.9.16 pinned by Auth.js v5 peer range, server-stamped addresses make the DoS unreachable) and points at DECISIONS_LOG + lessons.md.
  - `apps/web/src/server/lib/email.ts` — JSDoc header documenting why the CVE is acceptable in this module: `from` server-stamped from `env.SMTP_FROM`, `to` from Zod-validated User.email column, subject/body composed from server-controlled values. No user-controlled string reaches nodemailer's address parser, so the DoS vector is unreachable. Revisit triggers documented inline.
  - `.github/workflows/ci.yml` — dropped hardcoded `--audit-level=high` flag from the security job's audit step. Bare `pnpm audit` now respects `.npmrc audit-level=critical`. Single source of truth: change `.npmrc` once and both local + CI follow.
  - Closes the only remaining `pnpm audit` failure on main. CI security job exits 0.
  - **Locked decisions added to DECISIONS_LOG.md**: new "Unfixed CVE acceptance — nodemailer GHSA-rcmh-qjqh-p98v" entry. Risk accepted with email.ts JSDoc as in-code mitigation. Revisit when @auth/core widens peer range to allow nodemailer >=7.0.11.
  - Verification: `pnpm audit` exit 0 ✓ / `pnpm audit --audit-level=critical` exit 0 ✓ / eslint email.ts ✓ / tsc apps/web ✓. TDD inapplicable (pure doc + config policy change, no behavior to RED→GREEN). Two-stage review (Rule 25): Stage 1 spec PASS, Stage 2 quality PASS.
  - Tier 1 direct Opus 4.7, ~15K context. 3 files touched (1 new + 2 modified), all in mitigation blast radius.
  - Lesson added: 1 new 🟤 decision entry `[[nodemailer-cve-mitigation]]` codifying the documented-acceptance pattern as the framework's default response to unfixed HIGH CVEs in transitively-pinned deps.

- ✅ Phase 7 Feature Update — Socket.IO realtime foundation (2026-05-16, SHAs `dd57c9c` + `c399b43` + e-3 governance)
  - **Sub-session (e)-1 [SHA `dd57c9c`]** — bootstrap + auth middleware:
    - `inputs.yml` — `ports.dev.socket = 43515` (base+13, between worker at +11 and prisma_studio at +20)
    - `.env.example` + `.env.dev` — `SOCKET_PORT=43515` and `NEXT_PUBLIC_SOCKET_URL=http://localhost:43515`
    - `apps/web/src/env.ts` — `SOCKET_PORT` added to serverSchema + getServerEnv (defaults to 0 = disabled listener when unset, so build + typecheck with `SKIP_ENV_VALIDATION` work uninterrupted); `NEXT_PUBLIC_SOCKET_URL` added to clientSchema + getClientEnv
    - NEW `apps/web/src/instrumentation.ts` — Next.js 15 `register()` hook gated on `NEXT_RUNTIME=nodejs` + `!SKIP_ENV_VALIDATION` + `SOCKET_PORT>0`. Dynamic imports of `http`, `socket.io`, and `@/server/socket/*` keep the file Edge-bundle-safe (Edge runtime evaluates the file but Node-only modules never load there). SIGTERM/SIGINT clean shutdown: stops revalidation loop → io.close() → httpServer.close() → process.exit(0)
    - NEW `apps/web/src/server/socket/server.ts` — `createSocketServer(httpServer)` factory. CORS to `NEXT_PUBLIC_APP_URL` with `credentials: true` (separate ports = separate origins, browser needs explicit allowance to send the Auth.js session cookie). Installs `socketAuthMiddleware` on `io.use()` so EVERY handshake is verified
    - NEW `apps/web/src/server/socket/auth.ts` — `verifySocketAuth({cookieHeader, isProduction})` pure function parses Auth.js v5 cookie (`authjs.session-token` dev / `__Secure-authjs.session-token` prod) and decodes with cookie name as JWE salt — Auth.js v5 derives the AEAD key from `secret + cookie-name` (KEY GOTCHA: wrong salt returns null even with right secret; prevents cross-environment token replay). Narrows decoded JWT mirroring auth.config.ts session callback; rejects on missing `organizationSlug` / `organizationId` / `role` / `securityVersion`. `socketAuthMiddleware` wraps the pure function for `io.use()`
    - NEW `apps/web/src/server/socket/auth.test.ts` — 13 cases: null/empty cookie → null; dev vs prod cookie name selection; decode-null → null; missing required fields → null; bad role → null; accepts all 3 valid roles (tenant_admin, host, participant); defaults isSuperAdmin to false when missing; trims whitespace in cookie pairs. RED→GREEN proven inline.
  - **Sub-session (e)-2 [SHA `c399b43`]** — org channels + 60s revalidation:
    - NEW `apps/web/src/server/socket/channels.ts` — `orgChannelName(organizationId, eventType)` and `platformChannelName(eventType)` formatters per DECISIONS_LOG.md:173 (`${tenantId}:${eventType}`; tenant = organization in Yelli's schema). `joinOrgChannel(socket, eventType)` takes NO orgId parameter so cross-org subscription via API surface is impossible — the helper always reads `socket.data.session.organizationId`, so a malicious client cannot coerce subscription to another org's room through the API. `joinPlatformChannel(socket, eventType)` is the parallel surface for super-admin-only events; requires `session.isSuperAdmin === true`. `emitToOrg` + `emitToPlatform` complete the symmetry
    - NEW `apps/web/src/server/socket/revalidation.ts` — `revalidateConnectedSockets({io, prisma?})` walks `io.fetchSockets()`, dedupes userIds, runs a single `platformPrisma.user.findMany` with `include: { organization: { select: { suspended_at: true } } }`, disconnects sockets that fail any of: missing user / `status != "active"` / `organization.suspended_at !== null` / `security_version` mismatch. Emits `"session:invalidated"` BEFORE disconnect so clients can trigger re-auth UX. `startSessionRevalidationLoop(io, intervalMs=60_000)` is the setInterval wrapper with `.unref()` so it doesn't keep the Node process alive past Next.js's own lifecycle
    - NEW `apps/web/src/server/socket/channels.test.ts` — 10 cases: name composition; joinOrgChannel happy path + sessionless rejection + cross-org coercion-attack-surface guard; joinPlatformChannel super-admin acceptance + non-super-admin rejection + sessionless rejection; emitToOrg + emitToPlatform target the correct room
    - NEW `apps/web/src/server/socket/revalidation.test.ts` — 9 cases: 0 sockets short-circuit; valid session preserved; security_version bump → disconnect + emit `"session:invalidated"`; user.status != active → disconnect; organization suspended → disconnect; missing user → disconnect; sessionless socket cleanup; N-socket-per-user query dedupe (1 findMany regardless of socket count); mixed valid/invalid subset disconnects only the bad ones
    - MODIFIED `apps/web/src/instrumentation.ts` — wires `startSessionRevalidationLoop` after `createSocketServer`; SIGTERM/SIGINT shutdown calls the returned cleanup before `io.close()` so a stale revalidation tick can't trigger during graceful shutdown
  - **Sub-session (e)-3 [governance batch — this commit]** — STATE.md / CHANGELOG_AI.md / IMPLEMENTATION_MAP.md / `docs/DECISIONS_LOG.md` (new "Realtime Hosting Topology (Phase 7 #8e)" entry locking option B) / lessons.md (3 new entries) / .whatsnext / agent-log.md.
  - **Decomposition rationale**: per memory-governance §1, file count 12 × 2.5 + module count 2 × 5 + dependency depth 3 × 3 = 49, above the 40 threshold → Tier 3 → mandatory split. Per Phase 7 #7's [[sonnet-thrash-sessionstart-hooks]] precedent, Sonnet dispatch skipped — all 3 sub-sessions Opus self-executed (Step 2.5b). Total ~90K Opus context across the session, well under 100K ceiling.
  - **User-confirmed decisions**: (1) hosting topology = option B (instrumentation.ts + separate port) over (A) custom server.ts or (C) separate compose service; (2) decomposition = 2 sub-sessions + governance over 1 or 3.
  - **Locked decisions added to DECISIONS_LOG.md**: new "Realtime Hosting Topology (Phase 7 #8e)" entry consolidates the option B choice, the SOCKET_PORT=43515 dev port, the CORS + credentials:true cross-origin pattern, the cookie-name-as-salt Auth.js v5 convention, and the joinOrgChannel API-surface guard pattern. Locked 2026-05-16.
  - Test suite: 42 → 75 (+33). All GREEN.
  - Coverage: `trpc/routers/auth.ts` per-file gate intact at 100/80.95/100/100; new `server/socket/auth.ts` at 78.94/84.21/66.66/79.41; new `server/socket/revalidation.ts` at 74.28/64.7/50/78.12; new `server/socket/channels.ts` at near-100% (not shown individually in v8 reporter rollup); server/socket aggregate at 75.53/77.04/60/76.47. Globals: 21.37→29.43 stmts / 16.1→25.06 branches / 17.7→25 funcs / 21.49→29.2 lines.
  - Build: 27 routes unchanged (Socket.IO is server-only, no new pages).
  - Phase 5 validation: lint ✓ typecheck ✓ test ✓ test:coverage ✓; `pnpm audit` still fails on pre-existing nodemailer CVE (flagged as Phase 7 #9 candidate j, recommended next).
  - Two-stage review (Rule 25): Stage 1 spec PASS (all 4 declared behaviors delivered: server init, auth middleware, room namespace, tests; plus locked-decision honoring on heartbeat 30s / revalidation 60s / channel naming); Stage 2 quality PASS (no `any`, narrowing mirrors existing patterns, cross-org guard is API-surface-shape not runtime check — correct-by-construction; eslint disables only where intentional with rationale comments).
  - Lessons added: 3 new entries — 🟤 Auth.js v5 JWE cookie-name-as-salt gotcha; 🟤 cross-org subscription guard via API-surface design (correct-by-construction); 🟤 Next.js 15 instrumentation.ts hosting topology decision.

- ✅ Phase 7 Feature Update — JWT org-slug encoding + middleware URL↔session cross-check (2026-05-16, SHAs `0e0a892` + `740cdd2` + c-3 governance)
  - **Sub-session (c)-1 [SHA `0e0a892`]** — JWT + session shape:
    - `apps/web/src/types/next-auth.d.ts` — `organizationSlug: string` added to all 4 module augmentations (Session.user, User, next-auth/jwt JWT, @auth/core/jwt JWT — the last is required because next-auth v5 internals resolve JWT through @auth/core/jwt on some code paths)
    - `apps/web/src/server/auth.config.ts` — jwt callback writes `token.organizationSlug` from `user`; session callback narrows from token, includes in invalid predicate, assigns to `session.user.organizationSlug` (mirrors organizationId pattern). This is the Edge-safe callback consumed by middleware
    - `apps/web/src/server/auth.ts` — `authorize()` returns `organizationSlug: userRecord.organization.slug` (loaded via existing `include: { organization: true }`); DB-revalidating session callback extends `current.include` to pull `organization.slug` and assigns `current.organization.slug` to `session.user.organizationSlug` — freshness over token. Org slug renames invalidate stale tokens within one session-read cycle on the Node side. The Edge variant in auth.config.ts trusts the token (rotation handles staleness)
    - `apps/web/src/server/trpc/routers/meetings.test.ts` — SESSION_USER fixture +1 line (organizationSlug: "tenant-org") to satisfy newly-required User field
    - NEW `apps/web/src/server/auth-config.test.ts` — 7 cases exercising authConfig.callbacks.{jwt,session} directly: 3 jwt cases (write from user, organizationId regression guard, pass-through with no user); 4 session cases (copies slug, invalid-when-slug-missing, invalid-when-orgId-missing regression guard, organizationId regression guard). RED→GREEN proven inline.
  - **Sub-session (c)-2 [SHA `740cdd2`]** — middleware enforcement:
    - NEW `apps/web/src/server/tenant-redirect.ts` — two pure helpers extracted from middleware so Node-based vitest can import them without pulling next-auth + next/server (which the Edge bundle requires): (1) `resolveTenantRedirect({urlSlug, sessionSlug, isSuperAdmin, path})` returns discriminated union `{kind:"allow"} | {kind:"redirect", targetSlug}`; option C super-admin bypass codified as exact `path === "/superadmin" || path.startsWith("/superadmin/")` (NOT a `startsWith("/superadmin")` that would also match `/superadminer/*`). (2) `buildTenantRedirectUrl({currentHostname, currentPath, currentSearch, currentUrlSlug, targetSlug, origin})` returns a `URL`; detects subdomain pattern via ≥4 dot-separated parts with first label matching current slug, falls back to `/t/{slug}` path swap with regex-escaped slug to prevent injection
    - NEW `apps/web/src/server/tenant-redirect.test.ts` — 13 cases: 8 for resolveTenantRedirect (no-slug, match, mismatch non-super, super on /superadmin/users, super on /superadmin exact, super on /superadminer/foo defensive, super on /app/foo redirect, null sessionSlug defensive) + 5 for buildTenantRedirectUrl (prod subdomain swap, staging subdomain swap, /t/{slug} swap, /t/{slug} exact, port preservation)
    - `apps/web/src/middleware.ts` — imports the helpers; on `(isProtected && session.user && tenantSlug)`, runs the decision; if `redirect`, calls `buildTenantRedirectUrl` and returns `NextResponse.redirect(url)`. Adds `x-organization-slug` request header alongside `x-organization-id` for tRPC defense-in-depth (session-derived, post-cross-check guaranteed to match URL slug when both are present). Drops the defensive `if (session.user.organizationId)` guard — post c-1 the type system guarantees presence (invalid predicate filters earlier)
  - **Sub-session (c)-3 [governance batch — this commit]** — STATE.md / CHANGELOG_AI.md / IMPLEMENTATION_MAP.md / lessons.md (4 new entries) / agent-log.md / .whatsnext.
  - **Decomposition rationale**: per memory-governance §1, file count 8 × 2.5 + module count 4 × 5 + dependency depth 2 × 3 = 46, above the 40 threshold → Tier 3 → mandatory split. Per memory-governance §4, sub-sessions ≤30K Sonnet budget each. First Sonnet dispatch on c-1 thrashed within 3 turns (SessionStart hooks auto-load CLAUDE.md + .claude/rules/* into the subagent context, exhausting Sonnet's 60K window before task work begins). Pivoted to Step 2.5b Opus self-execution for all 3 sub-sessions; total context ~50K across the session, well under Opus's 100K Step 2.5b ceiling. Pattern documented in lessons.md 🟤 entry.
  - **User-confirmed decisions**: (1) field name `organizationSlug` (camelCase TS, snake_case `organization_id` in DB — matches existing convention); (2) type is non-nullable `string` (every user row has organization_id via auth.ts:95); (3) freshness on Node side via DB re-fetch of current.organization.slug, Edge side trusts token; (4) super-admin behavior = option C (path-scoped /superadmin bypass; all other paths enforce match for super-admins too).
  - Test suite: 22 → 42 (+20). All GREEN.
  - Coverage: trpc/routers/auth.ts per-file gate intact at 100/80.95/100/100 (≥75 with 5.95% slack). Globals improved: statements 13.25→21.37 (115/538), branches 6.94→16.1 (57/354), functions 12.9→17.7 (17/96), lines 13.62→21.49 (112/521). New files covered: auth-config.test.ts exercises auth.config.ts to 100/73.91/100/100; tenant-redirect.test.ts exercises tenant-redirect.ts to ~100% (not individually shown in v8 reporter rollup but aggregate server-section coverage rose 35.52→48.95 statements).
  - Build: 27 routes, unchanged from Phase 7 #6 (middleware + JWT shape only; no new routes).
  - Phase 5 validation: lint ✓ typecheck ✓ test ✓ test:coverage ✓ build pending re-verify ✓ check-product-sync pending re-verify ✓; `pnpm audit --audit-level=high` still fails on pre-existing nodemailer CVE (NOT introduced by this branch — flagged as Phase 7 #8 candidate j).
  - Two-stage review (Rule 25): Stage 1 spec PASS (all three .whatsnext-declared behaviors delivered + super-admin option C as user-confirmed), Stage 2 quality PASS (no `any`, narrowing mirrors existing pattern, regex-escape on slug interpolation, discriminated union return type, defensive null guard, pure-helper extraction for Edge/Node test co-compat).
  - Lessons added: 4 new entries — 🟤 Sonnet-thrash-on-SessionStart-hooks → default to Opus executor; 🟤 edge-safe vs DB-revalidating session callback duplication contract; 🟤 super-admin option C decision (path-scoped /superadmin bypass); ⚖️ vercel-plugin proxy.ts auto-suggestion is a false positive on Next.js 15 — ignore until Next.js 16 upgrade.

- ✅ Phase 7 Feature Update — per-user 24h reset-request cap (2026-05-16, SHA `5325b8c`)
  - `apps/web/src/server/trpc/routers/auth.ts` — added 2 module constants (`PER_USER_RESET_CAP=5`, `PER_USER_RESET_WINDOW_MS=24h`) and a `passwordResetToken.count` query with a 24h gte where clause + early-return on cap-met. Placement is AFTER the user lookup so unknown emails skip the cap query entirely; preserves no-enumeration because all three paths (unknown email / known under cap / known at cap) return identical `{ok:true}` shape with similar latency profile.
  - `apps/web/src/server/trpc/routers/auth.test.ts` — added `count: vi.fn()` to the mock factory; updated existing happy-path test to mock `count=0`; added 2 new cases: cap-engaged (count=5 → ok, no create, no send, count call signature asserted with `user_id` + ~24h gte window) and one-under-cap (count=4 → still mints + sends); updated unknown-email test to assert count is NOT called (extra defence on no-enumeration posture).
  - Defence-in-depth design: the per-email LRU rate limit (10/min) catches rapid-fire bursts; the per-user 24h cap catches sustained low-rate flooding (attacker spacing requests >6s apart). Without the cap, an attacker passing Turnstile could send 14k+ reset emails per day to a known user.
  - Test suite: 22/22 passing in ~344ms (was 20/20, +2 cases).
  - Coverage on auth.ts: 100/78.94/100/100 → 100/80.95/100/100 (branches improved as the new cap-met branch is exercised). Per-file gate (≥75) passes with widened slack.
  - Build: 27 routes, unchanged from Phase 7 #5 (backend-only change). 45.8s.
  - Phase 5 validation: lint ✓ typecheck ✓ test ✓ test:coverage ✓ build ✓ check-product-sync ✓; `pnpm audit --audit-level=high` still fails on pre-existing nodemailer CVE (NOT introduced by this branch — flagged as Phase 7 #7 candidate j).
  - Two-stage review (Rule 25): Stage 1 spec PASS (cap engages at request #6, returns ok, no enumeration, no email — all asserted), Stage 2 quality PASS (no `any`, only blast-radius files touched, module-scope constants match existing pattern, comment explains WHY).
  - Lessons added: 🟤 per-user 24h cap design pattern documents (1) AFTER-lookup placement vs before, (2) `count` primitive over `findFirst+orderBy`, (3) per-user.id keying vs per-email keying.

- ✅ Phase 7 Feature Update — coverage threshold gate (2026-05-16, SHA `78fc022`)
  - `apps/web/vitest.config.ts` — added `coverage.thresholds` block. Global floor: statements ≥ 12, branches ≥ 6, functions ≥ 12, lines ≥ 12 (matches measured baseline; locks against catastrophic regression like an accidentally `.skip`'d suite). Per-file gate: `src/server/trpc/routers/auth.ts` at statements 100 / branches 75 / functions 100 / lines 100 (auth router is the only fully-tested file in the codebase — partial test deletion now surfaces immediately). Added `lcov` reporter alongside text + html for future tool integration.
  - `turbo.json` — added `test:coverage` task (dependsOn `^build`, outputs `coverage/**`).
  - `package.json` (root) — added `test:coverage` script delegating to `turbo run test:coverage`.
  - `.github/workflows/ci.yml` — added `coverage` job under `needs: governance` (parallel to `quality` and `security` matrix). Runs `pnpm test:coverage` and uploads `apps/web/coverage/` as a 14-day artifact (HTML + lcov) via actions/upload-artifact@v4.
  - Gate verified: temporarily bumped auth.ts branches threshold 75 → 99 → confirmed `pnpm test:coverage` exited 1 with explicit message "Coverage for branches (78.94%) does not meet ... threshold (99%)" → restored to 75. RED→GREEN cycle proven for the gate itself.
  - Test suite: still 20/20 passing in ~459ms (vitest run) / ~3.8s (turbo run); no test changes.
  - Build: 27 routes, unchanged from Phase 7 #4 (config-only change).
  - Phase 5 validation: lint ✓ typecheck ✓ test:coverage ✓ build ✓ check-product-sync ✓; `pnpm audit --audit-level=high` fails on pre-existing nodemailer CVE (NOT introduced by this branch — confirmed identical 6/1/4/1 severity counts on main).
  - Two-stage review (Rule 25): Stage 1 spec PASS (PRODUCT.md does not declare specific thresholds; ticket scope is "add coverage gate"), Stage 2 quality PASS (4 files all in scope, no `any`, gate engagement verified inline).
  - Lessons added: see `.cline/memory/lessons.md` for the vitest per-file glob threshold pattern + the pre-existing nodemailer CVE follow-up note.

- ✅ Phase 7 Feature Update — /app/meetings/new tests + RHF/Zod form polish (2026-05-15, SHA `8709595`)
  - `apps/web/src/server/trpc/routers/meetings.test.ts` — 7 cases against `meetingsRouter.create`: happy path verifies server stamps `organization_id` from ctx + `host_user_id` from ctx + `status="scheduled"` + crypto.randomUUID for `meeting_link_token` and `livekit_room_name`; minimal-input defaults (recording=false, lobby=false, description=null, scheduled_at=null); Zod rejects empty title + title >300; Zod rejects description >2000; strict() rejects unknown field (proves client can't override server-stamped `status`); UNAUTHORIZED on null session with no rate-limit fired
  - `packages/shared/src/schemas/meeting.ts` — new `MeetingCreateClientInputSchema` (5 client-facing fields: title, description, scheduled_at, recording_enabled, lobby_enabled). Distinct from pre-existing `MeetingCreateInputSchema` (entity projection — admin/internal). Pattern locks the boundary: `<Entity>CreateClientInputSchema` for client input, `<Entity>CreateInputSchema` for projections where server-stamped fields are visible.
  - `apps/web/src/server/trpc/routers/meetings.ts` — refactored `create` to import `MeetingCreateClientInputSchema` from `@yelli/shared` (replaced inline `z.object`). Behavior equivalence proven by the 7 tests added in the same commit.
  - `apps/web/src/app/app/meetings/new/_meeting-form.tsx` — ported from useState chain to react-hook-form + zodResolver + shadcn Form primitives. Local `formSchema` covers the datetime-local string input; `onSubmit` transforms to wire shape before invoking `trpc.meetings.create.useMutation`. All existing UX preserved: title required + max-300, optional description + max-2000, optional datetime-local scheduled_at, recording/lobby toggles, success→/app/meeting/[id], cancel→/app/meetings, toast feedback, disabled-during-pending.
  - `packages/ui/src/components/form.tsx` — shadcn/ui Form primitive installed (Form/FormField/FormItem/FormLabel/FormControl/FormDescription/FormMessage + useFormField hook). Closes UI Rule #4 gap. Future forms throughout apps/web `import { Form, FormField, ... } from '@yelli/ui'`.
  - `packages/ui/{package.json,src/index.ts}` — added `./form` subpath export, `react-hook-form` peerDependency (apps/web already had RHF 7.53.2 as direct dep), and barrel re-exports.
  - Visual QA (Rule 16) deferred — auth-gated route requires login credentials the agent cannot read; manual smoke pending next dev-up session.
  - Test suite: 12/12 passing in ~411ms (5 auth + 7 meetings).
  - Build: 26 routes, `/app/meetings/new` 4.46 kB / 218 kB first-load (RHF + zodResolver bundled into this route now), middleware unchanged at 99.1 kB, 53.4s.
  - Two-stage review (Rule 25): Stage 1 spec PASS, Stage 2 quality PASS (no `any`, 2 documented `as never` casts per lessons.md, 8 files all in blast radius).
  - 4 new lessons captured: shared schema split (client-input vs entity-projection); namespace-import + @typescript-eslint/consistent-type-imports gotcha; vi.mocked Prisma mockImplementation `as never` on the function (extends [[trpc-test-pattern]]); shadcn Form primitive scoped to @yelli/ui.

- ✅ Phase 7 Feature Update — vitest infrastructure + auth.register smoke coverage (2026-05-15, SHA `81c0279`)
  - `apps/web/vitest.config.ts` — vitest 4 with native `resolve.tsconfigPaths: true`, `environment: "node"`, `SKIP_ENV_VALIDATION=1` in `test.env`, v8 coverage provider
  - `apps/web/src/server/trpc/routers/auth.test.ts` — 5 cases: happy path returns `{ ok: true, slug }` and lowercases email for rate-limit key; rate-limit throws before turnstile (early-exit verified); turnstile fail → UNAUTHORIZED with no DB write; slug conflict → CONFLICT with no $transaction; Zod rejects weak password + uppercase slug before rate-limit fires
  - `apps/web/package.json` — added `test` / `test:watch` / `test:coverage` scripts; turbo `test` pipeline now executes for `@yelli/web`
  - Mock pattern (reusable for future tRPC router tests): `vi.mock("@yelli/db")` for platformPrisma surface; `vi.mock("@/server/lib/turnstile")` + `vi.mock("@/server/lib/rate-limit")` for sibling deps; `vi.mock("bcryptjs")` with both `default` and named exports; real Zod via `@yelli/shared/schemas`; `createCallerFactory(authRouter)` + fake `Request` context
  - Closes Rule 25 TDD deferral from Phase 7 #1; all subsequent Phase 7 tickets must write failing test first
  - Test suite: 5/5 passing in ~600ms cold start
  - Two-stage review (Rule 25): Stage 1 spec PASS (closes the deferred test ordering), Stage 2 quality PASS (typecheck + lint green)

- ✅ Phase 7 Feature Update — auth.register tRPC procedure (2026-05-15, SHA `ce709ff`)
  - `apps/web/src/server/trpc/routers/auth.ts` — new `authRouter` with `register` publicProcedure
  - `packages/shared/src/schemas/auth.ts` — new `registerInputSchema` (Zod, includes turnstileToken)
  - Wired `apps/web/src/app/(auth)/register/page.tsx` to call `trpc.auth.register.useMutation`; on success → toast + `router.push('/login?org=' + slug)`; on error → toast + reset captcha
  - Flow: rate-limit (auth tier, keyed on `register:${email}`) → Turnstile siteverify → slug pre-check → bcrypt cost 12 → atomic `platformPrisma.$transaction` creating Organization (billing_email = registrant) + tenant_admin User
  - Exposed `createCallerFactory` on tRPC base (for future tests)
  - Fixed `.js` extension leftovers in `packages/shared/src/{index,schemas/index,types/index,schemas/subscription}.ts` (same pattern as the prior storage scaffold-bug — surfaced by the first real import of `@yelli/shared/schemas` from a route)
  - **Deferred**: vitest not installed → no test file shipped. Next Phase 7 ticket should install vitest + write `auth.register` coverage.
  - Two-stage review (Rule 25): Stage 1 spec PASS, Stage 2 quality PASS (with test deferral)

- ✅ Phase 0 Bootstrap (2026-05-11)
  - Folder structure, governance docs, MCP wiring, Phase 4 task files (8 parts), CREDENTIALS.md with ⏳ placeholders.

- ✅ Phase 2.5 Spec Summary (2026-05-11) — confirmed by human.

- ✅ Phase 2.6 Design System (2026-05-11) — SKIPPED (no UI UX Pro Max skill, no Section K in PRODUCT.md). docs/DESIGN.md present as fallback visual reference per Scenario 33.

- ✅ Phase 2.7 Spec Stress-Test (2026-05-11) — PASSED, 0 gaps.

- ✅ Phase 3 Spec File Generation (2026-05-11)
  - inputs.yml (v3) written — 13 entities, 13 modules, 6 roles, 4 job queues
  - inputs.schema.json written — JSON Schema validation
  - .env.dev / .env.staging / .env.prod written with AI-generated credentials (22-char passwords + 48-char auth secrets)
  - .env.example written (placeholders only — safe to commit)
  - scripts/sync-credentials-to-env.sh written + executable
  - DECISIONS_LOG locked: tenancy, tech stack, Docker publish, Komodo+Traefik V27 deploy, Xendit, Turnstile, WCAG AA, dev port base 43502, LiveKit/Coturn/Socket.IO
  - All AI-producible secrets generated; ⏳ placeholders remain for: GitHub PAT, Docker Hub token, SMTP, Komodo URL, Xendit keys, Turnstile LIVE keys, LiveKit keys, Coturn static-auth-secret

- ✅ Phase 3.5 Execution Plan (2026-05-11) — .cline/tasks/execution-plan.md generated, 14-session decomposition (Part 1×1, Part 2×1, Part 3×4, Part 4×2, Part 5×5, Part 6 skipped, Part 7×2, Part 8×1).

- ✅ Phase 4 Part 1 — Root config (2026-05-11)
  - pnpm-workspace.yaml + turbo.json + tsconfig.base.json + .editorconfig + .prettierrc + .eslintrc.js + .gitignore + .nvmrc + package.json + pnpm-lock.yaml
  - Branch scaffold/part-1 → squash-merged to main

- ✅ Phase 4 Part 2 — packages/shared + packages/api-client (2026-05-12)
  - 13 entity Zod schemas + inferred TS types + Create/Update inputs (organization, user, department, meeting, callLog, participant, chatMessage, recording, sharedFile, whiteboardSnapshot, subscription, invoice, platformSettings)
  - Generic typed tRPC v11 client wrapper (createApiClient<TRouter> + httpBatchLink + superjson)
  - Branch scaffold/part-2 → squash-merged to main

- ✅ Phase 4 Part 3 — packages/db (2026-05-13)
  - 14-model Prisma schema with L6 denormalization (organization_id on meeting-scoped children)
  - L6 tenant-guard via `Prisma.defineExtension` `$allOperations` (injects organization_id into where + data; exempts AuditLog/Organization/PlatformSettings; super-admin bypass via ALS)
  - L5 writeAuditLog with Prisma.JsonNull handling
  - L2 RLS commented in migration (activates on multi-tenant SaaS deploy)
  - AsyncLocalStorage tenant context + super-admin platformPrisma (separate unguarded client)
  - Initial migration up + down + webmaster super-admin seed (bcrypt cost 12, idempotent upsert)
  - Branch scaffold/part-3 → squash-merged to main

- ✅ Phase 4 Part 4 — packages/ui + packages/jobs + packages/storage (2026-05-13)
  - packages/ui: shadcn/ui New York workspace (9 base components + Tailwind tokens from DESIGN.md + globals.css with HSL CSS vars + Recharts chart-1..5 colors added in Part 5e)
  - packages/jobs: 4 typed BullMQ queues (recording-processing, report-generation, usage-calculation, billing-cycle) — IORedis singleton + TenantJobBase validator + registerCronJobs helper
  - packages/storage: AWS SDK v3 S3-compatible wrapper — buildStorageKey + verifyKeyOwnership + MIME blocklist (SVG/HTML/JS rejected) + 100 MB cap + presigned URLs
  - Branch scaffold/part-4 → squash-merged to main

- ✅ Phase 4 Part 5a — apps/web shell (2026-05-13)
  - 9 config files (next.config.ts with 7 security headers + Turnstile/LiveKit CSP, postcss, tailwind extending @yelli/ui, components.json, eslint, etc.)
  - 7 server core files (env.ts Zod validation, auth.ts with bcrypt + securityVersion staleness check, rate-limit.ts LRU 5-tier, sanitize.ts DOMPurify, turnstile.ts siteverify)
  - 5 routing + theme files (middleware.ts tenant + auth gate, layout.tsx Inter + ThemeProvider, theme-provider, turnstile-widget)
  - 6 auth pages (layout, form-card, login, register, forgot-password, join/[token])
  - Branch scaffold/part-5 → squash-merged to main (Architect-Execute: 4 Sonnet dispatches)

- ✅ Phase 4 Part 5b — Speed Dial Board + 1:1 Video Call UI (2026-05-13)
  - 6 Speed Dial Board files (adaptive grid 1/2 → 2/3/4/5 by count, 88/120/140 min-h tap targets, presence dot, Auto badge, Socket.IO presence subscription + 30s heartbeat)
  - 8 Video Call UI files (server-only HS256 JWT minter, LiveKit room hook, /app/call/[id] page, IntercomCall + CallControls + IncomingCallDialog with Web Audio API ringtone, /api/livekit/token Route Handler with manual auth + rate-limit)
  - Branch scaffold/part-5b → squash-merged to main

- ✅ Phase 4 Part 5c — tRPC server + call initiation + Socket.IO skeleton (2026-05-13)
  - 8 tRPC v11 files (trpc.ts with auth+tenant+rateLimit middleware chain, context, router, departments router, /api/trpc Route Handler, TRPCReactProvider, createServerCaller for RSC)
  - 3 Socket.IO skeleton files (types, globalThis-cached io singleton, 503 stub Route Handler — custom Next.js server needed for WS upgrade)
  - calls.ts initiate mutation (departments.findUnique L6-scoped + mintLiveKitToken + emitIncomingCall via io)
  - Branch scaffold/part-5c → squash-merged to main

- ✅ Phase 4 Part 5d — Meeting Management UI + multi-participant LiveKit + CallLog persistence (2026-05-13)
  - 9 new files (meetings.ts router with list/byId/create/getJoinToken/end + status gating + idempotent CallLog write; call-log.ts helpers; /app/meetings list + new + meeting/[id] RSC; MeetingRoom + MeetingControls + useMeetingRoom)
  - calls.ts: added end mutation (intercom CallLog write); speed-dial-grid.tsx wired to trpc.calls.initiate
  - authMiddleware refactored to inline chain (eliminates 4 advisory non-null-assertion warnings)
  - Architect-Execute with Step 2.5b Opus escalation after Sonnet 5d-1 thrashed (3 bug classes: wrong Prisma relation names, wrong field aliases, bogus server-only import, wrong path /app/meetings/[id] vs /app/meeting/[id])
  - 3 new lessons.md entries: 🔴 Sonnet 30K budget silently exceeded; 🟤 tRPC v11 standalone middleware ctx narrowing; 🟤 Prisma strict create + L6 cast pattern
  - Branch scaffold/part-5d → squash-merged to main as ec50f4f

- ✅ Phase 4 Part 5e — Admin pages + Super-admin pages (2026-05-14)
  - 16 new source files: 3 backend routers (admin with dashboard.stats/users/settings/reports, billing with Xendit 503-graceful checkout, superadmin with platformPrisma + securityVersion bump on suspend), 8 admin UI pages, 3 super-admin pages + 3 UI primitives (badge, alert, table)
  - adminProcedure + superAdminProcedure tRPC chains (super-admin deliberately skips runWithTenantContext — uses platformPrisma exclusively)
  - departments.ts extended with create/update/delete/csvImport/regenerateDeviceToken (all in $transaction with writeAuditLog)
  - writeAuditLog parameter widened from Prisma.TransactionClient to structural AuditLogWriter (accepts L6-extended client tx)
  - Direct Opus implementation (Step 2.5b) — 3 commit bundles on scaffold/part-5e, each verify clean
  - 3 new lessons.md entries: 🟤 AuditLogWriter structural type; 🟤 superAdminProcedure tenant bypass discipline; 🟤 Xendit 503 graceful degradation pattern
  - Branch scaffold/part-5e → squash-merged to main

- ✅ Phase 4 Part 7 — tools/ + deploy/compose/ + Dockerfile + push.sh + COMMANDS.md (2026-05-14)
  - Tools workspace (6): validate-inputs (Ajv 2020-12), check-env (DEV_ONLY_KEYS allowlist), check-product-sync (normalize() strips `[_\-&/,()[].:]` + private-tag scan), hydration-lint (SERVER_ONLY_PATH_SEGMENTS allowlist — 66 files, 0 false positives)
  - Deploy/compose (22 files): dev (db/cache/storage/infra/media/pgadmin/app + pgadmin-servers.json — 8), stage (db/cache/storage/media/pgadmin/app + servers — 7), prod (db/cache/storage/media/pgadmin/app + servers — 7); all share COMPOSE_PROJECT_NAME=yelli_<env> + ../../../.env.<env> + named volumes; LiveKit dev `--dev` single UDP port vs stage/prod `--rtc-port-range-start/end=7882-7892`; Coturn UDP relay 49160-49200; Traefik wss labels for signal in stage/prod
  - start.sh (env-aware compose dispatcher; --build on dev `up`), push.sh (manual image promotion dev→staging→prod)
  - apps/web/Dockerfile (multi-stage pnpm-workspace-aware: deps copies all package.json before `pnpm install` for layer cache; builder runs `pnpm --filter @yelli/db prisma generate` then `pnpm --filter @yelli/web... build`; runner is node:22-alpine standalone, non-root nodejs:1001)
  - apps/web/.dockerignore (excludes node_modules + .next + .turbo + .env* + CREDENTIALS.md + .cline + .specstory + design-system + deploy/compose + docs + tests)
  - deploy/k8s-scaffold/README.md (INACTIVE placeholder per Rule 6)
  - .socraticodecontextartifacts.json (4 entries: database-schema, implementation-map, decisions-log, product-definition; gitignored, machine-local)
  - COMMANDS.md (project-root master command reference)
  - Direct Opus implementation (Step 2.5b) — single scaffold/part-7 branch with one atomic squash-merge; 32 new + 4 modified files
  - 5 new lessons.md entries: 🟤 Compose env_file path = ../../../.env.<env>; 🟤 LiveKit dev `--dev` single UDP vs stage/prod explicit range; 🟤 check-env DEV_ONLY_KEYS allowlist; 🟤 check-product-sync normalize() connector chars; 🟤 hydration-lint SERVER_ONLY_PATH_SEGMENTS
  - Branch scaffold/part-7 → squash-merged to main

- ✅ Phase 4 Part 5f — Feature surface: call history + recordings library + chat history + in-call overlays (2026-05-14)
  - 12 new source files in 3 sequential bundles within one Opus session:
    - Bundle A backend (3 new + 1 wired): recordings.ts (list paginated + getDownloadUrl 1h pre-signed + softDelete transactional + writeAuditLog L5; verifyKeyOwnership; NOT_FOUND on tenant mismatch); chat.ts (listByMeeting chronological 200/500 max + send with sanitizePlainText XSS guard + blocks on cancelled/ended); calls.ts append listHistory (CallLog with caller/department/meeting includes, optional type filter); router.ts register
    - Bundle B pages (3 + 1 helper): /app/history (RSC, status badges, type chips, duration formatter), /app/recordings (RSC, BigInt-as-string formatter, download button is client island), /app/chat/[id] (RSC, meetings.byId for title + chat.listByMeeting, NOT_FOUND → notFound() for cross-tenant), recording-download-button.tsx (client island opens signed URL in new tab)
    - Bundle C overlays (4 new + 2 modified): InCallRecordingIndicator (pulsing red badge driven by recording_enabled prop), InCallChat (right-aside, 3s polling refetchInterval, send mutation invalidates query), InCallFileDropzone (Dialog + HTML5 dnd + 10MB cap + file_url=pending:// placeholder), InCallWhiteboard (Dialog + HTML5 canvas pointer-event drawing, local-only); MeetingRoom wired with 4 overlay toggles + recording_enabled prop; meeting page extracts and passes recording_enabled
  - 2 errors caught early by per-bundle verify: FAILED_PRECONDITION→PRECONDITION_FAILED (correct tRPC v11 identifier); calls.listHistory args-type widening erased select literal-narrowing → fixed via conditional spread `...(input?.type ? { where } : {})`
  - No new lessons.md entries (all patterns extend existing 5d/5e/7 patterns)
  - Direct Opus implementation (Step 2.5b — same as 5d/5e/7); Tier 3 score 51.5; total Opus context ~60K
  - Branch scaffold/part-5f → squash-merged to main as ae2f2bc

- ✅ Phase 4 Part 8 — CI workflows + MANIFEST.txt + README.md + final IMPLEMENTATION_MAP rewrite (2026-05-14)
  - 5 new files:
    - **.github/workflows/ci.yml** — three jobs: `governance` (pnpm install → tools:validate-inputs → tools:check-product-sync → tools:hydration-lint; `tools:check-env` deliberately excluded as it validates local .env files, CI uses GitHub Secrets), `quality` (Turbo matrix lint/typecheck/test/build with per-task `.turbo` cache key on ref+sha), `security` (`pnpm audit --audit-level=high` — blocks merge on HIGH/CRITICAL CVEs). Uses `pnpm/action-setup@v4` with version pinned to PNPM_VERSION=10.0.0 + `actions/setup-node@v4` with `cache: pnpm` (canonical 2026 pattern — avoids corepack permission issues on root CI runners). Concurrency group cancels stale runs on the same ref. `fetch-depth: 0` for git-history-aware Turbo cache. `fail-fast: false` so all four quality tasks always report.
    - **.github/workflows/docker-publish.yml** — V27 deployment model. Triggers on push to main + workflow_dispatch. Multi-platform build (linux/amd64 + linux/arm64) via QEMU + Buildx. Pushes three tags per run: `:latest` (Komodo prod manual Deploy), `:staging-latest` (Komodo staging auto_update polls Docker Hub digest), `:sha-<short>` (immutable per-commit, rollback target). Image identity hardcoded to `${{ secrets.DOCKERHUB_USERNAME }}/yelli` — DOCKER_IMAGE_NAME repository variable not needed since image name is locked in inputs.yml. GHA cache mode=max for layer reuse. `provenance: false` to avoid OCI attestation manifests that some downstream Komodo configurations stumble on (can re-enable once verified). No Komodo webhook step — V27 Komodo polls Docker Hub directly.
    - **MANIFEST.txt** — full file inventory by Phase 4 Part. ~255 tracked + 5 [LOCAL] gitignored. Each entry tagged [GIT]/[LOCAL]/[SOFT]. Includes deferred follow-ups list (Socket.IO real-time chat, file upload pipeline, whiteboard multiplayer, Egress recording state, Kibo dropzone, mid-call moderator features, test harness).
    - **README.md** — project-root developer onboarding. Quick-start (5 commands), service URLs (with non-standard dev port table), daily commands, Phase 7 feature-update loop, architecture table, security stack table (L1–L6 with state per layer), codebase intelligence (SocratiCode/Context7/shadcn MCP), SpecStory note, project structure tree, phase status, Phase-5 credential checklist (8 ⏳ items grouped by service), license + ownership notes (shadcn MIT, Kibo MIT, lucide ISC, Recharts MIT, Prisma Apache 2.0, DESIGN.md from awesome-design-md MIT).
    - **docs/IMPLEMENTATION_MAP.md** — this file, rewritten as final Phase-4-complete snapshot.
  - Files modified: .cline/STATE.md (PHASE=Phase 4 Part 8 complete, NEXT=Phase 5 Validation), docs/CHANGELOG_AI.md (Part 8 entry appended), .cline/memory/agent-log.md (Part 8 entries appended)
  - Schema/migrations: none (Part 8 is governance + CI, no source-code changes)
  - Verification: pnpm tools:validate-inputs PASS, pnpm tools:check-product-sync PASS, pnpm tools:hydration-lint PASS. Workflow YAML manually inspected (uses-clauses pinned to v4/v5/v6 majors; no untrusted-input injection — all `run:` lines reference only env vars and matrix values).
  - Hook interaction note: PreToolUse:Write fired security_reminder_hook (informational GHA injection-attack warning) and vercel-plugin skill suggestion (workflow + deployments-cicd + bootstrap). Vercel skills were skipped — Yelli's deployment is locked as Komodo + Traefik (DECISIONS_LOG, priority 5 > plugin skills priority 7 per Rule 28). Security warning's safe pattern is followed: no untrusted GitHub event payload (issue title, PR description, commit message) flows into `run:` shell commands.
  - SocratiCode initial index: documented as post-merge human action — SocratiCode MCP tools were not loaded in this Claude Code session (Docker required + Qdrant/Ollama containers). To trigger: ensure Docker Desktop is running, then in a Claude Code session say "Index this codebase" → invokes `codebase_index` + `codebase_status` + `codebase_context_index`.
  - Direct Opus implementation (Step 2.5b — same as 5d/5e/7/5f). Tier 2 ≤12 files / ≤80K SAFE single session. Total Opus context ~55K — well within 200K budget.
  - Branch scaffold/part-8 → squash-merged to main (commit SHA recorded in .cline/STATE.md and agent-log.md after merge).

## Not Yet Built

> Rewritten 2026-05-23 (Phase 8 Batch 1 Item 1) — supersedes the Phase-4-era Phase-5/6/7
> stub. Phases 5 + 6 + 7 are all complete. Phase 7 shipped 17 tickets (`#1`–`#17`) plus
> 2 hotfix SLOTs (turnstile-app-env-guard, incoming-call-dialog-singleton). This section
> now reflects the **module-by-module depth gap between PRODUCT.md declarations and
> shipped functionality** as of `6f9ac04` (main) + `phase8-item1-preflight` (this branch).

### Depth-status legend
- **✅ built** — PRODUCT.md feature list shipped end-to-end with tests
- **🟡 scaffolded-logic-stub** — page + tRPC router exist but business logic is mock/partial
- **⬜ not-built** — declared in PRODUCT.md but no implementation in `apps/web/src`
- **⏸ deferred** — documented gap, intentionally deferred to a later Phase 8 batch

### Module gap matrix (13 PRODUCT.md modules)

| Module                                           | Route                  | Depth | Phase 8 Batch              |
|--------------------------------------------------|------------------------|-------|----------------------------|
| Speed Dial Board (Intercom)                      | `/app`                 | ✅    | n/a (closed)               |
| Video Calling                                    | `/app/call/[id]`       | ✅    | n/a (closed)               |
| In-Call Chat                                     | `/app/chat/[id]`       | ✅    | n/a (B sub-1 closed)       |
| File Sharing (in-call overlay)                   | `/app` overlay         | 🟡    | B (quick-wins)             |
| Whiteboard (in-call overlay)                     | `/app` overlay         | 🟡    | B (quick-wins)             |
| Recording library + playback                     | `/app/recordings`      | 🟢    | B sub-3 (Egress wired; playback page pending) |
| Meeting Management (create / lobby / host ctrl)  | `/app/meetings/*`      | ✅    | n/a (closed)               |
| Department Management                            | `/admin/departments`   | ✅    | n/a (closed)               |
| User Management                                  | `/admin/users`         | ✅    | n/a (closed)               |
| Tenant Admin Dashboard analytics                 | `/admin`               | 🟡    | C (admin polish)           |
| **Billing and Subscription (SaaS) — Xendit**     | `/admin/billing`       | ✅    | n/a (A item 3 closed)      |
| **Super Admin Panel (SaaS)**                     | `/superadmin/*`        | 🟡    | C (admin polish)           |
| Reports and Export (CSV/PDF)                     | `/admin/reports`       | ✅    | n/a (B sub-2 closed)       |

### Module-by-module gap detail (🟡 + ⬜ entries)

#### In-Call Chat — ✅ depth-complete (Phase 8 Batch B sub-session 1 — shipped 2026-05-25)
- ✅ `/app/chat/[id]` post-meeting history page renders, `chat.ts` tRPC router exists, messages persist to DB (Phase 4)
- ✅ Real-time delivery via Socket.IO — replaces prior 3s `useQuery` polling. Channel scope: org-wide `${organizationId}:chat:message` (per the locked DECISIONS_LOG.md line 173 channel-naming rule); per-meeting filtering happens on the client (`meetingId` field on payload) so a single socket connection serves every meeting tab a user has open simultaneously.
- ✅ Server: `apps/web/src/server/socket/chat.ts` (`attachChatHandlers` + `CHAT_MESSAGE_EVENT` constant) joins the auth-gated socket to the org channel; wired into `server.ts` connection handler alongside the existing 3 attach calls. `chat.send` mutation emits via `getIO()` + `emitToOrg(io, ctx.organizationId, CHAT_MESSAGE_EVENT, {meetingId, message})` after `prisma.chatMessage.create` resolves. Null-io guard for tRPC-without-socket contexts (test harness + boot races).
- ✅ Client: pure handler `apps/web/src/lib/chat/chat-message-handler.ts` mirrors `user-presence-handler.ts` shape; React hook `apps/web/src/lib/chat/use-chat-messages.ts` composes `useSocketOptional()` + the pure handler; filters payloads by `meetingId` and re-uses a ref so inline `onMessage` closures do not churn subscriptions. `in-call-chat.tsx` removed `refetchInterval`, uses `useChatMessages(meetingId, handleChatMessage)` where the callback invalidates the listByMeeting query. Send-mutation onSuccess invalidate kept as defensive fallback (idempotent — TanStack dedupes concurrent fetches).
- ✅ TypedSocket: registered `"chat:message": (payload: ChatMessageEventPayload) => void` in `ServerToClientEvents` (apps/web/src/lib/socket/types.ts) so the hook compiles against the strict typed-socket contract without cast.
- ✅ Tests: 11 new cases — 8 router cases (chat.test.ts: happy-path emit shape + sanitize-before-emit + BAD_REQUEST/NOT_FOUND/FORBIDDEN-no-emit paths + getIO-null graceful degradation + listByMeeting-never-emits) + 3 pure handler cases (chat-message-handler.test.ts: callback invocation + dispose + multi-subscription independence). Suite 381 → 392.
- Cost actual: 4 new files + 4 modified files = 8 files / 11 new test cases / 0 migrations. Initial estimate Tier 2 ~6 files; pre-flight scan confirmed pattern fully scaffolded by Phase 7 realtime infra, executed as Tier 1 gap-find pivot per [[existing-surface-scan-before-scaffold]]. ~50K total session context (Opus inline solo per [[opus-inline-escalation-pattern]]).

#### File Sharing (in-call overlay) — 🟡 scaffolded-logic-stub
- ✅ `<InCallFileShare>` component exists with dropzone UI
- ⬜ Upload pipeline — currently inserts `pending://...` placeholder, no actual S3 PUT.
  Needs `storage.uploadObject({ tenantId, callId, file })` pre-signed PUT URL flow via
  `packages/storage` MinIO/S3 wrapper (per `.claude/rules/security.md` FILE UPLOAD SAFETY:
  MIME whitelist, magic-byte check, 10MB limit, tenant-scoped path prefix, randomized
  filename, SVG/HTML blocked).
- ⬜ Kibo UI dropzone swap-in: `npx shadcn add @kibo-ui/dropzone` (per Phase 4 deferred note).
- Cost: ~5 files. Tier 2.

#### Whiteboard (in-call overlay) — 🟡 scaffolded-logic-stub
- ✅ Single-user whiteboard renders, strokes captured client-side
- ⬜ Multi-user broadcast — needs `meeting:{id}:whiteboard` Socket.IO channel,
  stroke event protocol (`stroke:begin` / `stroke:point` / `stroke:end` / `clear`),
  cursor-position presence, snapshot persistence to `WhiteboardSnapshot` entity.
- Cost: ~8 files. Tier 2-3.

#### Recording library — 🟡 scaffolded-logic-stub
- ✅ `/app/recordings` page lists rows, `recordings.ts` tRPC router exists
- ⬜ LiveKit Egress wiring — needs `/api/webhooks/livekit` route handler for
  `recording:started` / `recording:stopped` / `recording:failed` events, signature
  verification, `Recording.status` state machine, `InCallRecordingIndicator` reactive
  feed via `meeting:{id}:recording` Socket.IO channel. PRODUCT.md splits this:
  Self-Hosted → local MinIO file; SaaS paid plans only → platform S3/R2.
- ⬜ Playback player — needs HLS-compatible `<video>` element with chapter markers.
- Cost: ~8-10 files. Tier 3.

#### Tenant Admin Dashboard analytics — 🟡 scaffolded-logic-stub
- ✅ `/admin` page renders (171 lines), navigation works
- ⬜ Usage analytics widgets — total calls / total minutes / active users / calls-by-dept
  chart all read from mock data. Needs `admin.usageMetrics` tRPC procedure with date-range
  filter aggregating `CallLog` + `Participant` tables (tenant-scoped via L1+L6).
- ⬜ Call logs table — currently mock rows. Needs paginated `admin.listCallLogs`
  procedure with filter (caller, recipient, type, date-range) per `.claude/rules/security.md`
  pagination + ownership-check guardrails.
- ⬜ Chat history viewer — needs `admin.listChatHistory` procedure scoped to org.
- ⬜ Export wiring — calls into Reports & Export (see below).
- Cost: ~6-8 files. Tier 2-3.

#### Billing & Subscription (SaaS) — ✅ depth-complete (Phase 8 Batch 1 Items 2 + 3 — shipped 2026-05-23 + 2026-05-25)
- ✅ `/admin/billing` page renders (241 lines + invoice list with security-hardened wire shape per 3c-i)
- ✅ Plan tier enforcement engine (Item 2 — shipped 2026-05-23): `packages/shared/plan-limits.ts` SoT (PLAN_LIMITS record + NumericPlanFeature/BooleanPlanFeature unions + helpers); `apps/web/src/server/trpc/middleware/plan-limit.ts` (enforceNumericPlanLimit/requirePlanCapability factories + assertNumericPlanLimit/assertPlanCapability inline helpers for tx-consistent counts); router gating wired into departments.create (departments + autoAnswerStations caps), departments.update (autoAnswerStations on false→true flip), admin.users.invite (users + admins sub-cap); `billing.usage.current` query returning {plan_tier, limits, usage}; UI `<PlanLimitBanner>` + `usePlanUsage` hook integrated on Speed Dial / Departments / Users pages with disabled-CTA at cap + cache invalidation on mutation success; 40 new test cases (12 middleware + 14 integration + 14 banner state); 35 files / 339 tests / typecheck 0 / lint 0.
- ✅ Xendit Invoice/Subscription creation tRPC mutations + checkout redirect flow (Item 3 sub-session 3a — shipped 2026-05-24): `apps/web/src/server/lib/xendit/checkout.ts` pure helper composing payload from {plan_tier, billing_email, success_redirect_url, failure_redirect_url}; rejects free-tier at the function signature (PlanTier excluding 'free' as input type); currency=PHP locked, duration=86400s locked; `billing.checkout.create` adminProcedure mutation wires the helper with billing_email guard.
- ✅ `/api/webhooks/xendit` route handler with `x-callback-token` verification + BullMQ idempotency (Item 3 sub-sessions 3b-i + 3b-ii — shipped 2026-05-24): Next.js Route Handler at `apps/web/src/app/api/webhooks/xendit/route.ts` (POST only, no auth middleware per security.md webhook rule); constant-time signature verify via crypto.timingSafeEqual; graceful 503 degradation when XENDIT_WEBHOOK_TOKEN env var unset (dev/CI safety); enqueues to xendit-webhook BullMQ queue then returns 200 immediately. Pure handler `processXenditWebhook` in `apps/web/src/server/lib/xendit/webhook-worker.ts` handles invoice.paid (status='active'), invoice.expired (status='past_due', grace_period_end=now+7d), subscription.cancelled (status='cancelled'); two-layer idempotency (ProcessedWebhookEvent table @@unique(provider, external_event_id) at storage layer + Redis SETNX 24h fast-path); AuditLog every transition with PLATFORM:* prefix. Migration `20260524100300_xendit_webhook_idempotency` adds the ProcessedWebhookEvent table.
- ✅ Invoice list security hardening (Item 3 sub-session 3c-i — shipped 2026-05-24): invoices.list select shape no longer leaks `xendit_invoice_id` per security.md AGENT PROHIBITIONS #13 (internal correlation IDs do not belong in API wire shapes); regression test asserts xendit_invoice_id is NOT in response shape. Empty-state copy: "No invoices yet. Your first invoice will appear here after your initial payment."
- ✅ Failed-payment grace state machine (Item 3 sub-session 3c-ii-b — shipped 2026-05-24): full data layer. Migration `20260524140200_grace_period_suspension` adds `grace_period_end DateTime?` to Subscription and `'suspended'` enum value to SubscriptionStatus. xendit-webhook writes grace_period_end = now + 7d atomically with status='past_due' flip on invoice.expired. Cron worker `packages/jobs/src/workers/grace-sweeper.ts` (BullMQ `grace-sweeper:cron` at `0 */6 * * *`) scans Subscription where status='past_due' AND grace_period_end<=now → per-row $transaction (subscription.update(status='suspended') + organization.update(subscription_status='suspended', plan_tier='free', suspended_at=now) + writeAuditLog(PLATFORM:GRACE_PERIOD_EXPIRED)); uses platformPrisma to bypass L6 per security.md cron rule #7; try/catch per row so one tenant failure doesn't abort the sweep; filter shape (status='past_due' AND grace_period_end<=now) IS the idempotency guarantee — regression-guarded by a dedicated test that asserts the exact WHERE clause.
- ✅ Past-due grace period UI banner (Item 3 sub-session 3c-ii-a — shipped 2026-05-25): `apps/web/src/components/billing/past-due-banner.tsx` + `apps/web/src/lib/billing/past-due-banner-state.ts` (pure decision logic mirroring PlanLimitBanner extraction pattern for node-env testability); renders only when subscription.status='past_due' AND grace_period_end !== null; destructive Alert variant with formatted deadline + CTA link to /admin/billing; mounted in both `/app/layout.tsx` AND `/admin/layout.tsx` at shell top (max-w-7xl container) so the deadline is unmissable across the entire authenticated surface — DEPARTURE from PlanLimitBanner per-page mount because grace suspension is a hard deadline. Silent 403 degradation for non-admin sessions (retry:false on the tRPC hook prevents refetch loops). Uses local SubscriptionStatus string union per Rule 13 (no @yelli/db imports in client code).
- ⏸ Deferred to a later "billing polish" batch (NOT Item 3 scope per PRODUCT.md decomposition):
  - Invoice PDF download per row (PRODUCT.md "downloadable PDF receipts")
  - In-app 80% / 100% usage threshold notifications (PRODUCT.md L108)
  - 5-minute call duration warning (PRODUCT.md L108)
  All three are usage-metering concerns, not payment-flow concerns. Item 2's `PlanLimitBanner` + Item 3's `PastDueBanner` cover the in-flow gates; threshold-based alerts are a separate feature requiring a notification subsystem (in-app toast queue + email-via-SMTP-transport reuse).
- Aggregate cost across Items 2 + 3: 6 atomic commits on `feat/phase8-item3-xendit-billing-flow` + 4 atomic commits on `feat/phase8-item2-plan-tier-enforcement` = ~50 files / ~80 new test cases / 339 → 381 suite count / 2 migrations.

#### Super Admin Panel (SaaS) — 🟡 scaffolded-logic-stub
- ✅ `/superadmin` (190 lines) + `/superadmin/platform-settings` (199 lines) pages render,
   `superadmin.ts` tRPC router exists (using dedicated unguarded Prisma client per
   `.claude/rules/security.md` SUPERADMIN section)
- ⬜ Platform analytics — total tenants / total active calls right now / total revenue /
   growth-trend charts. Needs `platform.metrics` tRPC procedure on the unguarded Prisma client.
- ⬜ Tenant CRUD — list/detail/edit-plan/suspend/reactivate. Currently mock list.
   Needs `platform.tenants.list / .get / .updatePlan / .suspend / .reactivate` procedures.
   Every action logs to AuditLog with `PLATFORM:` prefix (security rule).
- ⬜ Revenue reports — revenue-by-period / by-plan-tier / by-payment-method with CSV/PDF export
- ⬜ Platform settings persistence — currently form renders but does not persist to
   `PlatformSettings` singleton entity. Needs `platform.settings.get / .update` procedures.
- ⬜ System health probes — LiveKit server status / database status / storage usage cards.
   Needs `platform.health` tRPC procedure that runs lightweight probes.
- Cost: ~12-15 files. Tier 3, single batch (Phase 8 Batch C).

#### Reports & Export — 🟡 scaffolded-logic-stub
- ✅ `/admin/reports` page renders (136 lines)
- ⬜ CSV generation — needs server-side `csv.generate(rows, columns)` helper using
   stream-based CSV builder; downloadable via signed-URL response or direct stream.
- ⬜ PDF generation — needs server-side PDF builder (puppeteer-core or pdf-lib).
   PRODUCT.md scope: usage summary / call detail records / revenue summary / department activity.
- ⬜ Date range presets (today / this week / this month / last 30/60/90 days) — UI exists
   but does not feed into the report fetch.
- ⬜ Scheduled reports — **explicitly out of scope per PRODUCT.md** ("none in v1").
- Cost: ~6-8 files. Tier 2.

### Phase 8 batch organization (proposed, not yet locked)

Batch labels match the proposal accepted on 2026-05-23 PM. Module routing to batch is shown
in the matrix above. Lock decisions and split plans live in `.cline/STATE.md` + this section
when each batch begins.

- **Batch A (closed — accepted 2026-05-23, completed 2026-05-25):**
  1. ✅ Pre-flight hardening (Tier 1) — CVE refresh + 9-guardrail DECISIONS_LOG section + IMPLEMENTATION_MAP refresh — shipped as `307963f`.
  2. ✅ Plan tier enforcement engine (Tier 2, planned 2 sub-sessions, executed as 4 atomic commits e985ca8 → 1232110 → 5bbeeb8 → a066621) — `packages/shared/plan-limits.ts` SoT + tRPC middleware + UI banner + page integration + 40 new tests. Branch `feat/phase8-item2-plan-tier-enforcement` squash-merged.
  3. ✅ Xendit payment + billing flow (Tier 3, planned 3 sub-sessions, executed as 6 atomic commits across `feat/phase8-item3-xendit-billing-flow`: 1b936e3 (3a checkout enrichment) → 5046397 (3b-i webhook route + queue scaffold) → 247275e (3b-ii webhook worker + ProcessedWebhookEvent idempotency) → 58238d6 (3c-i invoices.list security fix + empty-state copy) → f3b9798 (3c-ii-b grace period suspension state machine) → 2c22921 (3c-ii-a PastDueBanner UI)) — full payment flow live. 42 new test cases / 2 migrations / 6/6 atomic commits / branch squash-merged 2026-05-25 PM. See Billing module-by-module detail above for full artifact list. **Architect-Execute pattern locked**: ~20K Sonnet effective budget is the operating threshold — 5 of 6 Item 3 sub-sessions executed inline as Opus per §2.5b after re-estimate; 3c-ii-a (~13K) was the only clean Sonnet dispatch. Lessons.md proposes amendment to memory-governance.md §1 Step 2.5 to reflect this.
- **Batch B — Quick-wins (in progress — accepted 2026-05-25 via user "confirmed", 1 of 3 closed):**
  1. ✅ Real-time chat over Socket.IO (Tier 2 → pivoted to Tier 1 gap-find per [[existing-surface-scan-before-scaffold]]; pre-flight scan revealed Phase 7 realtime infra had scaffolded 80%+ of the pattern — actual scope: 4 new + 4 modified = 8 files, 11 new tests, suite 381 → 392, 0 migrations, ~50K Opus-inline session) — shipped on branch `feat/phase8-batchB-realtime-chat`, single atomic commit `feat(chat): realtime chat over Socket.IO — replaces 3s polling`, squash-merged to main 2026-05-25. See In-Call Chat module-by-module detail above for full artifact list.
  2. ⬜ Reports CSV/PDF generation (~6-8 files; Tier 2) — usage summary / call detail records / revenue summary / department activity per PRODUCT.md; server-side csv stream + PDF builder via pdf-lib or puppeteer-core; date range presets wiring on `/admin/reports`. Apply [[existing-surface-scan-before-scaffold]] before scoping.
  3. ⬜ LiveKit Egress recording feed (~8-10 files; Tier 3) — `/api/webhooks/livekit` route handler for `recording:started` / `recording:stopped` / `recording:failed` with signature verification + `Recording.status` state machine + `InCallRecordingIndicator` reactive feed via `meeting:{id}:recording` Socket.IO channel; PRODUCT.md splits storage: self-hosted → local MinIO, SaaS paid plans only → platform S3/R2. Apply [[existing-surface-scan-before-scaffold]] before scoping.
- **Batch C — Admin polish (proposed, not yet locked):** Super Admin platform analytics + tenant CRUD + revenue reports (3 Tier 2-3 sessions)

**Recommended next session:** Batch B sub-session 2 (Reports CSV/PDF). Run pre-flight grep against existing `/admin/reports` surface + any csv/pdf helpers in `apps/web/src/server/lib/` before scoping — may pivot to Tier 1 if the page already has data-feed wiring.

### Deferred follow-ups (kept here for governance traceability — still applicable)

- Mid-call moderator features (role promotion, kick, force-mute) — requires LiveKit Server SDK
- Test harness consolidation (dedicated test-suite Part once a 3rd batch's worth of routers stabilize)
- Part 6 (apps/mobile) — **PERMANENTLY SKIPPED** (Yelli is web-only Mobile-First responsive per
  inputs.yml `mobile.enabled: false`; PRODUCT.md does not declare a native mobile app)

## Modules (13 — per inputs.yml)

- Speed Dial Board (Intercom) — `/app`
- Video Calling — `/app/call`
- In-Call Chat — `/app/chat`
- File Sharing — `/app` (overlay)
- Whiteboard — `/app` (overlay)
- Recording — `/app/recordings`
- Meeting Management — `/app/meetings`
- Department Management — `/admin/departments`
- User Management — `/admin/users`
- Tenant Admin Dashboard — `/admin`
- Billing and Subscription (SaaS) — `/admin/billing`
- Super Admin Panel (SaaS) — `/superadmin`
- Reports and Export — `/admin/reports`

## Entities (13 — per inputs.yml)

Organization, User, Department, Meeting, CallLog, Participant, ChatMessage, Recording, SharedFile, WhiteboardSnapshot, Subscription (SaaS), Invoice (SaaS), PlatformSettings (SaaS singleton)

## Tech Stack (locked in DECISIONS_LOG)

- Frontend: Next.js 15 (App Router) + Tailwind + shadcn/ui (New York)
- API: tRPC v11 + Zod
- ORM: Prisma + PostgreSQL 16 + PgBouncer
- Cache + Jobs: Valkey 7 (MIT Redis fork) + BullMQ
- Storage: MinIO (dev/self-hosted) → S3/R2 (SaaS prod)
- Auth: Auth.js v5 beta (Credentials + bcrypt + securityVersion staleness check)
- Video: LiveKit self-hosted (SFU + Egress)
- TURN: Coturn self-hosted
- Realtime: Socket.IO (presence, chat, ringing)
- Payment: Xendit
- Bot protection: Cloudflare Turnstile
- UI: shadcn/ui + Tailwind + Recharts (via shadcn chart) + lucide-react + (Kibo UI on-demand)
- Deploy: Docker Compose + Komodo (V27 auto_update for staging, manual for prod) + Traefik (HTTPS routing)
- CI/CD: GitHub Actions — ci.yml (governance + quality matrix + security audit) + docker-publish.yml (multi-platform Docker Hub push)

## Port Assignments

Dev base: 43502 (random, locked in inputs.yml + .env.dev)
postgres=43502 · pgbouncer=43503 · valkey=43504 · minio=43505 · minio_console=43506
mailhog_smtp=43507 · mailhog_ui=43508 · pgadmin=43509 · app=43512 · worker=43513
prisma_studio=43522 · livekit_signal=43532 · livekit_turn_udp_start=43537 · coturn=43542
Staging: standard ports (postgres=5433, valkey=6380, minio=9010, pgadmin=5051, app=3000 behind Traefik)
Prod: standard ports (postgres=5432, valkey=6379, minio=9000, pgadmin=5050, app=3000 behind Traefik)

## File Counts (as of 2026-05-14 Phase 4 Part 8)

Source code (tracked in git):

- apps/web: 78 TS/TSX (Part 5a 27 + Part 5b 14 + Part 5c 11 + Part 5d 9 + Part 5e 13 + Part 5f 12 + components reorg = 78 unique) + 12 config = 90
- packages/shared: 16 files
- packages/api-client: 4 files
- packages/db: 13 files (10 src + schema.prisma + 2 migration SQL + migration_lock.toml)
- packages/ui: 21 files (15 components + 3 styles/utils + 3 config)
- packages/jobs: 10 files (8 src + 2 config)
- packages/storage: 6 files (4 src + 2 config)
- **Source subtotal: 160 files**

Infrastructure (tracked):

- tools/: 6 files (4 mjs + package.json + .eslintrc.cjs)
- deploy/: 25 files (dev 8 + stage 7 + prod 7 + start.sh + push.sh + k8s-scaffold/README.md)
- scripts/: 2 files (log-lesson.sh + sync-credentials-to-env.sh)
- **Infra subtotal: 33 files**

Governance + spec + meta (tracked):

- docs/: 7 files (PRODUCT, CHANGELOG_AI, DECISIONS_LOG, IMPLEMENTATION_MAP, DESIGN, YELLI_BUSINESS_MODEL, yelli-mockup-v2.jsx)
- Root spec: inputs.yml + inputs.schema.json
- Root config (Part 1): 8 dotfiles + .nvmrc
- Root meta: CLAUDE.md, AGENTS.md, COMMANDS.md, project.memory.md, package.json, pnpm-lock.yaml, pnpm-workspace.yaml, tsconfig.base.json, turbo.json, MANIFEST.txt, README.md
- .claude/rules/: 7 files
- .cline/: 12 files (9 tasks + STATE + lessons + agent-log)
- .vscode/: 3 files
- .github/: 4 files (ci.yml + docker-publish.yml + skills/SKILL.md + agents/n8n-architect.agent.md)
- **Governance subtotal: ~62 files**

**Tracked total: ~261 files** (Phase 7 #4 added `apps/web/src/server/lib/email.ts`, `apps/web/src/app/(auth)/reset-password/[token]/page.tsx`, `apps/web/src/app/(auth)/reset-password/[token]/_reset-form.tsx`, and the `password_reset_tokens` Prisma migration directory)

Local-only (gitignored — never committed): CREDENTIALS.md, .env.dev, .env.staging, .env.prod, .socraticodecontextartifacts.json, .specstory/history/*, .code-review-graph/*

See MANIFEST.txt at project root for the complete per-file inventory.

## ⏳ Pending Human Action Before Phase 5

Fill these sections in CREDENTIALS.md:

- 🐙 GitHub username + Personal Access Token
- 🐳 Docker Hub access token (username already locked as bonitobonita24)
- 📧 SMTP credentials (staging + prod — dev uses MailHog)
- 🦎 Komodo UI URL
- 💳 Xendit API keys (test + live + webhook token)
- 🛡️ Cloudflare Turnstile LIVE keys (prod only — dev/staging use test keys pre-filled)
- 🔑 LiveKit API key + secret + URL (Yelli core dependency — needed for the entire video stack)
- 🔁 Coturn static-auth-secret

Then run:

```bash
bash scripts/sync-credentials-to-env.sh
pnpm tools:check-env
```

When `check-env` passes with zero placeholder warnings, say `"Start Phase 5"` in a fresh
Claude Code session to begin the 9-command validation suite.

## ⚡ Post-Phase-4 Human Action — SocratiCode Initial Index

After this Part 8 squash-merge lands on main, trigger the SocratiCode initial index from
a Claude Code session **with Docker Desktop running**. The MCP server auto-pulls Qdrant
+ Ollama containers on first use (~5 minutes one-time).

```
In Claude Code, say: "Index this codebase"
→ codebase_index {}
→ codebase_status {} (poll until complete)
→ codebase_context_index {} (indexes the 4 artifacts from .socraticodecontextartifacts.json)
```

Once indexed, every Phase 7 Feature Update auto-uses `codebase_search` (Rule 17) before
opening any file, saving an estimated 61% of read tokens vs grep-based exploration.
