# pnpm audit threshold.
# Was: audit-level=high. Raised to `critical` on 2026-05-17 (Phase 7 #9 ticket (j))
# to accept the unfixed HIGH advisory GHSA-rcmh-qjqh-p98v (nodemailer addressparser
# DoS, <=7.0.10) — pinned to 6.9.16 by the Auth.js v5 peer range. The exploit path
# requires user-controlled address strings reaching the parser; apps/web/src/server/lib/email.ts
# only feeds server-stamped addresses. See DECISIONS_LOG.md "Unfixed CVE acceptance —
# nodemailer GHSA-rcmh-qjqh-p98v" and lessons.md [[nodemailer-cve-mitigation]] for the
# full rationale and revisit triggers.
audit-level=critical
